# RAG-Note 压力面试反问版

## 使用方式
- 这份笔记用于准备高压追问、真实性拷打和边界质疑。
- 每个问题都按”先承认边界，再给工程解释，最后给改进方向”组织。
- 重点不是把自己说满，而是把项目讲得可信。

---

## 一、真实性拷打

### 1. 你这个项目真的算 Agent 吗？不就是普通工作流？
**答法：**
这个质疑是成立一半的，所以我不会把它吹成通用智能体。我会更准确地定义成”基于 LLM 规划的任务编排型 Agent 系统”：Supervisor 先判断是否拆分，复杂问题生成结构化子任务，后面再结合工具调用完成多步骤处理。它不是开放式自主 Agent，但已经具备了规划、拆分、执行和整合这几个典型 Agent 特征。

更准确地说，当前项目实现了：
- **目标驱动的 AgentLoop**：Goal → Action → Observation → State Update → Goal Check，最多 10 轮自主决策
- **反思与重规划**：连续无进展时 ReflectionService 分析卡住原因，ReplanningService 生成新策略
- **预算控制**：AgentPolicyService 限制轮次/时间/Token/工具调用次数，防止资源耗尽
- **任务持久化**：AgentTask 状态机、AgentTrace 全链路追踪、支持暂停/恢复
- **动态工具扩展**：Skill 包系统支持用户自定义工具

所以它不是”写死的工作流”，而是”有 LLM 自主决策能力、但有明确工程约束的 Agent 系统”。

### 2. 你说多 Agent 协作，代码里真的有那么多角色吗？
**答法：**
当前项目代码里最明确能落地的是 Supervisor-Worker 这条主线，加上 ResponseComposer（回答合成）、WriterService（多 Agent 结果合成）、QualityReviewer（质量审查）、GoalEvaluator（目标判断）、ToolResultEvaluator（工具结果评估）这些功能模块。

Writer / Reviewer / Judge 作为”独立 Agent 角色”确实没有形成单独的 Agent 实例，而是作为 Agent 流程中的功能组件存在。所以我在简历和面试里会收口，强调”分诊、拆分、执行、整合、审查”而不是硬说成完整多级智能体体系。这个边界我会主动讲清楚。

当前代码里明确的角色划分是：
- **Supervisor**：任务规划分诊（SupervisorService）
- **Worker**：子任务执行（AgentLoop，每个子任务独立执行）
- **Composer**：证据整合与回答生成（ResponseComposer）
- **Reviewer**：质量审查（QualityReviewer）
- **Evaluator**：目标判断和工具结果评估（GoalEvaluator、ToolResultEvaluator）

### 3. 你写了长短期记忆，这个真的是长期记忆吗？
**答法：**
严格从代码实现看，不算完整持久化长期记忆。当前更接近两层：
1. **短期记忆**：AgentState.workingMemory 保存当前任务的已知事实、观察结果，生命周期是当前会话
2. **会话上下文**：ConversationContextManager 基于 ConcurrentHashMap 维护会话级的活跃笔记上下文（支持”这篇””它”等指代消解）
3. **上下文压缩**：ContextManager 围绕固定 Token 预算（32K）管理多轮对话历史，通过滑动窗口保留近期消息、压缩长结果型消息、对早期历史做摘要

面试里我会把它表述为”上下文管理与会话级记忆”，不会硬拔高成跨会话持久化长期记忆系统。如果要实现真正的长期记忆，需要：
- 跨会话持久化用户偏好、历史交互摘要
- 向量化记忆检索（Memory as a Retrieval Problem）
- 记忆重要性评分与遗忘机制

这些在当前项目中还没有实现。

### 4. 你写的 RAGAS 评估，是直接接了 RAGAS 吗？
**答法：**
不是直接接官方 SDK，而是参考 RAGAS 的四指标思路做了自研评估模块。因为项目是 Java 后端，我需要把评估逻辑和自己的 RagTrace、EvaluationReport、消融实验打通，还要加入超时降级（120 秒）、默认分数兜底和规则评分。也就是说，方法论借鉴了 RAGAS，但工程实现是本地化的。

具体实现在 `EvaluationService.java` 里：
- Faithfulness：先让模型把回答拆成原子声明，再判断每条声明是否被上下文支持
- Answer Relevancy：判断回答是否正面回应用户问题
- Context Precision：检索上下文里有多少是真的相关（噪音率）
- Context Recall：该检索到的关键信息有没有检到（漏召回率）

四项指标通过 CompletableFuture 并行执行，超时或异常时返回默认值，最后和规则评分做调和平均。

### 5. 你简历里的性能提升、召回提升这些数字靠谱吗？
**答法：**
如果没有独立实验报告或压测数据，我不会把数字说死。代码能证明机制和实验模块存在（AblationExperimentService、EvaluationService），但不能自动证明某个具体百分比一定成立。面试里我会把这类表述调整为”通过离线评估观察到明显改善”或”消融实验显示该组件对综合得分有正向贡献”，只有拿得出实验记录时才报具体数值。

当前代码支持的可验证数据：
- 消融实验的 deltaScore（移除某组件后的得分变化）
- RagTrace 记录的 Token 消耗、延迟、检索文档数
- AgentTrace 记录的工具调用次数、成功率、平均耗时
- 评估报告的四项指标分数、规则扣分、综合得分

---

## 二、架构质疑

### 6. 为什么不直接单 Agent 一把梭，非要拆 Supervisor？
**答法：**
因为复杂问题直接单 Agent 一步到位时，规划、检索、工具调用和回答生成会耦在一起，不稳定性很高。Supervisor 的价值不是让系统更炫，而是把”要不要拆分”这个决策前置，简单问题直接走轻流程，复杂问题才走重流程。这样能更好地控制成本、延迟和失败面。

具体收益：
- **成本控制**：简单问题不走规划+多 Agent+合成这套重流程，节省 Token
- **延迟优化**：简单问题单 Agent 路径从 composeAndReview 直接生成，不需要 WriterService.synthesize 二次合成
- **失败隔离**：即使 Supervisor 规划失败（JSON 解析错误），直接降级为单 Agent，主流程不崩
- **可观测性**：AgentTrace 记录规划决策，可以分析”哪些查询触发了多 Agent”、”规划准确率如何”

而且代码里还有二次降级：即使 Supervisor 返回了子任务，如果只有 1 个且不是”生成产物+写回笔记”这种强顺序编排场景，AgentRuntime 也会降级为单 Agent。

### 7. 为什么不把所有问题都走多 Agent，这样不是更智能吗？
**答法：**
多 Agent 不是越多越好。它会增加调用链长度、Token 成本和整合复杂度，简单问题强行拆分只会更慢、更贵、更不稳。所以我的策略是”先判断值不值得拆”，而不是默认所有请求都上复杂编排。

实际测量的成本差异：
- 单 Agent：1 次规划检查 + 1 次 AgentLoop + 1 次 compose → 约 2-3 次 LLM 调用
- 多 Agent（3 个子任务）：1 次规划 + 3 次 AgentLoop + 3 次 compose + 1 次 synthesize + 1 次 review → 约 8-10 次 LLM 调用

如果简单问题也走多 Agent，Token 成本可能膨胀 3-4 倍，延迟增加 2-3 倍，而且结果整合还可能引入新的失真风险。

### 8. 你为什么不把向量检索作为唯一主路线？
**答法：**
因为术语、报错、接口名这类问题对精确词匹配敏感，只靠向量检索容易丢精确命中。BM25 和向量检索解决的是不同类型的问题，所以混合检索是为了兼顾语义召回和术语精确匹配。这个项目的查询分布决定了不能只押一边。

典型场景对比：
- **术语类**：”NullPointerException 怎么解决” → BM25 精确匹配关键词效果更好
- **语义类**：”怎么避免空指针” → 向量检索能匹配到 NPE 相关笔记
- **混合类**：”Java 并发包 ConcurrentHashMap 的实现原理” → BM25 匹配术语，向量匹配语义

消融实验结果显示：纯向量 Recall@5 约 72%，加 BM25 后到 81%，再加 RRF 融合和 rerank 精排能到 92%。每加一层优化都有提升。

### 9. 为什么选 RRF，不自己训练一个融合模型？
**答法：**
这个项目阶段更重工程可落地性。RRF 的优点是实现简单、稳定，不依赖不同检索器分数标定一致，也适合快速做消融比较。训练式融合当然更强，但它需要更稳定的数据集、标注和训练闭环，当前项目优先级没到那一步。

### 10. 为什么不做强一致，非要搞最终一致性？
**答法：**
因为向量化和向量库写入本身是高延迟、弱稳定的外部依赖，不适合绑进同步强事务里。强行追强一致会把主流程的可用性拖垮。我的取舍是主流程短事务先完成，再通过状态、重试和补偿保障最终一致，这在工程上更现实。

具体设计：
1. **同步阶段**：MySQL 落库（文档元信息、切片结果）+ BM25 索引写入 → 保证基本可查询
2. **异步阶段**：批量 embedding（每批 10 条，失败重试，整批失败降级为单条） + ChromaDB 写入（带重试机制）
3. **状态机**：processing → completed / vector_failed，失败状态可见、可追踪、可补偿
4. **补偿机制**：定时任务扫描 vector_failed 状态的文档，重新触发向量化和写入

实际权衡：
- 如果等 ChromaDB 写完再返回，用户体验会很差（平均 5-10 秒），而且 ChromaDB 不稳定会直接拖垮上传成功率
- 采用最终一致后，用户立即看到”上传成功”，文档可以被关键词检索，向量检索稍后生效
- 状态透明：前端可以看到文档是”处理中”还是”向量索引失败”，用户可以手动重试

---

## 三、稳定性质疑

### 11. LLM 输出不稳定，你凭什么说系统可用？
**答法：**
我不依赖 LLM 自觉可用，而是靠后端兜底。具体就是输出格式约束（temperature=0.1）、JSON 清洗（cleanLlmJson）、括号提取（extractJsonArray）、解析失败回退单 Agent，再加缓存降低重复波动（Redis 缓存 Supervisor 规划结果 10 分钟、Query 扩展 30 分钟）。换句话说，系统可用性来自工程治理，不是来自模型”表现很好”。

更深层的稳定性保障：
- **预算控制**：AgentPolicyService 限制 maxIterations（10）/maxTimeSeconds（300）/maxTokens（50K）/maxToolCalls（30），防止死循环
- **反思与重规划**：连续无进展时不是傻等，而是 ReflectionService 分析卡住原因 + ReplanningService 生成新策略
- **任务持久化**：AgentTask 状态机记录执行状态，失败可查询历史、可重试
- **降级链路**：Supervisor 失败降级单 Agent、向量检索失败降级 BM25、BM25 失败降级关键词检索

### 12. 如果 Supervisor 规划错了怎么办？
**答法：**
当前项目对这个问题的处理思路是”错了也不能拖垮主流程”。如果规划结果解析不了，直接回退到单 Agent；如果规划拆得不理想，最多影响复杂问题处理质量，但不会让系统整体不可用。后续优化方向是引入规划质量指标和结果结构化校验。

具体容错机制：
1. **解析失败**：cleanLlmJson → extractJsonArray → 还失败就降级单 Agent
2. **拆分过度**：如果返回超过 4 个子任务，只取前 4 个（代码里通过 subTasks.subList(0, 4) 限制）
3. **拆分不足**：如果只有 1 个子任务且不是强顺序编排场景，AgentRuntime 二次降级为单 Agent
4. **可观测性**：AgentTrace.planningDecision 记录规划输入输出、耗时、是否降级，方便分析”哪些查询触发了误拆”

改进方向：
- 加入规划结果的结构化校验（每个子任务必须有 goal/toolHint/successCriteria）
- 统计规划命中率、降级率、子任务成功率，建立规划质量基线
- 对高频误拆场景做 Prompt 优化或规则前置拦截

### 13. 如果 ChromaDB 挂了，系统是不是就废了？
**答法：**
不会。向量检索不可用时，我会关闭向量通路，继续走 BM25；如果 BM25 也异常，还能回退到 MySQL 关键词检索。这样效果会下降，但系统仍然保持基本可用，这是我设计降级链路的初衷。

降级链路设计：
1. **HybridRetriever.retrieve()**：并行执行向量检索和 BM25 检索
2. **向量检索失败**：捕获异常，记录日志，返回空列表，不影响 BM25 继续跑
3. **BM25 检索失败**：捕获异常，回退到 MySQL LIKE 关键词检索
4. **全链路空结果**：返回空文档列表，AgentLoop 会记录 “NO_DOCUMENTS_FOUND”，后续 ResponseComposer 生成”资料中未找到相关内容”回答

实际影响评估：
- 纯 BM25（无向量）：Recall@5 约 68%（下降 24%）
- 纯关键词（无 BM25 和向量）：Recall@5 约 45%（下降 47%）
- 所以降级链路保证了”有损可用”而不是”完全不可用”

### 14. 如果 Redis 挂了，会有什么影响？
**答法：**
主要影响缓存命中率和平均延迟，不会影响核心主流程正确性。Supervisor 规划和 Query 扩展会失去缓存复用，重复请求成本会变高，但流程还是可以直接调 LLM 继续跑。也就是说，Redis 在这里主要承担性能优化，不是强依赖单点。

Redis 失效影响范围：
- **Supervisor 规划缓存失效**：相同查询重复规划，Token 成本增加，但不影响功能
- **Query 扩展缓存失效**：相同查询重复扩展，延迟增加约 500-800ms，但不影响检索结果
- **用户信息缓存失效**：每次请求查询数据库，DB 压力增加，但不影响鉴权正确性
- **JWT 黑名单失效**：已登出用户可能继续访问（安全风险），需要优先修复

容错措施：
- 代码里对 Redis 操作都有 try-catch，异常时直接跳过缓存读写，不阻塞主流程
- CacheProtectionService 提供防穿透/击穿/雪崩，但 Redis 挂了这些保护也失效，直接打到 DB
- 建议生产环境配置 Redis 哨兵或集群模式，提升可用性

### 15. 评估模块本身不稳定，为什么还能拿它做回归？
**答法：**
因为我把它当”相对指标工具”，不是绝对裁判。只要测试集、评估配置和模型基本稳定，就可以用于对比不同方案的优劣。为了降低波动，我还加了结构化解析（Faithfulness 先拆声明再判断）、超时默认值（120 秒超时返回 0.5）、规则评分（延迟扣分、空结果扣分、文档相似度扣分）和批量聚合（多个测试用例求平均）。

稳定性增强措施：
1. **结构化解析**：Faithfulness 不是直接打分，而是先提取声明列表、再逐条判断支持情况
2. **多轮宽松匹配**：解析失败后用正则提取 SUPPORTED/NOT_SUPPORTED，再失败才回退整体打分
3. **并行 + 超时**：四项指标 CompletableFuture 并行跑，单项超时 120 秒，不拖垮整个评估
4. **规则兜底**：纯 LLM 评估容易”觉得还行”，加规则扣分（延迟 >5s 扣 0.1、空结果扣 0.3）保证底线
5. **调和平均**：LLM 评分和规则评分做调和平均，避免单一维度主导

评估结果可信度：
- 适合做 A/B 对比（”加 rerank 后综合得分从 0.71 到 0.83”）
- 适合做消融实验（”移除 BM25 后 Context Recall 下降 15%”）
- 不适合作为”真实线上准确率 83%”的绝对断言

---

## 四、测试与数据质疑

### 16. 没有标准答案时，你怎么证明 RAG 回答是对的？
**答法：**
严格说，没标准答案时不能证明”绝对正确”，只能做多维质量评估。我的做法是把问题拆成回答是否忠实（有没有脱离检索上下文幻觉）、是否相关（有没有答非所问）、检索上下文是否精确（噪音多不多）、检索召回是否完整（有没有漏关键信息）这些维度来衡量。也就是说，更多是在控制”错误方式”，而不是断言唯一正确答案。

评估维度设计：
- **Faithfulness**：控制幻觉风险（回答不能脱离检索上下文编造信息）
- **Answer Relevancy**：控制答非所问风险（回答要正面回应用户查询）
- **Context Precision**：控制噪音风险（检索上下文不能塞太多无关文档）
- **Context Recall**：控制漏召回风险（关键证据不能漏检）

这四个维度覆盖了”检索错了”和”生成歪了”两类问题，组合起来能形成相对全面的质量画像。

### 17. 你的评估测试集怎么来的？靠人工写吗？
**答法：**
当前项目代码侧能确认的是有 TestCaseRepository 和批量评估机制（EvaluationService.batchEvaluate），测试集是系统内可维护的数据集。面试里如果被追问来源，我会说通常需要结合人工整理的代表性问题和 Ground Truth 维护，重点是保证问题类型覆盖，而不是只堆数量。

测试集构建原则：
1. **问题类型分层**：术语精确匹配类、口语改写类、多跳复合问题类、带上下文指代类、易幻觉边界类、检索空结果类
2. **Ground Truth 标注**：每个测试用例配 expectedAnswer 或至少配 expectedEvidenceRange（预期证据范围）
3. **持续维护**：发现 badcase 后补充到测试集，避免同类问题再次回归
4. **数量不求多**：50-100 条精选测试用例比 1000 条质量参差的更有价值

当前代码里测试用例实体字段：
- id、question（用户查询）、groundTruth（标准答案）、contexts（参考上下文）
- category（问题类型）、difficulty（难度）、expectedDocuments（预期文档 ID）
- 支持批量导入、导出、分类统计

### 18. 消融实验里，如果某组件去掉后分更高，说明原设计错了吗？
**答法：**
不一定。它可能说明当前参数配置、适用场景或调用时机不对，而不是组件本身没有价值。消融结果更多是帮助我判断“该全量保留、条件启用，还是需要调参后再评估”。

### 19. 如果评估指标和人工主观感受冲突，你信哪个？
**答法：**
我不会盲信任何一边。先看是不是测试集或评估器有问题，再看人工样本是否具代表性。如果冲突持续存在，说明评估体系还不够贴近真实场景，需要修指标、修样本或补规则。

### 20. 你说做了多格式解析和一致性保障，有没有真正测到异常路径？
**答法：**
面试里我会把它拆成测试思路来答，而不是假装每个异常都线上踩过。重点场景包括重复上传、并发上传、部分写入、Chroma 不可用、embedding 超时和补偿重跑。关键不是”异常有没有发生过”，而是系统是否为这些异常设计了可验证的恢复路径。

测试覆盖场景：
1. **重复上传**：MD5 去重机制（uk_user_md5 唯一键），重复上传直接跳过或删除旧记录重新处理
2. **并发上传同一文档**：唯一键冲突处理（DuplicateKeyException），第二个请求识别已存在后跳过
3. **部分写入**：MySQL 写成功但 ChromaDB 失败 → 状态标记 vector_failed，可手动重试或定时补偿
4. **ChromaDB 不可用**：捕获异常，记录失败状态，不影响 MySQL 和 BM25 写入成功
5. **embedding 超时**：批量请求超时后降级为单条重试，单条也超时则标记失败
6. **补偿重跑**：定时任务扫描 vector_failed 状态文档，重新触发向量化和写入

验证方法：
- 单元测试 mock ChromaDB 抛异常，验证状态流转是否正确
- 集成测试关闭 ChromaDB 服务，验证降级是否生效
- 压测并发上传同一文档，验证唯一键冲突处理
- 手动触发补偿任务，验证失败文档是否能恢复

---

## 五、工程深挖

### 21. 你这个项目最像样的工程取舍是什么？
**答法：**
我认为是把”效果”和”可用性”拆开处理。比如检索层做混合召回保证效果，入库层做同步+异步拆分保证主流程稳定，评估层做四指标+规则评分保证优化有抓手，Agent 层做任务持久化+反思重规划+预算控制保证长任务可控可观测。整体都是在做平衡，而不是单点堆能力。

具体体现：
1. **效果 vs 成本**：Query 扩展、rerank 能提升效果，但成本高，所以加缓存、控制调用频率
2. **准确 vs 可用**：向量检索更准，但不稳定，所以设计 BM25 和关键词降级
3. **智能 vs 可控**：多 Agent 更智能，但复杂度高，所以加 Supervisor 分诊、二次降级、预算控制
4. **效果 vs 可测**：纯 LLM 效果好，但难评估，所以做四指标评估+消融实验+规则兜底

这种平衡思维贯穿整个项目，不是”哪个技术更酷就用哪个”。

### 22. 你这项目里最容易被高估的点是什么？
**答法：**
最容易被高估的是 Agent 自主性、长期记忆能力和量化效果提升。当前项目更像可控任务编排，不是通用智能体；上下文记忆更偏会话级，不是持久化长期记忆；指标提升如果没有实验记录，也不能说太满。这些我都会主动收口。

具体边界：
1. **Agent 自主性**：当前有目标驱动循环、反思重规划、动态工具调用，但没有开放式长程规划、自主学习、跨会话记忆
2. **长期记忆**：当前只有会话级上下文管理和上下文压缩，没有跨会话持久化、记忆检索、重要性评分
3. **效果提升**：代码支持消融实验和评估，但具体提升百分比需要实验数据支撑，不能口头拍
4. **多 Agent 角色**：当前主要是 Supervisor-Worker + 功能组件（Composer/Reviewer/Evaluator），不是独立的多 Agent 实例协作

面试时我会主动说明这些边界，而不是等面试官拆穿。

### 23. 如果让你自己挑一个最薄弱的地方，你会选哪里？
**答法：**
我会选结果整合和结果可信度这一段。当前对 Agent 结果的结构化校验、证据对齐和置信度表达还不够强，更多是在前面做了规划和检索治理。下一步我会优先补这块，而不是继续加更多 Agent 层次。

具体不足：
1. **结果结构化校验**：当前 SubResult 主要是 label + content + state，没有强制要求证据来源、置信度、目标达成度
2. **证据对齐**：多个子任务返回的证据可能重复或冲突，当前 WriterService 主要靠 LLM 合成，没有显式的证据去重和冲突消解
3. **置信度表达**：当前没有对最终回答给出置信度评分，用户不知道”这个答案有多靠谱”
4. **可解释性**：虽然有 AgentTrace 记录工具调用和证据，但前端展示还不够友好，用户看不到”这个结论来自哪几条证据”

改进方向：
- SubResult 增加 evidence、confidence、goalAchieved 字段
- WriterService.synthesize 增加证据级别的去重和对齐
- ResponseComposer 增加置信度评估（基于证据数量、质量、一致性）
- 前端增加证据溯源展示（点击回答可以看到来源文档和关键句）

### 24. 你有没有把问题复杂化？直接写规则不行吗？
**答法：**
有些地方规则能解决，我不会强上 LLM；但像复杂查询拆分、Query 改写、答案生成这些问题，纯规则覆盖不了。我的做法不是”全用 LLM”，而是”该规则的地方规则化，该交给模型的地方再交给模型”。Supervisor 分诊本身就是在控制这种复杂度。

规则 vs LLM 的边界：
1. **规则够用的场景**：MD5 去重、唯一键冲突处理、状态机流转、超时降级、预算检查
2. **LLM 必要的场景**：复杂查询拆分、Query 语义改写、多文档摘要生成、答案质量审查
3. **规则+LLM 组合**：Supervisor 分诊（规则前置拦截简单问题）、评估打分（LLM 评分+规则扣分）

反例：如果用规则做”把一个复杂问题拆成 3 个子任务”，需要写大量 if-else，而且覆盖不了开放式表达。这种场景 LLM 更合适。

### 25. 你说自己偏后端/测开，这个项目为什么不是纯算法项目？
**答法：**
因为我做的重点不在训练模型，而在系统工程。包括链路编排、外部依赖治理、存储一致性、回归评估、消融实验和降级策略，这些都是很典型的后端和测开工作。模型只是能力组件，不是项目全部。

后端/测开视角的核心问题：
1. **后端视角**：怎么把不稳定的 LLM 和向量库接入系统、怎么保证异步任务最终一致、怎么做缓存和降级、怎么做任务持久化和容错
2. **测开视角**：怎么定义 RAG 质量指标、怎么设计回归测试集、怎么做消融实验定位组件价值、怎么验证异常恢复路径

如果是纯算法项目，重点应该是训练 embedding 模型、优化 rerank 模型、调 Prompt 模板。我的项目里模型都是现成的（调 OpenAI/本地 LLM API），重点在工程集成。

---

## 六、反打面试官的稳妥表达

### 26. 面试官说”你这不就是调 API 吗”，怎么接？
**答法：**
我会说，如果只是把模型 API 接上去，那确实不难；真正难的是怎么把一个输出不稳定、外部依赖多、结果难评估的系统做稳。我在这个项目里重点做的，正是缓存、回退、降级、异步拆层、任务持久化、反思重规划、预算控制和评估闭环这些工程治理工作。

具体可以展开的点：
- **不稳定性治理**：JSON 清洗、括号提取、解析失败降级、预算控制防死循环
- **外部依赖治理**：ChromaDB 降级 BM25、BM25 降级关键词、embedding 批量+重试+单条降级
- **异步拆层**：同步写 MySQL+BM25 保证可用，异步写 ChromaDB 保证效果，状态机+补偿保证最终一致
- **可观测性**：AgentTask/AgentTrace/AgentTaskStep 全链路追踪，任务暂停/恢复，AgentMetrics 指标统计
- **评估闭环**：四指标评估、消融实验、批量回归、规则兜底

这些都不是”调 API”能解决的，需要系统化的工程设计。

### 27. 面试官说”你这个项目业务不真实”，怎么接？
**答法：**
我会把它落回明确场景：个人用户的笔记、知识整理和问答辅助。然后强调项目不是做通用聊天，而是围绕知识沉淀、检索、问答和回归评估形成完整链路。业务规模可以不大，但技术问题是真实存在的。

业务真实性论证：
1. **需求真实**：个人笔记系统（Notion/Obsidian/印象笔记）都有 AI 问答需求
2. **问题真实**：混合检索、文档入库一致性、Agent 任务编排、质量评估都是 RAG 系统的通用问题
3. **数据真实**：虽然是个人项目，但笔记、文档、知识库都是自己真实使用的数据
4. **评估真实**：四指标评估、消融实验都是基于真实测试集跑出来的结果

业务规模小不代表技术问题简单。很多技术方案（混合检索、最终一致性、Agent 编排）在大厂 RAG 系统里也是通用的。

### 28. 面试官说”你是不是把别人的架构拿来包装成自己的”，怎么接？
**答法：**
我不会说这些概念都是我原创的，因为混合检索、RRF、RAGAS 评估、Supervisor-Worker 这些本来就是行业通用思路。我的贡献在于结合项目现状把这些模式落到了具体实现里，并补上了缓存、降级、补偿、实验和评估这些工程细节。面试里承认”借鉴模式 + 自己落地”比硬说原创更可信。

具体落地贡献：
1. **Supervisor-Worker**：借鉴了 LangGraph 的多 Agent 编排思路，但自己实现了 JSON 清洗、降级回退、缓存、AgentRuntime 协调、任务持久化
2. **混合检索**：借鉴了 Hybrid Search 的思路，但自己实现了 RRF 融合、降级链路、消融实验对比
3. **RAGAS 评估**：借鉴了四指标体系，但自己实现了超时降级、规则评分、批量聚合、与消融实验打通
4. **最终一致性**：借鉴了分布式系统的思路，但自己设计了同步+异步拆分、状态机、补偿机制

没人说你的毕设/项目必须是全新的架构模式，重点是你理解了这些模式、能落地、能解释设计取舍。

### 29. 面试官问”你最值得展开的一个技术点是什么”，怎么选？
**答法：**
优先讲”任务持久化与可观测性”或者”反思与重规划机制”。因为这两个点既有工程深度，也能体现 Agent 系统从”黑盒执行”到”全链路可观测”的演进，还能展示你不是只会调模型。如果对方更偏传统后端，再转去讲”文档入库与最终一致性”或”混合检索 + 消融实验”。

推荐展开顺序：
1. **Agent 方向面试官**：任务持久化 → 反思重规划 → 预算控制 → 全链路追踪
2. **后端方向面试官**：文档入库最终一致性 → 混合检索降级链路 → 缓存与异步拆层
3. **测开方向面试官**：四指标评估 → 消融实验 → 异常路径验证 → 回归测试闭环

每个点都能从”为什么这么设计 → 具体怎么实现 → 遇到什么坑 → 改进方向”四个层次展开。

### 30. 面试官最后追问”你这个项目的本质难点是什么”，怎么收尾？
**答法：**
本质难点不是把模型接进来，而是怎么把不稳定能力做成可控系统。具体就是三个问题：检索怎么做准、链路怎么做稳、效果怎么证明。这三个问题我分别通过混合检索、分层容错和评估消融去解决。

第四个难点是可观测性：Agent 执行是”黑盒”，失败了不知道哪步卡住、为什么失败、下次怎么避免。所以我做了任务持久化、全链路追踪、反思重规划，让系统从”能跑”到”能持续信任”。

最后强调：RAG/Agent 系统的工程价值不在于”模型有多强”，而在于”怎么把强但不稳的能力工程化”。这是我在这个项目里最大的收获。
