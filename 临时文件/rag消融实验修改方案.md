# RAG 消融实验修改方案（最终定稿）

> 目标：**不是证明某个 RAG 模块理论有效，而是在当前项目场景下找到最优可落地 Pipeline，并判断每个环节是否值得存在。**
> 
> 评价体系：效果（综合得分）× 成本（耗时 + Token）双指标驱动，使用 **收益/成本比** 进行模块价值判断。

---

## 一、当前已有实验数据（基线）

| 实验 | 综合得分 | Δ效果 | 状态 |
|------|---------|-------|------|
| Full（完整流水线） | 96.2 | — | 基线 |
| w/o Vector Search | 30.2 | -64.7% | ✅ 结论清晰，必须保留 |
| w/o BM25 Search | 91.4 | -3.5% | ✅ 有贡献，需看成本 |
| w/o Reranker | 91.5 | -3.4% | ✅ 有贡献，需看成本 |
| w/o Retrieval QualityReviewer | -9.0% | 明显下降 | ✅ 需看成本 |
| w/o RRF Fusion | 97.5 | +1.3% | ⚠️ 疑似负收益，需优化验证 |
| w/o Query Expander | 97.0 | +0.8% | ⚠️ 疑似负收益，需优化验证 |
| w/o Source Attribution | +0.4% | 基本无影响 | ✅ 移出消融，改功能测试 |

---

## 二、整体路线（4 个 Phase + 决策矩阵）

```
Step 0: 数据采集改造
  确保每次实验记录完整成本数据
  ↓
Phase 1: 基础参数优化
  Chunk Size → Overlap → TopK → 参数组合确认
  ↓
Phase 2: 模块价值评估
  逐模块消融，计算收益/成本比
  ↓
Phase 2.5: 问题模块优化
  RRF（参数调整 + 架构调整）
  QE（降温 + 条件触发 + 实体保持约束）
  ↓
Phase 3: 候选 Pipeline 对比
  6 个候选方案输出决策矩阵
  ↓
最终输出：最优 Pipeline + 各模块收益/成本表
```

---

## 三、Step 0：数据采集改造（半天）

**这是后续所有实验的基础设施，必须先做。**

每次实验必须记录：

| 字段 | 来源 | 说明 |
|------|------|------|
| experiment_id | 实验编号 | 如 R-1, R-3 |
| score | EvaluationService | 综合得分 |
| latency | RagTrace.totalLatencyMs | 端到端耗时 |
| retrieval_time | RagTrace.retrievalLatencyMs | 检索阶段耗时 |
| generation_time | RagTrace.generationLatencyMs | 生成阶段耗时 |
| input_token | TokenCounter | 输入 Token 数 |
| output_token | TokenCounter | 输出 Token 数 |

**评价指标定义：**

```
效果收益（ΔScore） = 模块开启得分 - 模块关闭得分

成本增量（ΔCost）  = latency增加比例 + token增加比例
                    = (L_open - L_close) / L_close + (T_open - T_close) / T_close

模块收益率          = ΔScore / ΔCost
```

> 不使用 `百分比/ms` 的原因是不同机器、不同模型环境下不具备通用性。

---

## 四、Phase 1：基础参数优化（半天）

### 4.1 实验设计

采用**单因素逐步优化 + 最终组合确认**策略，不做全排列。

#### Round 1：Chunk Size

| chunk_size | chunk_overlap | 综合得分 | Latency | Token | 说明 |
|------------|--------------|---------|---------|-------|------|
| 200 | 20（固定） | baseline | baseline | baseline | 当前默认 |
| 400 | 20（固定） | ? | ? | ? | — |
| 800 | 20（固定） | ? | ? | ? | — |

选出最佳 size。

#### Round 2：Overlap（固定 Round 1 最佳 size）

| overlap | 综合得分 | Latency | Token | 说明 |
|---------|---------|---------|-------|------|
| 0 | ? | ? | ? | 无上下文重叠 |
| 20 | ? | ? | ? | 当前默认 |
| 50 | ? | ? | ? | 中等重叠 |
| 100 | ? | ? | ? | 高重叠，适合大 chunk |

#### Round 3：TopK（固定 Round 1+2 最佳参数）

| topK | 综合得分 | Latency | Token | 说明 |
|------|---------|---------|-------|------|
| 3 | ? | ? | ? | 精确但可能遗漏 |
| 5 | ? | ? | ? | — |
| 10 | baseline | baseline | baseline | 当前默认 |
| 20 | ? | ? | ? | 覆盖广但可能过载 |

#### Phase 1.5：参数组合确认

| 方案 | chunk_size | overlap | topK | 综合得分 | Latency | 说明 |
|------|-----------|---------|------|---------|---------|------|
| 当前基线 | 200 | 20 | 10 | baseline | baseline | 对照 |
| 最佳组合 | best | best | best | ? | ? | 单因素优化结果 |
| 次优组合 | second | second | second | ? | ? | 确认非偶然提升 |

> 目的：单因素逐步优化可能错过组合最优，这一步做最后确认。

---

## 五、Phase 2：模块价值评估（半天）

### 5.1 实验设计

固定 Phase 1 确定的最佳参数，7 组消融实验。

| 编号 | 实验 | 目的 | 预计决策 |
|------|------|------|---------|
| R-0 | Full Pipeline（完整） | 基线 | — |
| R-2 | w/o Vector Search | 验证语义检索必要性 | 保留（-64.7%，已有结论，但仍采集成本数据） |
| R-3 | w/o BM25 Search | BM25 是否值得 | 看收益率 |
| R-5 | w/o Reranker | Reranker 是否值得 | 看收益率 |
| R-7 | w/o Retrieval Reviewer | Reviewer 是否值得 | 看收益率（当前 +9%） |
| R-1 | w/o Query Expander | QE 是否值得 | 进入 Phase 2.5 优化 |
| R-4 | w/o RRF Fusion | RRF 是否值得 | 进入 Phase 2.5 优化 |

### 5.2 输出格式

每个模块输出一张价值评估卡：

```
模块名称: BM25 Search
ΔScore:   +3.5
ΔLatency: +300ms
ΔToken:   +200
ΔCost:    0.15  (latency增比例 + token增比例)
收益率:    0.233 (ΔScore / ΔCost)
决策:      保留（收益率中等，精确匹配场景不可替代）
```

---

## 六、Phase 2.5：问题模块优化（半天）

### 6.1 RRF 优化验证

#### Step 1：场景价值分析

抽 20~30 个 case，按问题类型分类：

| 问题类型 | 数量 | RRF 有帮助 | RRF 有伤害 | 无影响 | 分析 |
|---------|------|-----------|-----------|--------|------|
| 专业术语/代码 | N | ? | ? | ? | 高价值场景 |
| 语义理解类 | N | ? | ? | ? | Vector 主导 |
| 混合查询 | N | ? | ? | ? | — |

**判断逻辑**：不机械使用"80% 无影响则删除"，而是看 RRF 在**高价值场景**（专业术语、配置、异常排查等）是否有不可替代的帮助。

#### Step 2：两种优化方案

| 方案 | 改动 | 说明 |
|------|------|------|
| **RRF-v1** | 调 BM25/Vector 各路 topK 配比、k 值参数 | 在当前框架内优化 RRF |
| **Fusion-v2** | 改为 BM25→Rerank + Vector→Rerank，最后结果合并 | 跳过 RRF，改变融合架构 |

> 注意：Fusion-v2 本质是架构调整而非 RRF 优化，但放在同一阶段验证，便于后续决策。

---

### 6.2 QE 优化验证

#### Step 1：失败 case 分析

抽 10 个 QE 导致下降的 case，分析原因。

#### Step 2：三种优化方案

| 方案 | 改动 | 说明 |
|------|------|------|
| **QE-v1** | temperature 0.8 → 0.3 | 减少随机性，降低离谱扩展 |
| **QE-v2** | 条件触发：检索结果不足时才扩展 | 默认不扩展，需要时启用 |
| **QE-v3** | 增加实体保持约束 Prompt | 强制保留产品名、版本号、类名、方法名、错误信息等关键实体 |

---

## 七、Phase 3：候选 Pipeline 对比（半天）

### 7.1 候选方案

| 方案 | Pipeline | 说明 |
|------|----------|------|
| **A** | Full（V+BM25+RRF+Rerank+Reviewer） | 当前完整方案（对照） |
| **B** | 优化后完整（V+BM25+RRF-v1+Rerank+Reviewer） | 优化 RRF 后的完整版 |
| **C** | 去 RRF（V+BM25+Rerank+Reviewer） | 核心候选 |
| **D** | 去 QE（V+QE-v2+BM25+Rerank+Reviewer） | 条件 QE 方案 |
| **F** | **最优核心（V+BM25+Rerank+Reviewer）** | **最可能最终方案** |
| **E** | 低成本（V+Rerank+Reviewer） | 极致精简 |

### 7.2 决策矩阵

| 方案 | Pipeline | 综合得分 | Δ效果 | Latency | Token | 收益率 | 结论 |
|------|----------|---------|-------|---------|-------|--------|------|
| A | Full | baseline | — | baseline | baseline | — | 对照 |
| B | 优化后完整 | ? | ? | ? | ? | ? | 候选 |
| C | 去 RRF | ? | ? | ? | ? | ? | 候选 |
| D | 去 QE | ? | ? | ? | ? | ? | 候选 |
| **F** | **V+BM25+Rerank+Reviewer** | **?** | **?** | **?** | **?** | **?** | **★ 最可能** |
| E | 低成本 | ? | ? | ? | ? | ? | 候选 |

### 7.3 最终决策原则

- **效果优先**：如果某方案效果与最高分差距 < 1%，选成本最低的
- **成本敏感**：如果某方案效果提升 < 3% 但耗时翻倍，放弃
- **场景适配**：如果某模块只在特定场景有效（如专业术语），通过条件启停保留而非全局删除

---

## 八、实施时间线

| 阶段 | 内容 | 预计耗时 |
|------|------|---------|
| Step 0 | 数据采集改造（AblationResult 增加成本字段 + 采集逻辑） | 半天 |
| Phase 1 | Chunk(3) + Overlap(4) + TopK(4) + 组合确认(3) = 14 组实验 | 半天 |
| Phase 2 | 7 组模块消融 + 收益/成本分析 | 半天 |
| Phase 2.5 | RRF 场景分析(20~30case) + 2 种优化方案 + QE case 分析 + 3 种优化方案 | 半天 |
| Phase 3 | 6 个候选 Pipeline 对比 + 决策矩阵输出 | 半天 |
| **总计** | | **约 2.5 天** |

---

## 九、不纳入本次实验的内容

| 原计划项 | 处理 | 理由 |
|---------|------|------|
| Source Attribution 消融 | 移出，改为功能测试 | 影响可解释性而非准确率 |
| Agent 消融 A-1~A-8 | 降为低优先级 | 当前焦点是 RAG Pipeline 决策 |
| 联合消融 C-1~C-5 | 取消 | 工程决策不需要交互效应分析 |
| Recall@K / nDCG | 不加入 | 最终决策只看综合得分 + 成本 |
| P95/P99 延迟 | 不加入 | 平均耗时足够做工程决策 |
| 统计显著性/多轮运行 | 不加入 | 工程决策不需要 mean±std |
| RRF k 参数全量调优 | 取消 | 改为 RRF-v1 快速验证 + Fusion-v2 架构调整 |

---

## 十、最终能回答的 8 个问题

1. **Chunk 应该多大？** → Phase 1 确定
2. **TopK 应该多少？** → Phase 1 确定
3. **Vector 是否必须？** → 是（-64.7%，已有定论）
4. **BM25 是否值得？** → Phase 2 看收益率
5. **Reranker 是否值得？** → Phase 2 看收益率
6. **Reviewer 是否值得？** → Phase 2 看收益率
7. **RRF/QE 是删除还是优化？** → Phase 2.5 先优化再决策
8. **最终上线方案是什么？** → Phase 3 决策矩阵输出
