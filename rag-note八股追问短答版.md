# RAG-Note 八股追问短答版

## 使用方式
- 这份笔记用于面试前 10 分钟速看。
- 每题控制在 3 句内，目标是先答稳，再等面试官追问。
- 回答只保留结论、原因、取舍，不展开源码细节。

---

## 一、项目一句话

### 1. 你这个项目是做什么的？
面向个人用户的智能笔记辅助系统，支持知识库文档管理、笔记生成和 AI 问答。核心是用 RAG 把用户私有知识和大模型结合起来。为了提升复杂问题处理能力，又加了 Supervisor-Worker 的多 Agent 编排。

### 2. 你的核心贡献是什么？
我主要做了四块：多 Agent 分诊与任务编排（含任务持久化、反思重规划、全链路追踪）、混合检索链路、RAG 评估与消融实验、文档入库一致性和上下文压缩。整体重点是把不稳定的 LLM 和外部依赖做成可控、可观测系统。

---

## 二、Agent 八股

### 3. 为什么要做 Supervisor？
不是所有问题都值得走多 Agent。Supervisor 先判断复杂度，简单问题直接单 Agent，复杂问题再拆成 2-4 个子任务。这样能控制延迟、成本和失败面。还有二次降级，只有 1 个子任务时也降级单 Agent。

### 4. Supervisor 怎么判断复杂问题？
当前是通过 LLM 规划输出子任务列表。返回空列表就视为简单问题，返回多个 `SubTask` 就进入多 Agent 流程。重点不在判断绝对准确，而在于提供一个可回退的分流机制。规划结果缓存 10 分钟，记录到 AgentTrace 便于分析规划准确率。

### 5. LLM 规划结果不稳定怎么处理？
我把它当不稳定上游处理。先强约束输出 JSON（temperature=0.1），再做 JSON 清洗和括号提取，最后解析失败就回退成单 Agent。核心原则是不能让规划失败把主流程打断。

### 6. 为什么要缓存 Supervisor 结果？
因为规划是高成本、弱副作用操作，而且重复问题很多。缓存能减少重复 LLM 调用、降低延迟，也让相同问题的规划结果更稳定。当前实现里缓存 TTL 是 10 分钟，基于 SHA-256 哈希。

### 7. 多 Agent 最大风险是什么？
最大风险不是”拆不拆”，而是”拆完后结果能不能稳”。具体包括误拆分、并发放大成本（3 个子任务可能产生 8-10 次 LLM 调用）、以及多路结果冲突。我的做法是优先保证可回退、可降级、可追踪。

### 8. Agent 执行失败了怎么办？
当前项目有完整任务持久化和容错机制。每个请求创建 AgentTask 记录状态（CREATED → PLANNING → RUNNING → COMPLETED/FAILED/PAUSED），AgentTrace 记录全链路（规划决策、工具调用、反思、质量评审），AgentTaskStep 记录每一步操作。支持任务暂停/恢复，失败可查询历史。

### 9. 反思与重规划是什么？
连续无进展时（consecutiveNoProgress >= 2），ReflectionService 分析卡住原因（比如”工具参数错误”），ReplanningService 生成新策略（比如”换用 searchNotes 模糊搜索”）。这是事中动态调整，不是事前静态规划。反思结果持久化到 AgentReflection 表，注入 AgentState.workingMemory。

### 10. 预算控制怎么做的？
AgentPolicyService + BudgetConfig 限制四个维度：maxIterations（10）、maxTimeSeconds（300）、maxTokens（50K）、maxToolCalls（30）。每轮开始前检查，任意一项超限立即返回 BUDGET_EXCEEDED。防止死循环、资源耗尽。

---

## 三、RAG / 检索八股

### 8. 为什么用混合检索，不只用向量检索？
因为术语类问题和口语化问题混在一起。BM25 更适合术语、报错、接口名，向量检索更适合同义表达和语义召回。混合检索是为了兼顾精确匹配和语义理解。消融实验显示纯向量 Recall@5 约 72%，加 BM25 后到 81%。

### 9. 你的混合检索链路怎么走？
先做 Query 扩展，再分别走向量检索和 BM25 检索，然后用 RRF 融合，最后用 rerank 精排。向量或 BM25 不可用时会降级到关键词检索。整体思路是先高召回，再高精排。

### 10. 为什么用 RRF，不直接加权分数？
因为向量分和 BM25 分不在同一分布上，直接加权不好调。RRF 只看排名，不依赖不同检索器分值可比性，工程上更稳。也更适合多 query、多路结果统一融合。

### 11. rerank 为什么放最后？
因为 rerank 成本高，适合放在候选集缩小之后。前面先做低成本召回和粗排，最后再对少量候选精排，整体性价比更好。这个是典型的多阶段检索设计。

### 12. Query 扩展的收益和风险是什么？
收益是提升召回，尤其适合口语问题和文档表述不一致的情况。风险是增加 Token 消耗，也可能带来噪音召回。我的处理是记录成本，并通过 RRF 和 rerank 把噪音压下去。扩展结果缓存 30 分钟。

### 13. 为什么要做关键词降级？
因为 Chroma 或 BM25 可能不可用。RAG 系统不能因为高级检索挂了就整体不可用，所以我做了多级降级：向量失败降 BM25、BM25 失败降关键词。优先级是先保可用，再保效果。纯 BM25 下降 24%，纯关键词下降 47%，但系统仍可用。

---

## 四、评估 / 消融八股

### 14. 为什么要做 RAG 评估？
因为 RAG 输出不是固定答案，不能只靠人工感觉调系统。我要知道问题是在检索还是生成，于是把质量拆成 Faithfulness（控制幻觉）、Answer Relevancy（控制答非所问）、Context Precision（控制噪音）、Context Recall（控制漏召回）四个指标。这样后续优化和回归都有依据。

### 15. 这四个指标分别看什么？
Faithfulness 看有没有幻觉（回答不能脱离检索上下文编造），Answer Relevancy 看有没有答非所问（回答要正面回应查询）。Context Precision 看检索噪音（不能塞太多无关文档），Context Recall 看漏召回（关键证据不能漏检）。四个指标覆盖”检索错了”和”生成歪了”两类问题。

### 16. 为什么要做超时降级？
因为评估本身也是多次 LLM 调用，不能让一个指标卡死整个批量回归。当前实现里四项指标并行执行（CompletableFuture），单项超时 120 秒就给默认值 0.5。评估模块首先要工程可用，然后才谈精度。

### 17. 为什么不直接用官方 RAGAS？
我参考了它的指标思路，但项目里是 Java 服务，直接自研更方便和现有 RagTrace、EvaluationReport、消融实验模块打通。更重要的是我需要接入自己的超时降级、规则评分（延迟扣分、空结果扣分）、批量实验逻辑。也就是说，我借鉴的是方法论，不是直接搬 SDK。

### 18. 消融实验的价值是什么？
是验证某个组件到底值不值得保留。不是只看”加了之后感觉更聪明”，而是比较加减组件前后的效果、延迟和 Token 成本。这样优化更像工程决策，而不是拍脑袋。AblationExperimentService 支持控制 Query Expansion/Vector/BM25/RRF/Rerank/TopK 等开关。

### 19. 消融实验你怎么看结果？
我主要看三点：综合分变化（deltaScore）、成本变化（deltaCost）、收益成本比（ROI）。项目里还专门算了 buildValueAssessment，给出”保留、优化、可移除”的建议。因为有的组件提分不多，但成本很高，不一定值得全量启用。

---

## 五、入库与一致性八股

### 20. 为什么文档入库要同步 + 异步拆开？
因为向量化和 Chroma 写入慢（平均 5-10 秒），而且强依赖外部服务。同步部分先保证 MySQL 和 BM25 可用（用户立即看到”上传成功”，可被关键词检索），异步部分再补齐向量索引（向量检索稍后生效）。这样主流程不会被外部依赖拖死，用户体验好。

### 21. 多格式解析怎么做的？
非 PDF 默认走 Tika 统一解析，PDF 单独走 PDFBox 按页解析。这样既能兼容多格式（Word/TXT/HTML），也方便保留页码信息用于后续定位。PDF 里还做了 NFKC 归一化，避免 CJK 兼容字符影响检索匹配。

### 22. 为什么切片要有 overlap？
因为固定长度硬切容易把语义切断，尤其是说明文、步骤文档、代码块边界。overlap 能让相邻 chunk 保留一部分共享上下文，减少关键信息被切断后两边都检不全的问题。对于代码、表格、列表还会尽量整块保留，避免从结构中间截断。

### 23. 怎么做去重？
用 MD5 做文档级去重（基于文件内容而非文件名）。重复上传时先查 MD5 记录和向量文档，都存在直接跳过；只有 MD5 记录但向量丢失时会删除旧记录重新处理。并发上传同一文档时还依赖 uk_user_md5 唯一键约束兜底（捕获 DuplicateKeyException）。

### 24. 这套入库一致性是强一致吗？
不是，是最终一致性。MySQL 先落库保证主记录存在，向量异步写入（DocumentTaskExecutor），失败后标记状态 vector_failed 并支持重试和补偿（定时任务扫描失败文档重新触发）。我的目标不是分布式强事务，而是可见、可恢复、可追踪。

### 25. 为什么 embedding 要分批和重试？
因为 embedding 服务本身有限流和波动风险。分批（默认每批 10 条）可以控制吞吐和失败面，批量失败后多次重试，整批一直失败还能回退单条，提高成功率。这个是稳定性和效率之间的平衡。

---

## 六、上下文与记忆八股

### 26. 上下文压缩做了什么？
先按 token 预算（32K）估算上下文长度，不是按消息条数。超预算时优先压缩早期长结果型消息（检索结果、长工具输出，信息密度不稳定），再对更早消息做摘要，只保留最近若干条原文。目标是在有限上下文内尽量保留当前最关键的信息。

### 27. 为什么先压缩工具结果？
因为长结果型消息通常又长又杂（占 token 多、信息密度不稳定），很多内容不需要原样带入后续轮次，只需要保留”找到了什么、关键结论是什么”。先压工具结果可以优先去掉低价值长文本，再保留对话语义骨架。这样比一上来整体摘要更稳，也避免过早损失用户意图。

### 28. 你说的长短期记忆，代码里是持久化的吗？
严格说不是完整长期记忆。当前更准确是上下文压缩（ContextManager 围绕 32K token 预算管理多轮历史） + 会话级内存（ConversationContextManager 用 ConcurrentHashMap 维护当前 session 的活动笔记上下文，支持”这篇””它”等指代消解）。面试里这点要讲严谨，不要说满。真正长期记忆需要跨会话持久化、记忆检索、重要性评分。

---

## 七、后端 / 测开 / Agent 通用追问

### 29. 你这个项目最体现后端能力的地方是什么？
不是把功能串起来，而是把不稳定组件做进可控主流程。包括缓存（Supervisor 规划 10 分钟、Query 扩展 30 分钟、ChromaDB Collection ID 24 小时）、回退（规划失败降单 Agent、向量失败降 BM25）、降级（BM25 失败降关键词）、异步拆层（同步 MySQL+BM25、异步 ChromaDB）、状态管理（processing → completed/vector_failed）和评估闭环。我的重点一直是稳定性治理。

### 30. 这个项目最体现测开能力的地方是什么？
是把 RAG 问答这种非确定性系统做成可回归、可比较、可定位问题的体系。四指标评估（Faithfulness/Answer Relevancy/Context Precision/Context Recall）、消融实验（对比移除组件前后的效果/成本/ROI）、异常场景覆盖（重复上传/并发上传/部分写入/ChromaDB 不可用/embedding 超时/补偿重跑）和一致性验证（状态机闭环、可恢复性验证），都是围绕这个目标做的。不是只写接口断言。

### 31. 这个项目最体现 Agent 工程能力的地方是什么？
是我没有把 Agent 当成 prompt 拼接，而是当成不稳定上游来治理。核心是结构化输出（JSON 清洗、括号提取）、失败回退（规划失败降单 Agent、工具失败记录质量评估）、缓存（规划结果、Query 扩展）、预算控制（限制轮次/时间/Token/工具调用次数）、任务持久化（AgentTask/AgentTrace/AgentTaskStep）、反思重规划（连续无进展时动态调整策略）和可观测（全链路追踪、暂停恢复）。也就是让系统稳，而不是只让模型”看起来更聪明”。

---

## 八、最后 5 句必背

### 32. 一句话讲项目价值
这个项目的价值不只是接入大模型，而是把 RAG 和 Agent 做成了一个可控、可回归、可恢复、可观测的后端系统。

### 33. 一句话讲你的技术亮点
我主要做的是多 Agent 分诊与任务编排（含任务持久化、反思重规划、全链路追踪）、混合检索、评估实验和一致性保障这四块。

### 34. 一句话讲检索设计
我的检索链路是 Query 扩展 + 向量检索 + BM25 + RRF + rerank，并且做了多级降级（向量降 BM25、BM25 降关键词）。

### 35. 一句话讲稳定性设计
我把 LLM 和外部依赖都当成不稳定上游处理，所以大量用了 JSON 清洗、失败回退、缓存、异步拆层、补偿机制、预算控制、任务持久化和全链路追踪。

### 36. 一句话讲你的成长
我最大的成长是从”接模型、调 prompt”转到”做大模型系统工程治理”，理解了怎么把强但不稳的能力工程化。
