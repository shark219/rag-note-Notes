# RAG-Note 简历深挖面试题（后端 / 测开 / Agent 开发）

## 使用说明
- 下面的问题和答案严格基于当前项目实现整理，优先覆盖简历里最容易被深挖的点。
- 回答风格按面试表达组织，不等于逐字背诵稿；建议先理解”设计取舍 + 风险点 + 现有不足”。
- 对于项目中没有代码证据支撑的内容，本文不会强行扩写成”已实现”。

---

## 一、项目整体与架构

### 1. 你这个项目解决的是什么问题？为什么要做成 RAG，而不是普通搜索？
**答：**
这个项目是一个面向个人用户的智能笔记辅助系统，核心场景是把笔记、知识库文档和 AI 问答结合起来。普通搜索更适合关键词命中，但用户实际提问里经常会混合术语、口语化表达和上下文指代，比如”帮我总结刚才那篇笔记的核心点”或者”之前那个 Java 并发问题怎么解决”。

所以我这边采用 RAG 的原因主要有三点：
1. 可以把用户私有知识和大模型能力结合起来，避免模型只靠参数记忆胡答。
2. 可以通过检索链路把回答约束在知识库和笔记内容上，降低幻觉。
3. 可以做评估和消融实验，比较不同检索组件对问答质量的影响，而不是只能靠主观感觉调参。

如果你想落到代码层面，核心链路在 `HybridRetriever`、`RagService`、`EvaluationService` 和 `AblationExperimentService` 这些模块里。

### 2. 你的项目整体技术架构是怎样的？
**答：**
后端是 Spring Boot 3 的单体服务，主要包含 Agent 编排、RAG 检索、文档入库、评估实验几个模块。存储层是 MySQL + ChromaDB + Redis + Lucene BM25 的组合。

我的理解是：
1. **MySQL** 负责结构化数据和文档元信息持久化。
2. **Lucene BM25** 负责稀疏检索，适合术语类和精确词匹配。
3. **ChromaDB** 负责向量检索，适合语义召回。
4. **Redis** 用在 Query 扩展和 Supervisor 规划结果缓存这类高频、可复用结果上，以及 JWT 黑名单、用户信息缓存、防缓存穿透/击穿/雪崩等场景。
5. **Agent 层** 包含九个子包 69 个 Java 文件，实现了 Supervisor 任务分诊、AgentLoop 目标驱动循环、反思与重规划、任务持久化、全链路追踪、预算控制、动态工具扩展等核心能力。
6. **评估层** 用四指标批量跑回归和消融实验。

前端是 Vue 3，通过 SSE 和后端做流式问答交互。

### 3. 这个项目里你最核心的技术贡献是什么？
**答：**
如果按价值排序，我会说主要有五块：
1. 做了 Supervisor-Worker 的多 Agent 分诊和任务编排，让复杂查询可以拆分执行；并构建了完整的任务持久化、暂停/恢复、反思与重规划机制，实现从”黑盒执行”到”全链路可观测”的演进。
2. 做了向量检索 + BM25 + RRF + rerank 的混合检索链路，并加了降级策略。
3. 做了文档入库链路的一致性保障，把”同步可用”和”异步补齐”拆开，降低对外部向量服务的强依赖。
4. 做了 RAG 质量评估和消融实验模块，让效果优化有数据支撑，而不是拍脑袋。
5. 实现了上下文压缩与分层记忆机制，围绕固定 Token 预算管理多轮对话上下文，通过滑动窗口、长结果压缩和历史摘要避免上下文膨胀。

### I.LangChain vs LangGraph的区别？

LangChain和LangGraph主要区别在于职责不同。LangChain更多是一个LLM应用开发框架，提供模型调用、Prompt管理、工具调用、RAG检索等基础组件，适合构建简单的Chain或者Agent。

LangGraph是在LangChain基础上针对复杂Agent设计的状态机框架，它通过图结构管理Agent执行流程，支持循环、条件分支、状态保存、人工介入和多Agent协作。
比如简单RAG问答，只需要LangChain完成检索增强和生成；但如果涉及Supervisor拆任务、Worker执行、失败重试、动态规划，就更适合使用LangGraph管理整体执行流程。

在我的项目中，LangChain4j主要负责模型调用、工具接入和RAG链路，而Supervisor-Worker、多Agent任务编排、Agent循环控制属于LangGraph这类工作流框架解决的问题。

### II.LangChain、LangGraph和Spring AI有什么区别？

> LangChain主要提供大模型应用开发的基础组件，例如Prompt管理、模型调用、RAG检索和工具封装，适合构建简单LLM应用。LangGraph是在LangChain基础上针对复杂Agent场景设计的状态图编排框架，通过节点和边管理Agent执行流程，支持循环、状态保存和多Agent协作。Spring AI则是Spring生态下的大模型接入框架，主要解决Java应用调用LLM、Embedding和向量数据库的问题，更偏基础设施层，不负责复杂Agent流程编排。

结合你的项目：
> RAG-Note项目中的Supervisor-Worker、多Agent协作、循环执行更适合使用LangGraph；而Java业务系统中如果只是接入AI能力，更适合使用Spring AI。

---

## 二、多 Agent 规划与协作流水线

### 4. 你说的 Supervisor-Worker 架构具体是怎么做的？
**答：**
我这里的实现重点不在”堆很多 Agent”，而在于先判断用户问题有没有必要进入多 Agent 流程。

具体做法是：
1. `SupervisorService.plan()` 先调用 LLM 对用户问题做规划。
2. 如果返回空数组，就认为是简单问题，直接走单 Agent 流程。
3. 如果返回的是多个子任务，就把任务拆成 2-4 个 `SubTask`，每个子任务带上描述、工具提示、依赖关系、成功标准等信息。
4. 后续由 `AgentRuntime` 协调执行模式（SINGLE/SEQUENTIAL/PARALLEL），每个子任务通过独立的 `AgentLoop.run()` 执行。
5. 顺序模式下前一个子任务的结果通过 `completedContext` 累积传递给后续任务；并行模式下通过 `CompletableFuture` 并发执行，最后由 `WriterService.synthesize()` 汇总结果。

整个流程有完整的任务持久化支持。每个请求创建 `AgentTask` 记录状态（CREATED → PLANNING → RUNNING → COMPLETED/FAILED/PAUSED），`AgentTrace` 记录完整执行链路（规划决策、工具调用、反思、质量评审），`AgentTaskStep` 记录每一步操作详情。支持任务暂停/恢复，通过 `AgentExecutionSnapshot` 保存执行快照，`AgentTaskResumeService` 实现断点恢复。

这个设计的核心不是炫技，而是降低复杂任务直接让单模型一步到位时的不稳定性，同时提供完整的可观测性和容错能力。

### 5. 为什么要先做复杂度判断，而不是所有请求都走多 Agent？
**答：**
因为多 Agent 并不是越多越好，它会明显增加调用链长度、Token 消耗和失败面。

如果所有请求都强行走拆分流程，会有三个问题：
1. 简单问题本来单轮就能回答，拆分反而增加延迟。
2. LLM 规划本身不稳定，简单问题没必要额外引入一个不确定节点。
3. 多子任务并发后还要做结果整合，系统复杂度会上升。

所以我这里的原则是：**简单问题直接单 Agent，复杂问题再拆分**。这也是为什么在 `SupervisorService` 里返回空列表就降级走单 Agent，并且 `AgentRuntime` 里还有二次降级——只有 1 个子任务且不是”生成产物+写回笔记”这种强需要顺序编排的场景，也会降级为单 Agent。

### 6. LLM 规划结果不稳定，你是怎么处理的？
**答：**
这是我在 Agent 流程里重点处理的问题。因为模型经常不老实，明明要求它返回纯 JSON，它还是可能带中文前缀、markdown 代码块，甚至混杂解释文本。

我做了三层处理：
1. **输出约束**：在 system prompt 里强约束只能返回纯 JSON 数组，使用 temperature=0.1 的精确模式减少格式漂移。
2. **结果清洗**：在 `SupervisorService.cleanLlmJson()` 里去掉 json 标记、前后说明文字，只截取 JSON 主体；如果清洗失败，再用 `extractJsonArray()` 按括号计数精确提取第一个数组。
3. **失败回退**：如果清洗和提取都失败，就直接回退为单 Agent，而不是让整个主流程报错；规划结果记录到 `AgentTrace.planningDecision`，便于分析”哪些查询触发了多 Agent”、”规划准确率如何”。

这个思路本质上是把 LLM 当”不稳定上游”，后端必须兜底，而不是盲信模型输出。

### 7. 为什么要对 Supervisor 结果做 Redis 缓存？
**答：**
因为很多问题在真实场景里会重复，比如用户连续追问同类问题，或者同一个问题被重新发起。Supervisor 规划本质上是”高成本、弱副作用”的步骤，适合缓存。

当前实现里：
- `SupervisorService.plan()` 先通过 `QueryCacheService.hashKey()` 生成 key（基于 SHA-256 哈希）。
- 如果命中缓存，直接返回 `List<SubTask>`。
- 没命中才去调用 LLM，并把结果缓存 10 分钟。

这样做主要有三个收益：
1. 减少重复 LLM 调用，降低成本。
2. 降低规划阶段的平均延迟。
3. 在模型输出偶发波动的情况下，提高相同问题的结果稳定性。

Redis 中还缓存了 Query 扩展结果（30 分钟 TTL）、ChromaDB Collection ID（24 小时）、用户信息（30 分钟）、今日复习数据（10 分钟）等，并通过 `CacheProtectionService` 提供防穿透（空值标记 60 秒）、防击穿（SETNX 锁 10 秒）、防雪崩（随机 TTL 抖动）三层防护。

| 缓存 Key                                        | 存储内容                 | TTL         | 目的                                                                                                 |
| --------------------------------------------- | -------------------- | ----------- | -------------------------------------------------------------------------------------------------- |
| `blacklist:{jti}`                             | `"1"`                | Token 剩余有效期 | JWT 黑名单。用户登出后将 Token 的 `jti` 加入黑名单，后续请求校验时拒绝已登出的 Token                                             |
| `user:{userId}`                               | `UserResponse JSON`  | 30 分钟       | 用户信息缓存。避免每次请求都查询 DB 加载用户资料。`AuthService` 读取，更新/删除用户时执行 evict                                       |
| `note:{noteId}`                               | `Note JSON`          | 30 分钟       | 单条笔记缓存。减少数据库查询。笔记更新/删除时执行 evict                                                                    |
| `note:list:{userId}`                          | （未写入）                | —           | 笔记列表缓存 Key。代码中仅在更新/删除笔记时执行 evict，但 `listNotes()` 未实现缓存写入，属于预留功能                                    |
| `review:today:{userId}`                       | `Map JSON`           | 10 分钟       | 当日复习数据缓存。保存用户当天待复习内容列表，避免每次打开复习页重新计算                                                               |
| `chroma:collection:id:{collectionName}`       | Collection ID 字符串    | 24 小时       | Chroma 向量库集合 ID 缓存。避免每次操作向量库时调用 Chroma API 查询 Collection ID                                        |
| `query-cache:supervisor:{sha256(query)}`      | `List<SubTask>` JSON | 10 分钟       | Agent 任务规划缓存。相同查询复用 LLM 生成的子任务拆分结果，减少 Token 消耗和响应延迟                                                |
| `query-cache:query-expansion:{sha256(query)}` | `List<String>` JSON  | 30 分钟       | 查询扩展缓存。复用 LLM 生成的扩展查询词列表，避免重复调用 LLM                                                                |
| `cache:lock:{key}`                            | `"1"`（SETNX 锁标记）     | 10 秒        | 防缓存击穿锁。缓存未命中时，只有获取锁的线程查询 DB 并回写缓存，其他线程等待后读取缓存                                                      |
| `__NULL__`                                    | `__NULL__` 空值标记      | 60 秒        | 防缓存穿透。数据库不存在的数据也缓存空标记，避免大量不存在的 Key 重复查询 DB                                                         |
| `__health_check__`                            | `"1"`                | 用完即删        | Redis 健康检查探针。`HealthController` 执行写入、读取、删除流程验证 Redis 连通性                                           |
| **缓存保护机制**                                    | **组合策略**             | **—**       | **`CacheProtectionService` 提供三层防护：① `cache:lock:{key}` 防缓存击穿；② `__NULL__` 防缓存穿透；③ 随机 TTL 抖动防缓存雪崩** |


### 8. 多子任务并发后，结果整合最难的点是什么？
**答：**
最难的是结果之间可能重复、冲突，甚至粒度不一致。

多子任务并发后，结果整合的核心问题有：
1. **子任务边界很容易重叠**，结果会互相补充也会互相打架。
	Supervisor 拆任务时会给每个子任务明确 goal、successCriteria、dependsOn 和 executionMode，顺序模式下前一个任务的结果会通过 `completedContext` 累积传递（包含回答文本、关键事实、产物内容，最多 8000 字符），作为后一个任务的上下文，减少重复搜索；并行模式下则尽量把任务切成互补维度，最后统一合成。
	
2. 不同agent**输出粒度不一致**，有的是事实、有的是原始证据，直接拼会乱。
	不直接拼原文，而是把每个子任务的 AgentState、工具调用历史、Observation、结果摘要一起保留；在顺序链路里先用 ResponseComposer 做单任务收敛，再交给 WriterService.synthesize 做统一汇总，保证输出层级一致。每个子任务生成独立的 `AgentTrace`，记录完整的工具调用、证据级别、目标达成情况。
	
3. 并发结果的**证据可信度不同**，不能简单按照完成顺序或者多数票合并。
	合成时优先看直接命中资料的证据、工具返回和多个子任务一致的结果，弱化纯模型推断；最终还会走 QualityReviewer 审查，不通过就根据反馈重新合成（`synthesizeResults` 里会把 `review.feedback()` 拼到 query 里传给 WriterService，让第二次合成明确知道”哪里不对、要往哪个方向改”），避免把冲突结果硬拼成一个”看起来合理”的假答案。
	
4. 并发结果可能存在**冲突或缺失**，有的任务拿到了关键证据，有的任务只拿到部分信息。
	合成器（WriterService.synthesize）不会强行抹平冲突，而是保留冲突点、补充点和不确定性。如果多个子任务返回的信息互相矛盾（比如子任务A说"支持多线程"，子任务B说"不支持多线程"），WriterService 会在合成时标注冲突："根据子任务A的检索结果显示支持多线程，但子任务B的结果显示不支持，建议进一步核实"。
	如果某个子任务因为信息不足而失败（比如"未找到相关文档"），合成时会明确说明："关于X的部分未找到资料，仅基于Y和Z的结果回答"。这种"诚实地承认信息缺失"比"编造一个看似完整的答案"更可信。
	注意：反思与重规划只在单个子任务的 AgentLoop 内部发生，不在多子任务合成阶段发生。如果某个子任务在自己的 AgentLoop 内连续无进展，会触发反思重规划；但合成阶段只是对已完成的多个子任务结果做整合，不会再回到单个子任务的执行循环。
	
5. 并发执行中**某个子任务失败**，可能拖累整体答案完整性。
	子任务失败不会直接让整体流程崩掉，而是记录失败状态到 `AgentTask` 和失败原因到 `AgentTrace`，其他子任务继续执行；最后合成时会把失败子任务标记出来，只基于已成功子任务输出主答案，缺失部分则由合成器补充说明或提示未覆盖。

当前项目里对”结果质量裁判”的支持主要体现在 `QualityReviewer` 的质量审查（基于 `prompt/review_answer.txt`）和 `ToolResultEvaluator` 的工具结果质量评估（GOOD/POOR/ERROR + 恢复建议），更强的 hallucination reviewer 或 tool judge 独立模块是后续可增强的方向。

### 9. 这个多 Agent 流程有什么潜在问题？
**答：**
1. **规划误拆分**：Supervisor 可能把本来简单的问题拆复杂了，或者拆错边界。当前通过二次降级缓解——只有 1 个子任务时降级为单 Agent。
2. **并发放大成本**：子任务并行会提升吞吐，但也会放大 Token 消耗和下游依赖压力。通过 `AgentPolicyService` + `BudgetConfig` 限制单任务的 maxTokens（默认 50K）、maxToolCalls（默认 30）来防止资源耗尽。
3. **结果整合误差**：如果多个子任务各自正确，但汇总逻辑不够稳，最终答案仍然可能失真。通过 `WriterService.synthesize()` 统一合成 + `QualityReviewer` 带反馈重试机制缓解。
4. **长链条上下文膨胀**：顺序流水线中 `completedContext` 会越滚越大，子任务越靠后拿到的上下文就越长，可能顶到 `ContextManager` 的 32K token 预算。当前 Supervisor 一般只拆 2-4 个子任务，这个问题还不明显。

所以我的处理原则是：默认先保证主流程稳，再逐步提升智能程度。像当前项目里，最重要的工程保障其实是 JSON 清洗、失败回退、缓存、降级和全链路追踪，而不是盲目追求 Agent 层次更多。

### 10. 如果让你继续优化这套 Agent 系统，你会怎么做？
**答：**
1. 增加对子任务执行结果的结构化校验，比如每个任务必须返回目标完成情况、证据和置信度。当前 `AgentState` 已经有 `evidenceLevel`、`knownFacts`、`observations`，可以进一步规范化。
2. 把结果整合阶段从”文本级拼接”升级为”证据级合并 + 再生成”。当前 `WriterService` 已经接收 `List<SubResult>`（包含 label、content、state），可以进一步提取每个子任务的证据来源、工具调用结果做对齐。
3. 加上规划质量评估数据，比如拆分命中率、回退率、缓存命中率和各类失败标签，便于分析 Agent 真正带来的收益。当前 `AgentTrace` 已经记录规划决策，可以基于历史 trace 数据做统计分析。
4. 完善 `AgentMetrics` 指标体系，当前已有工具调用频率、成功率、平均耗时统计，可以扩展到规划准确率、子任务完成率、反思触发率等维度。

也就是说，下一步不是单纯再加一个 Agent，而是把现在已有的流水线做得更可观测、更可评估。

---

## 三、混合检索链路

### 11. 为什么要用混合检索，而不是只用向量检索？
**答：**
因为这类笔记和知识库场景里，用户提问会同时出现两类情况：
1. **术语、接口名、异常名**，这类更适合 BM25 这类稀疏检索。*比如当用户做的笔记主题是跟代码出错的各种异常情况相关的，用户往往会拿原始的错误串来搜索，而这个报错信息通常关键词很固定，包含类名、函数名、异常名、状态码、报错等*
2. **口语化表达、同义转述**，这类更适合向量检索。

只用向量检索时，术语类查询经常会丢掉精确匹配；只用 BM25 时，语义改写和自然表达召回效果又会比较差。所以我在 `HybridRetriever` 里把两路都做了，再通过 RRF 融合和 rerank 精排得到最终结果。

### 12. 你这条混合检索链路的完整流程是什么？
**答：**
按 `HybridRetriever` 的实现，完整流程是：
1. 先判断是否开启 Query 扩展。
2. 对原始 query 或扩展后的多个 query，分别执行向量检索和 BM25 检索。
3. 把所有检索结果汇总成多路 ranking。
4. 如果启用 RRF，就做 `rrfFusion()`；否则走简单合并。
5. 融合结果再进入 `rerankerService.rerank()` 精排。
6. 如果向量或 BM25 某一路不可用，就按情况降级到关键词检索。

这条链路本质上是“先尽量高召回，再逐步提纯”。

### 13. Query 扩展的价值是什么？它也会带来什么问题？
**答：**
Query 扩展主要是**提升召回**，尤其适合用户表达和知识库表述不一致的情况。比如用户问的是口语化问题，但文档里用的是专业术语。

但它也有两个典型副作用：
1. 会增加 Token 消耗和检索请求数。
2. 如果扩展得太发散，可能会把无关文档也拉进来，降低精度。

所以我这里做了两件事控制风险：
- 记录 Query Expansion 的 Token 消耗。
- 在后面通过 RRF 和 rerank 进一步过滤噪音。

另外，Query 扩展结果本身也适合做缓存，避免重复消耗。

### 14. 你为什么选择 RRF，而不是直接把向量分数和 BM25 分数加权？
**答：**
因为向量检索分数和 BM25 分数本身不在同一个分布上，直接做线性加权很容易受分值尺度影响，调参也麻烦。

RRF 的好处是它只看排名，不强依赖不同检索器分数的绝对值可比性。对于工程实现来说更稳，也更适合把多 query、多路检索结果统一融合。

在 `HybridRetriever.rrfFusion()` 里，我是按 `1 / (k + rank + 1)` 算每一路的贡献分，然后汇总排序。默认常量 `rrfK` 目前实现里是 30，并且也支持在消融实验里调参。

### 15. rerank 放在最后一层的原因是什么？
**答：**
因为 rerank 成本高、但精度提升通常最直接，所以更适合放在候选集已经收敛之后。

如果一开始就对大规模候选做 rerank，代价会比较高；但如果先通过向量、BM25、RRF 做召回和粗排，再把前面的候选交给 rerank，就能在可控成本下提升最终结果质量。

这个设计其实就是典型的信息检索分层思路：**低成本召回 + 高成本精排**。

### 16. 为什么还要做关键词降级？
**答：**
因为向量库和 BM25 都可能不可用。比如 ChromaDB 连不上，或者 BM25 检索异常。如果这时候整个检索链路直接失败，用户体验会很差。

所以在 `HybridRetriever` 里有两级降级：
1. **单路降级**：向量挂了就只走 BM25，BM25 出异常就回退到 MySQL 关键词检索。
2. **全链路降级**：如果所有 ranking 都拿不到，就直接走纯关键词检索。

这体现的是一个工程原则：RAG 系统不能把“高级能力不可用”直接等同于“整个问答不可用”。

### 17. 你怎么判断检索链路优化是否真的有效？
**答：**
我不会只看主观体验，而是结合评估模块和消融实验来判断。具体看四类信号：
1. 四项评估指标，比如 Precision、Recall、Faithfulness、Answer Relevancy。
2. 消融实验中移除某个组件后的得分变化，也就是 `deltaScore`。
3. 延迟和 Token 成本，比如 Query Expansion、rerank、generation 的平均 Token 消耗。
4. 用户体验层面的降级率、空结果率、失败率。

也就是说，检索优化不是单看“命中几个文档”，而是看**效果、成本、稳定性**三者的平衡。

---

## 四、RAGAS 评估与消融实验

### 18. 你这个评估模块为什么要自己做，而不是直接接 RAGAS？
**答：**
我这里参考的是 RAGAS 的评估思路，但项目里最终实现的是自研模块，而不是直接接官方 SDK。

这么做主要有几个原因：
1. 项目本身是 Java 后端，直接在现有服务内实现更顺手。
2. 我需要把评估过程和项目自己的 `RagTrace`、`EvaluationReport`、消融实验结果打通。
3. 我希望可控地加入超时降级、默认分数、规则评分这些工程化逻辑，而不是完全依赖外部库黑盒。

所以可以说是“借鉴 RAGAS 指标体系，结合项目场景做本地化实现”。

### 19. 四个指标分别在评估什么？
**答：**
四个指标我会这样解释：
1. **Faithfulness忠实度**：回答有没有脱离检索上下文，是否有幻觉。
2. **Answer Relevancy答案相关性**：回答是不是在正面回应用户问题，有没有答非所问。
3. **Context Precision上下文精确度**：检索出来的上下文里有多少是真的相关，重点看噪音多少。
4. **Context Recall上下文召回率**：该检索到的关键信息有没有检到，重点看漏召回。

这四个指标组合起来，基本能把“检索错了”还是“生成歪了”拆开分析。

### 20. Faithfulness 是怎么评估的？
**答：**
在 `EvaluationService` 里，Faithfulness 不是直接一句话打分，而是先让模型把回答拆成原子声明，再判断每条声明是否被上下文支持。

具体逻辑是：
1. 构造 prompt，要求输出 `CLAIMS` 和 `FAITHFULNESS_SCORE`。
2. 优先解析声明块，统计 `SUPPORTED / NOT_SUPPORTED` 比例。
3. 如果解析失败，再降级做宽松匹配。
4. 如果还不行，再退回整体 0-10 分的 LLM 打分。

这样做比单纯让模型“给个分数”更稳，因为中间保留了一个可解释的声明判断过程。

### 21. 为什么评估里要做超时降级？
**答：**
因为评估本身也是多次 LLM 调用，如果某个指标卡住，不能把整个批量回归任务都拖死。

当前实现里：
- 四项指标通过 `CompletableFuture` 并行执行。
- `joinSafe()` 会用 120 秒超时控制每个指标。
- 超时或异常时，直接返回默认值，比如 0.5 或 -1。

这个设计体现的是：评估模块服务于工程效率，不能为了“绝对精确”把回归测试变成一个不稳定的大型阻塞任务。

### 22. 你们为什么还加了规则评分，不只看 LLM 指标？
**答：**
因为纯 LLM 评估不够全面，而且有时会“觉得回答还行”，但从系统角度已经不可接受了。

比如：
- 延迟过高
- 没检到文档
- 回答太短
- 平均相似度过低

这些都是工程质量问题，不适合完全交给 LLM judge。所以 `ruleEvaluate()` 会对这些指标做扣分，再和 LLM 评估做调和平均，形成最终总分。

这个思路本质上是把“语义质量”和“系统质量”合在一起看。

### 23. 你是怎么做消融实验的？
**答：**
我这里做的是配置驱动的消融实验。核心是通过 `AblationConfig` 控制各组件开关，比如：
- Query Expansion
- Vector Search
- BM25
- RRF
- Rerank
- TopK
- RRF_K

然后 `AblationExperimentService.runExperiment()` 会对同一批测试用例逐个跑 RAG 流程、记录回答、再调用评估模块，最终聚合成实验结果。

这样可以比较移除某个组件后综合得分、延迟、Token 成本怎么变化，从而判断这个组件值不值得保留。

### 24. 消融实验结果你怎么看，不是只看谁分数高就完了吧？
**答：**
对，不能只看分数。我的判断标准一般是三维：
1. **效果收益**：综合分是否提升，提升多少。
2. **成本变化**：延迟和 Token 是否明显上升。
3. **性价比**：收益和成本是否匹配。

项目里我专门做了 `buildValueAssessment()`，本质上就是把 `deltaScore` 和 `deltaCost` 结合起来算 ROI，再给出“保留、优化、可移除”这类决策建议。

这能避免一个常见误区：某组件稍微提了一点分，但成本非常高，实际上可能不值得全量启用。

### 25. 这个评估体系有什么局限？
**答：**
我会主动承认三点：
1. 它仍然依赖 LLM judge，本身不是绝对客观。
2. 如果 Ground Truth 质量不高，Context Recall 的可信度会受影响。
3. 评估结果更适合做相对比较，不适合被过度解读成“真实业务正确率”。

所以我会把这套模块定位成**回归和调优辅助工具**，而不是绝对真理。

---

## 五、文档入库与一致性保障

### 26. 文档入库为什么要拆成 MySQL/BM25 同步写入 + Chroma 异步写入？
**答：**
因为向量化和向量库写入是整条链路里最慢、最不稳定的一段。如果把它和主流程绑死，用户上传文档就会明显受外部依赖影响。

所以我做了分层：
1. **同步阶段**：先把文档元信息、切片结果、BM25 所需数据写入 MySQL，保证文档至少可以被系统管理和部分检索。
2. **异步阶段**：再通过 `DocumentTaskExecutor.writeToChromaAsync()` 做 embedding 和 ChromaDB 批量写入。

这样即使向量库暂时不可用，主流程也不会整体阻塞，后续还能通过失败状态和补偿机制恢复。


### Q：RAG 切片是怎么做的？

我们没有采用简单的固定长度切片，而是做了**结构感知切片**，尽量保证一个 chunk 内语义完整。

整体流程分为三步：

**第一步，文档预处理。**

- 使用 **Tika** 统一解析 PDF、Word、TXT 等格式；
- PDF 会按页解析，并保留页码信息，方便后续检索结果定位来源；
- 对 PDF 文本做 Unicode NFKC 归一化，避免特殊字符导致检索匹配失败；
- 入库前通过 MD5 做文档去重，避免重复构建索引。

**第二步，根据内容类型选择不同切片策略。**

对于代码、表格、列表这类结构化内容：
- 尽量保持完整，不直接从中间截断；
- 如果超过长度限制，再按照行切分，避免破坏代码结构或者表格格式。

对于普通文本：
- 采用递归分隔方式切片；
- 优先按照段落、换行、句号等自然边界切分；
- 如果仍然过长，再继续细粒度拆分。

同时增加 **overlap 重叠窗口**：
- 普通文本按字符保留一部分上下文；
- 表格、代码按行 overlap，避免出现半行内容。

**第三步，保留结构信息。**

生成 chunk 时除了保存正文，还会额外保存：
- 文档标题；
- 章节路径；
- 内容类型；
- PDF 页码。

检索时不仅搜索正文，也会利用这些元数据辅助召回，提高相关性。

最终形成：
```
原始文档
   ↓
Tika解析
   ↓
文本清洗 + 元数据保留
   ↓
结构识别(text/code/table/list)
   ↓
针对性切片 + overlap
   ↓
生成chunk
   ↓
Embedding + BM25建立索引
```
这样相比固定长度切片，优势是：
1. **减少语义割裂**，提高向量检索准确性；
2. **保留代码、表格等结构信息**，避免上下文丢失；
3. **保留页码和章节信息**，方便回答时进行来源定位。

如果继续优化，还可以结合文档类型动态调整 chunk size，比如技术文档偏小粒度，论文类文档偏大粒度。

### 27. 你们是怎么做多格式文档解析的？
**答：**
主路线是 `DocumentProcessor.extractText()`：
- 非 PDF 文档默认用 **Apache Tika** 统一解析。
- PDF 单独走 **PDFBox**，按页抽取文本。

PDF 单独处理的原因是：PDF 场景下我更需要页码和逐页文本控制，便于后续切片保留页信息，而且项目里还做了 NFKC 归一化，解决 CJK 兼容字符影响检索匹配的问题。

所以这里不是简单“能读出来就行”，还考虑了后续检索和定位的可用性。

### 28. 切片为什么要做 overlap？
**答：**
因为如果严格按固定长度截断，语义很容易在边界处断掉，尤其是说明文、步骤文档、代码块或表格前后依赖较强的时候。

overlap 的作用是让相邻 chunk 之间保留一部分重叠上下文，减少关键信息被切断后两边都检不全的问题。

项目里 `RagChunker.splitKnowledge()` 和相关分块逻辑都支持 overlap，而且对 code/table/list 这类结构化内容还做了“尽量整块保留”或按行切分，避免从单元格中间截断。

### 29. 你怎么处理重复上传文档？
**答：**
这里是用 MD5 做去重。

在 `DocumentProcessor.processFile()` 里：
1. 先计算文件 MD5。
2. 如果 `md5Store` 和向量文档都存在，就直接跳过。
3. 如果只有 MD5 记录存在，但向量数据缺失，说明之前可能是部分失败，就删掉旧记录重新处理。

另外还考虑了并发上传同一文档的情况，对唯一键冲突 `uk_user_md5` 做了专门处理，避免重复入库。

### 30. ChromaDB 写入失败时你们怎么保证一致性？
**答：**
这里我不会说“强一致”，而是明确说做的是**最终一致性**。

具体机制是：
1. MySQL 先落库，保证主记录存在。
2. Chroma 异步写入失败时，把文档状态标成 `vector_failed`。
3. embedding 和 Chroma 批量写入都带重试。
4. 后续可以基于失败状态做补偿或重试。

也就是说，系统允许短时间内出现“文档已存在但向量索引未完成”的状态，但保证状态可见、可追踪、可恢复。

### 31. embedding 为什么还要分批，并且支持批失败后回退单条？
**答：**
因为 embedding 调用通常既有吞吐限制，也容易受上游模型服务波动影响。

如果一口气把大量 chunk 全送上去，失败面会很大；所以我这里在 `DocumentTaskExecutor` 里做了：
1. 批量 embedding，默认每批 10 条。
2. 批量失败时重试多次。
3. 如果整批一直失败，再回退成单条请求，尽量提高成功率。

这个策略本质上是用吞吐和成功率做平衡。

### 32. 这个入库链路你们测试时重点覆盖了哪些场景？
**答：**
如果站在测开角度，我会重点覆盖：
1. 多格式解析兼容性，比如 PDF、Word、TXT、HTML。
2. 重复上传和并发重复上传。
3. 空文档、超长文档、乱码文档。
4. Chroma 不可用、embedding 超时、部分批次失败。
5. MySQL 已写入但向量未写入的部分成功场景。
6. 重试后成功、重试后仍失败、补偿重跑。

这些场景的重点不是测“功能能不能走通”，而是测**异常条件下链路是否仍可恢复**。

### 33. 如果面试官问你“这套方案算不算事务一致性”，你怎么答？
**答：**
我会明确说：**不是分布式强事务，也不追求同步强一致**。

这是一个典型的“本地事务 + 异步补偿 + 状态机”的最终一致性方案。因为 ChromaDB 和 embedding 服务都不适合纳入传统数据库事务里，而且外部依赖的成功率和耗时都不可控。

所以我的取舍是：
- 先保证主流程短事务完成。
- 再通过状态流转、失败标记和重试补偿保证最终一致。

这在工程上比强行做分布式事务更现实。

---

## 六、上下文压缩与分层记忆

### 34. 你说的上下文压缩具体做了什么？
**答：**
项目里的 `ContextManager` 主要做了三层控制：
1. 先估算历史消息 token 数，而不是只看消息条数。
2. 如果超出token预算，优先压缩早期历史中较长、信息密度不稳定的结果型消息，比如检索结果、长工具输出或大段返回内容。
3. 如果压缩后还超预算，再把更早的消息整体摘要化，只保留最近若干条原文。

这套策略的目标是：在固定 token 预算下，尽量保留最近对当前问答最关键的信息。

### 35. 为什么优先压缩长结果型消息，而不是一上来就做摘要？
**答：**
因为长结果型消息通常是历史里*最占token、但信息密度不稳定*的部分。
比如检索原文、长工具输出、长摘录内容，很多时候后续并不需要保留完整原文，只需要保留：
  - 找到了什么
  - 关键结论是什么
  - 哪些证据最重要
  如果一上来就对整段历史做摘要，有两个风险：
  1. 容易把用户真正关心的历史意图也一起压缩掉。
  2. LLM 摘要会引入抽象和改写，可能过早损失语义细节。

  所以更稳做法是：先处理历史里的长噪音，再决定是否摘要历史语义本身。

  这里要特别说清：
  `ContextManager` 处理的是跨轮会话历史消息，不是当前这轮 AgentLoop 里临时的`ToolExecutionResultMessage`。当前轮工具结果主要活在本轮 loop里，跨轮保留下来的通常是它们的结论、摘要或已经进入会话消息的长内容。

### 36. 你们的“长短期记忆”是怎么理解的？

目前项目还未实现持久化的长期记忆系统，已经有的memory分为两种：**短期记忆和会话上下文**
	短期记忆和会话上下文的生命周期都是当前会话范围，但是承担的职责不同。
	*短期记忆*主要保存原始消息历史，用于支持多轮对话理解；
	*会话上下文*是在消息和工具执行过程中提取出的结构化状态，例如当前操作对象、任务进度、工具返回结果等，用于支持 Agent 持续执行任务。

当前项目中的 `ConversationContextManager` 内存态更接近会话状态管理，而不是传统意义上的长期记忆。如果需要跨会话保留用户偏好或历史信息，则需要进一步引入持久化存储。


### 37. `ConversationContextManager` 解决了什么问题？
**答：**
它主要解决的是多轮对话中的指代消解和上下文延续。

比如用户先打开一篇笔记，下一句说“帮我总结这篇”“把它改成面试题格式”，这时候如果没有会话上下文，系统并不知道“它”指的是哪篇笔记。
所以这里 `ConversationContextManager`会*按 session 维护一个当前活动笔记上下文*，在检测到“这篇、它、刚才那篇”这类引用词时，把上下文 hint 注入到 query 里，帮助后续检索和生成更准确。
本质上，它不是做知识记忆，而是做 会话里的引用解析和对象跟踪。

### 38. 上下文压缩可能带来什么副作用？

上下文压缩本质是“成本、时延、保真度”之间的平衡，不是无损过程。副作用主要有三类：
  1. 长结果型消息压缩后可能丢细节后续如果用户追问非常具体的证据，压缩后的内容可能不够用。
  2. 摘要本身有抽象和失真风险因为摘要是 LLM 生成的，不是原文复制，可能会遗漏边界条件或把弱结论说得太强。
  3. 压缩边界如果选得不好，可能提前压掉还重要的历史信息比如某条中间消息当下看不重要，但下一轮突然又变成关键上下文。

  所以这类方案没有绝对最优，一般都是“成本和保真度”的平衡，核心是：
  - 优先保留最近原文
  - 优先保留当前任务强相关信息
  - 对长结果型历史先做降噪
  - 摘要只作为第二层兜底，不轻易提前动用户语义主线
---

## 七、后端开发深挖

### 39. 你在这个项目里最看重的后端工程能力体现在哪里？
**答：**
我觉得主要体现在三点：
1. **把不稳定的 LLM 和外部依赖纳入可控主流程**，比如 JSON 清洗、失败回退、缓存、降级。
2. **把链路拆层**，比如文档入库里的同步写入和异步向量化分离。
3. **做可观测和可评估设计**，比如 Token 统计、消融实验、失败状态和报告聚合。

我理解后端开发不是把功能串起来，而是把系统在异常和复杂条件下也做稳。

### 40. 你在这项目里做过哪些性能或吞吐优化？
**答：**
当前代码里比较明确的优化点有：
1. Supervisor 规划结果缓存，减少重复 LLM 调用。
2. Query 扩展和检索链路的多路融合，减少低质量候选进入生成阶段。
3. 文档 embedding 分批处理和异步写入，减少长事务占用。
4. 评估四指标并行执行，降低回归任务总耗时。

如果面试官要压数据，我会实话实说：当前代码能证明机制存在，但像“吞吐提升 3 倍”这类具体数字，需要有独立压测记录才能严谨地讲。

### 41. 这个系统里你做了哪些容错设计？
**答：**
我会从链路上拆开说：
1. **Agent 层**：JSON 清洗、括号提取、规划失败回退单 Agent。
2. **检索层**：向量不可用时关闭向量检索，BM25 异常时回退关键词检索，全链路空时走纯关键词。
3. **入库层**：异步向量写入失败标记状态、支持重试和补偿。
4. **评估层**：异步评估超时默认值降级，避免整个批任务卡死。

这其实是典型的“分层容错”，每一层都要能自己兜一部分，而不是把异常全抛给最外层。

---

## 八、测试开发深挖

### 42. 如果你是以测开身份介绍这个项目，你的重点会怎么讲？
**答：**
我会强调自己做的不是“补单测”，而是围绕 RAG 系统的质量保障体系建设，主要包括：
1. 构建四指标评估模块，支持批量回归。
2. 设计消融实验，验证不同检索组件的真实价值。
3. 针对外部依赖不稳定设计异常降级和恢复测试。
4. 对文档入库做多格式、重复、部分失败和最终一致性验证。

也就是说，测开的价值在这里不是只测 API 正常返回，而是把“效果质量”和“系统稳定性”一起纳入验证。

### 43. RAG 系统最难测的是什么？
**答：**
我觉得最难测的是“输出不确定，但又不能没有质量判断标准”。

传统接口测试更容易做输入输出断言，但 RAG 回答不是固定字符串，所以要换思路：
1. 先对系统做结构化 trace，拿到 query、documents、answer、latency、token 等信息。
2. 再把质量问题拆成多个维度，比如忠实度、相关性、精度、召回。
3. 最后用批量评估和消融实验做相对比较。

换句话说，RAG 测试难点不在工具不够，而在于需要先定义“什么叫好”。

### 44. 你如何设计 RAG 回归测试集？
**答：**
我会按问题类型分层设计，而不是随机攒几条问答：
1. 术语精确匹配类。
2. 口语改写类。
3. 多跳或复合问题类。
4. 带上下文指代类。
5. 易产生幻觉的边界类。
6. 检索为空或文档缺失类。

每类都要配 Ground Truth 或至少配预期证据范围，这样后续四指标和消融实验才有比较基础。

### 45. 你怎么验证“最终一致性”真的成立？
**答：**
这个问题不能只靠接口 200 来判断，我会从状态流和数据面去验证：
1. MySQL 记录是否已存在。
2. 文档状态是否正确标成 `processing / completed / vector_failed`。
3. Chroma 中是否存在对应 chunk。
4. 失败后重试或补偿是否能把状态推进到完成。
5. 检索链路是否能在不同状态下表现出预期行为。

也就是说，要验证的是**状态机是否闭环**，而不是某一步单独成功。

### 46. 如果评估结果波动很大，你怎么排查？
**答：**
我会按从外到内的顺序查：
1. 测试集本身有没有变，Ground Truth 是否稳定。
2. 检索返回文档有没有明显变化。
3. Query 扩展、RRF、rerank 等开关或参数是否改过。
4. 评估使用的模型、Prompt、超时默认值是否变化。
5. 是否是 LLM judge 本身的随机性导致。

RAG 评估波动不一定说明主链路退化，也可能是评估器自己在漂，所以 trace 和配置快照非常关键。

---

## 九、Agent 开发深挖

### 47. 你觉得做 Agent 和做传统后端最大的差别是什么？
**答：**
我觉得最大差别是：传统后端处理的是“确定性逻辑”，而 Agent 更多是在处理“不稳定但高能力的推理组件”。

所以 Agent 开发的核心不是把 prompt 写长，而是把模型当成一个**不可靠但很强的外部系统**去治理，包括：
- 限制输出格式
- 做结构化解析
- 做失败回退
- 做缓存
- 做效果评估

换句话说，Agent 工程的重点不是“让模型更聪明”，而是“让系统更稳”。

### 48. 你怎么理解 Agent 的边界？什么时候不该上 Agent？
**答：**
我的判断是：只有当任务确实需要推理拆分、多步决策或工具编排时，Agent 才有价值。

如果只是简单问答、固定查询、单轮工具调用，其实直接走确定性流程更稳、更快、也更便宜。

所以在这个项目里，我专门做了 Supervisor 分诊，而不是所有请求都强行上多 Agent。这本质上也是在控制 Agent 的适用边界。

### 49. 如果面试官质疑“这不就是普通工作流，不是真 Agent”，你怎么答？
**答：**
我会说这个质疑有一定道理，关键看你怎么定义 Agent。

如果把 Agent 理解成“完全自主、可长程规划的智能体”，那当前项目更准确地说是**基于 LLM 规划的任务编排型 Agent 系统**；它的核心不是开放式自主，而是：
1. 由 LLM 决定任务是否拆分。
2. 对复杂任务进行结构化子任务规划。
3. 结合工具调用完成多步骤处理。
4. 在主流程上有明确的回退和工程约束。

所以我不会把它吹成通用 AGI，而会把它定义成“面向具体业务场景的可控 Agent 流水线”。

### 50. 如果让你面向生产环境继续推进 Agent 化，你最优先补什么？
**答：**
我会优先补三件事：
1. **更细的可观测性**：任务规划成功率、回退率、子任务耗时、工具失败率。
2. **结果可信度机制**：对子任务和最终答案增加证据对齐与置信度输出。
3. **更强的测试闭环**：把典型复杂任务沉淀成固定 benchmark，持续跑回归。

因为 Agent 最大的问题不是“能不能跑起来”，而是“跑出来的结果能不能持续信任”。

---

## 十、反问型高压问题

### 51. 你简历里写了多 Agent 并行，如果我问你并发一定比串行好吗？
**答：**
不一定。并行只是在任务独立、下游资源允许、结果整合成本可控时才更优。

如果子任务强依赖、共享上下文很多，或者结果冲突很严重，那并行带来的整合成本可能抵消收益。还有一个现实问题是，并发会放大 Token 消耗和外部调用压力。

所以我在项目里的态度不是“默认并行”，而是先由 Supervisor 判断是否值得拆分。

### 52. 如果没有 ChromaDB，你这项目还能工作吗？
**答：**
能，但效果会下降。

因为当前检索链路里已经做了降级：
- Chroma 不可用时可以关闭向量检索。
- 还能继续走 BM25。
- 如果 BM25 也异常，还能回退到 MySQL 关键词检索。

所以从系统可用性看它还能工作，但从语义召回能力看会损失一部分效果。这也是为什么我会说这是“可用性优先”的设计。

### 53. 如果评估模型本身不稳定，那你的评估结果还有意义吗？
**答：**
有意义，但更适合作为**相对指标**，不适合绝对化理解。

也就是说，我更关心“同一批测试集、同一套评估配置下，方案 A 和方案 B 谁更好”，而不是说某次跑出来 0.83 就代表真实线上准确率一定是 83%。

为了降低波动影响，项目里做了结构化解析、超时降级、规则评分和批量聚合，本质上是在提高评估结果的工程可用性。

### 54. 你觉得这个项目里还有哪些地方你简历上不能写太满？
**答：**
我会比较谨慎的点有三个：
1. **长期记忆**：当前更接近上下文压缩和会话级内存，不算完整持久化长期记忆。
2. **Writer / Reviewer / Judge 多层 Agent**：如果代码里没有独立模块，不会强写成完整多级智能体系统。
3. **具体性能和指标提升数字**：如果没有压测报告或实验记录，不会把提升百分比写得很死。

我觉得面试里主动把边界讲清楚，反而比写得很满更可信。

## 十一、任务持久化与可观测性深挖

### 57. 你简历里写了 Agent 规划与协作流水线，任务执行过程中如果失败了怎么办？
**答：**
当前项目已经实现了完整的任务持久化和容错机制，不会因为单次失败导致整个上下文丢失。

核心机制有四层：
1. **AgentTask 持久化**：每个对话请求创建一个 AgentTask 记录（taskId/sessionId/userId/query/status/startTime/totalSteps），状态从 CREATED → PLANNING → RUNNING → COMPLETED/FAILED/PAUSED 流转，失败时记录到数据库，前端可查询任务状态。
2. **AgentTrace 全链路追踪**：记录规划决策（Supervisor 返回的子任务列表、执行模式）、工具调用链（每次调用的 name/args/result/duration/quality）、反思记录（ReflectionService 分析卡住原因）、质量评审（QualityReviewer 的 verdict/feedback）、Token 消耗统计，支持回溯"第几步卡在哪"。
3. **AgentTaskStep 细粒度记录**：每一步操作（stepId/goal/toolsCalled/result/duration）都持久化，通过 ToolExecutionService 统一封装工具执行并记录。
4. **暂停/恢复机制**：用户可主动暂停任务（`POST /agent/tasks/{taskId}/pause`），系统保存 AgentExecutionSnapshot（当前轮次、已调用工具、AgentState、消息历史），后续通过 `POST /agent/tasks/{taskId}/resume` + AgentTaskResumeService 重建上下文继续执行。

服务重启后虽然内存上下文丢失，但 AgentTask/AgentTrace/AgentTaskStep 都已持久化，用户可以查看执行历史、了解失败原因，或者发起新请求重新执行。这是在可靠性和性能间取了平衡——正常暂停支持恢复，异常中断支持查询历史重新执行。

### 58. 你提到了反思与重规划机制，这是怎么工作的？
**答：**
这是我在 Agent 系统里做的自适应优化，解决"连续无进展时不是傻等，而是让 LLM 做元认知分析"的问题。

**反思机制**（ReflectionService）：
- 触发条件：AgentState.consecutiveNoProgress >= 2，即连续两轮没有新的工具调用、证据增长或目标推进。
- 执行流程：调用 `reflectionService.reflect(state, query)`，把当前 goal、toolHistory、failedActions、observations 发给 LLM，让它分析"为什么卡住了"。
- 输出结果：ReflectionResult（problem: 卡住原因如"工具参数错误"、suggestedStrategies: 建议策略如"换用 searchNotes 模糊搜索"），持久化到 AgentReflection 表。
- 使用方式：注入到 AgentState.workingMemory 作为"已知经验"，并触发重规划。

**重规划机制**（ReplanningService）：
- 触发条件：反思完成后调用 `replanningService.replan(state, reflectionResult)`。
- 执行流程：基于反思结果 LLM 生成新的执行计划，更新 AgentState.pendingSteps。
- 与 Supervisor 区别：Supervisor 是任务入口时的初始规划（事前，基于静态信息），ReplanningService 是执行过程中的动态调整（事中，基于实际执行反馈），粒度是单个 AgentLoop 内部的执行步骤调整，不会跨 AgentLoop 重新编排。

效果：对于"笔记标题记错"类错误，旧版会卡满 10 轮；新版第 3 轮触发反思 → 建议"先 searchNotes 模糊搜索" → 第 4 轮成功找到正确笔记。反思和重规划结果都记录到 AgentTrace，方便回溯"第几轮触发了反思、LLM 给了什么建议"。

### 59. 你提到了预算控制，具体是怎么限制的？
**答：**
通过 AgentPolicyService + BudgetConfig 实现四个维度的预算控制，防止 Agent 资源耗尽：

**BudgetConfig 配置**：
- maxIterations: 最大轮次（默认 10）
- maxTimeSeconds: 最大执行时间（默认 300 秒）
- maxTokens: 最大 Token 消耗（默认 50K）
- maxToolCalls: 最大工具调用次数（默认 30）

**检查流程**：
AgentLoop 每轮开始前调用 `agentPolicyService.checkBudget(state)`，检查当前累计的轮次、耗时（通过 AgentTask.startTime 和当前时间差）、Token（通过 TokenCounter 估算每次 LLM 调用后累加到 AgentState.totalTokensConsumed）、工具调用次数（从 AgentState.toolHistory.size() 获取）是否超限。如果任意一项超限，返回 PolicyCheckResult{allowed=false, reason="..."}，AgentLoop 立即返回 BUDGET_EXCEEDED 结果，不再继续执行。

**可配置性**：
BudgetConfig 可以按用户、按任务类型配置不同阈值，比如普通用户 maxIterations=10、VIP 用户可以放宽到 15。目前代码里是全局配置，后续可以扩展为动态策略。

这样即使 LLM 陷入循环或任务过于复杂，也能在超限时及时停止，避免成本失控或占用资源过久阻塞其他请求。

### 60. SSE 流式输出和 Agent 任务追踪是怎么对接的？
**答：**
前端通过 SSE（Server-Sent Events）接收实时更新，后端通过 `AgentEventService` 发送多种类型的事件：

**事件类型**：
- **thinking**：Agent 执行进度（stage: planning/running/reflecting/composing/complete，message/iteration/goal）
- **response**：流式回答内容（50 字符/块，间隔 50ms，模拟打字机效果）
- **done**：任务完成（包含 session_id、task_id、trace_id、token_used/token_max、artifacts 产物列表、total_steps、duration_ms）
- **error**：任务失败（包含 message、session_id、task_id）
- **task_created**：任务创建（task_id、session_id、status）
- **tool_call**：工具调用开始（tool_name、args）
- **tool_result**：工具调用完成（tool_name、result、quality）

**任务追踪 API**：
- `GET /agent/tasks/{taskId}` — 查询任务详情（AgentTaskDetailResponse）
- `POST /agent/tasks/{taskId}/pause` — 暂停任务
- `POST /agent/tasks/{taskId}/resume` — 恢复任务（AgentTaskResumeRequest）
- `GET /agent/tasks` — 查询用户任务列表（AgentTaskSummaryResponse[]）
- `GET /agent/traces/{traceId}` — 查询执行追踪详情
- `GET /agent/metrics/tools` — 查询工具调用指标

SSE 超时设置为 300 秒（5 分钟），通过 CompletableFuture.runAsync 配合自定义线程池异步执行，避免阻塞 Tomcat 的请求处理线程。finally 块里会清除 AgentTools 检索范围配置（ThreadLocal）、清除 SecurityContext、清除 SkillContext，如未发完成事件会发送 error 事件并更新 AgentTask 状态为 FAILED。

---

### 55. 如果让你用一句话总结这个项目的技术价值，你会怎么说？
**答：**
我会说：这个项目的核心价值，不只是把大模型接进笔记系统，而是围绕 **Agent 编排、混合检索、质量评估和一致性保障**，把一个不稳定的 RAG/LLM 系统做成了一个相对可控、可回归、可恢复的后端服务。

### 56. 如果让你总结自己在这个项目里的成长，你会怎么说？
**答：**
我最大的收获是把对大模型系统的理解，从“调 prompt”提升到了“做工程治理”。包括什么时候该拆 Agent、怎么给检索做降级、怎么设计最终一致性、怎么定义 RAG 质量，以及怎么用评估和实验支撑优化决策。

---

## 代码映射（面试前可快速复习）
- Supervisor 分诊与 JSON 清洗：`D:\code\javacode\RAG-Note\LangChain-RAG-FastAPI-Service-master\backend-java\src\main\java\com\rag\notebook\agent\SupervisorService.java`
- 混合检索链路：`D:\code\javacode\RAG-Note\LangChain-RAG-FastAPI-Service-master\backend-java\src\main\java\com\rag\notebook\rag\HybridRetriever.java`
- 文档解析与入库：`D:\code\javacode\RAG-Note\LangChain-RAG-FastAPI-Service-master\backend-java\src\main\java\com\rag\notebook\rag\DocumentProcessor.java`
- 异步向量写入与重试：`D:\code\javacode\RAG-Note\LangChain-RAG-FastAPI-Service-master\backend-java\src\main\java\com\rag\notebook\rag\DocumentTaskExecutor.java`
- 切片与 overlap：`D:\code\javacode\RAG-Note\LangChain-RAG-FastAPI-Service-master\backend-java\src\main\java\com\rag\notebook\rag\RagChunker.java`
- 四指标评估：`D:\code\javacode\RAG-Note\LangChain-RAG-FastAPI-Service-master\backend-java\src\main\java\com\rag\notebook\evaluation\service\EvaluationService.java`
- 消融实验：`D:\code\javacode\RAG-Note\LangChain-RAG-FastAPI-Service-master\backend-java\src\main\java\com\rag\notebook\evaluation\service\AblationExperimentService.java`
- 上下文压缩：`D:\code\javacode\RAG-Note\LangChain-RAG-FastAPI-Service-master\backend-java\src\main\java\com\rag\notebook\agent\ContextManager.java`
- 会话级上下文记忆：`D:\code\javacode\RAG-Note\LangChain-RAG-FastAPI-Service-master\backend-java\src\main\java\com\rag\notebook\agent\ConversationContextManager.java`
