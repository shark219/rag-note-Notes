# RAG-Note 简历深挖面试题（后端 / 测开 / Agent 开发）

## 使用说明

这份笔记按面试口述整理，不建议逐字背诵。每道题优先按“先说结论，再说实现，最后说取舍和边界”回答。

- **项目已实现**：代码中可以直接定位到的行为。
- **生产化建议**：面试设计或后续优化，不能说成当前已经上线。
- 没有压测报告时，不编造“提升 30%”“性能提升 3 倍”等数字；可以说优化方向和待验证指标。
- 面试中主动承认边界，比把普通工作流包装成“完全自主 Agent”更可信。

## 一、项目开场与整体架构

### 1. 请先介绍一下这个项目

这是一个面向研发团队的**企业知识库智能助手**，主要提供文档管理、知识库构建、RAG 问答和任务自动化处理。技术栈是 Spring Boot 3 + Vue3，MySQL 存业务数据，ChromaDB 做向量存储，Redis 做缓存，前端用 SSE 实现流式输出。

核心有三块：

  第一是 RAG 检索。我用向量检索和 BM25 混合召回，对知识库和笔记分别检索后用 RRF 融合，再调智谱的 rerank API精排。检索完会扩展上下文，知识库扩前后邻居 chunk，笔记如果命中多个就拉整节内容，最后拼参考资料让 LLM 生成摘要。

  第二是 Agent 执行。主入口不是直接调 RAG，而是先进 AgentService，AgentLoop 最多跑 10 轮，自己决定调什么工具。简单问题单Agent 直接干，复杂问题 Supervisor 会拆成 2-4 个子任务，顺序执行或并行执行。如果连续失败 2次，会触发反思分析原因，调整策略继续试，关键步骤失败就直接终止让用户介入。

  第三是多存储一致性。入库时 MySQL 和 BM25 同步写，ChromaDB 异步写，失败了标 vector_failed。删除时优先保证 MySQL 和 BM25删干净，ChromaDB 删失败写补偿任务，后台定时重试。这样主流程不会被向量库卡住，又能保证最终一致。

  我主要负责后端和 AI 链路，包括文档解析、混合检索、Agent 执行、反思重规划、多存储补偿这些核心功能，前端的 SSE交互我也参与了。整体上我比较关注模型能力和工程稳定性的平衡，通过工具白名单、轮次上限、预算管控、失败降级这些机制，让Agent 既灵活又可控。

**可能追问：**

- **你在这个项目中具体负责哪些工作？**：项目的研发工作主要由我负责，测试部分由另一位同学负责。我主要负责后端和 AI 相关链路，包括文档解析和知识库构建、RAG 检索、Supervisor-Worker Agent 编排，以及上下文管理和一些异常降级机制。前端的主要页面和 SSE 流式交互我也参与了开发，所以整体的核心功能链路基本都是我负责实现的。

- **项目最难的点是什么？**：主要有三点：一是模型输出不稳定，特别是 Supervisor 拆任务时 LLM 可能返回非标准 JSON或包含中文说明，需要做清洗和括号计数兜底提取；二是多存储最终一致性，MySQL/BM25/ChromaDB三者要保证主流程强一致、向量库最终一致，同时处理 ChromaDB 故障时的补偿重试；三是精确检索和语义检索的平衡，BM25对关键词敏感但语义能力弱，向量检索语义能力强但对专有名词召回不足，最终通过 RRF 融合和 Reranker精排找到了较好的平衡点。

- **这是完整自主 Agent 吗？**：不是完全自主的 Agent。它是受约束的任务编排型 Agent：有工具白名单限制（每个子任务只能调用指定工具）、有执行轮次上限（10轮）、有 Token 预算管控、有状态机和反思机制约束。Supervisor 拆任务后，每个 Worker 在独立上下文中执行，失败后通过反思和重规划调整策略，但不会无限循环，最终要么达成目标、要么达到轮次上限、要么触发用户介入。这种设计在保留 Agent灵活性的同时，通过工程化手段保证了执行过程的可控性和稳定性。单和状态机约束的任务编排型 Agent。

### 2. 为什么做 RAG，而不是直接调用大模型？又为什么不把相关文档发给豆包搜索整理呢？

RAG 主要是为了解决**企业内部知识无法直接依赖通用大模型，以及企业知识需要持续维护和可控检索**的问题。

如果直接调用大模型，它本身并不知道企业内部的研发文档、接口规范和历史技术资料，即使模型本身能力很强，也无法保证回答一定基于企业内部知识。

那为什么不直接把相关文档交给豆包这类大模型，让它搜索和整理呢？我认为这种方式可以解决一部分问题，但对于企业知识库来说，**数据的可控性、权限隔离、检索方式和知识更新**都比较重要。企业内部不同用户能访问的文档可能不同，而且我们希望能够控制“检索了哪些资料、为什么召回这些资料”，并对整个过程进行追踪和评估。

所以项目采用 RAG，把**企业自己的知识库和大模型解耦**：先在我们自己的知识库里检索相关资料，再把检索结果作为上下文交给大模型生成答案。这样既可以利用大模型的理解和总结能力，又可以控制知识来源。
具体检索上，我没有只使用一种方式。像**异常码、类名、接口名、版本号**这类精确技术信息，主要通过 BM25 检索；对于口语化或者同义表达，再通过向量检索补充语义召回，最后使用 RRF 融合两路结果，必要时再进行 Rerank。
另外，RAG 的检索链路也可以由我们自己控制。比如向量库异常时，可以关闭向量检索，继续使用 BM25 或 MySQL 关键词检索。虽然这时候语义召回能力会下降，但至少不会因为某个外部组件故障导致整个知识库问答不可用。

**可能追问：**

- **那直接把文档给豆包，让它总结不是也可以吗？**  
    可以，尤其是文档数量少、一次性分析的场景。但企业知识库通常是长期积累的，文档数量多，而且涉及权限、更新、检索和审计，所以更适合把企业知识沉淀到自己的知识库中，再通过 RAG 按需检索。
    
- **普通网页搜索不可以吗？**  
    如果问题是公开知识，网页搜索完全可以。但企业研发问题很多是**内部知识**，比如公司自己的接口规范、项目代码说明、内部故障记录，这些内容可能根本不存在于互联网，所以网页搜索解决不了。
    
- **为什么不能只用向量检索？**  
    研发知识里有很多异常码、类名、接口名和版本号，这些对精确匹配比较敏感，所以需要 BM25 和向量检索结合。
    
- **RAG 能消除幻觉吗？**  
    不能。RAG 是给模型提供外部证据，降低模型凭自己猜测的概率。但如果召回错误、文档过时或者模型误读证据，仍然可能产生错误。
    
- **"可控检索"怎么实现的？**
	  第一是检索策略可配置。我们支持通过消融配置动态开关 Query 扩展、向量检索、BM25、RRF 融合、Rerank 这些组件，可以做 A/B测试对比效果。
	  第二是检索范围可指定。用户可以明确指定只检索某几篇文档或笔记，避免无关内容干扰结果。
	  第三是检索结果可追溯。每次检索都记录到 RagTrace 表，包括原始 query、扩展 query、命中的 chunk、rerank前后的排序，方便问题复现和效果调试。
	  第四是检索失败可降级。向量库故障时自动降级到 BM25，BM25故障时降级到关键词检索，不会因为单个组件挂了导致整个检索不可用。
	  第五是检索质量可评估。我们参考 RAGAS 实现了忠实度、相关性、准确度、召回率这些指标，可以批量评估不同检索策略和 TopK参数的效果，通过量化指标驱动检索链路优化。
	  所以"可控"其实是指检索过程的每个环节都在我们自己的系统里，可以配置、追踪、降级、评估和优化，而不是完全依赖外部黑盒服务。

**补充细节：**

如果直接把文档交给豆包之类的大模型：

```text
企业文档
   ↓
大模型
   ↓
整理答案
```

确实能工作，但企业场景会进一步遇到：

```text
文档很多怎么办？
哪些文档这个用户有权限看？
应该检索哪些文档？
文档更新以后怎么保证使用最新版本？
这次回答到底引用了哪些资料？
检索效果怎么评估？
```

而 RAG 把这些事情拆开了：

```text
                企业知识库
                    ↓
用户问题 → 检索 → 相关 Chunk
                    ↓
              权限 / 过滤 / 排序
                    ↓
                  LLM
                    ↓
                  答案
```

所以你这个项目里，**RAG 的价值不只是“防幻觉”**。

更重要的是：

> **把企业自己的知识接入大模型，并且让知识来源、检索过程和数据权限尽可能由企业自己控制。**

这句话其实很适合面试时最后收尾。

另外有一点建议你特别注意：**不要把“豆包搜索”说成一定不安全或者一定不能用。** 更严谨的说法是：

> “通用大模型的搜索能力适合公开互联网信息，但企业研发知识库更关注私有数据、权限隔离、可控检索、知识更新和过程追踪，所以我们选择自建 RAG 链路。”

这样会比直接说“豆包不行，所以我们做 RAG”专业很多。

#### 补充：为什么不选择微调/纯 prompt？

我觉得这两种方案并不是完全不能用，而是和我们这个**企业研发知识库**的主要需求不太匹配。

先说微调。微调更适合改变模型的**行为和输出方式**，比如让模型形成特定的回答风格、格式或者任务能力。但我们这个项目的核心问题是让模型能够使用企业不断更新的研发知识。
比如接口文档、技术方案、故障案例都会持续变化，如果把这些知识直接训练进模型，每次知识更新都需要重新准备数据、训练和部署，维护成本比较高。而 RAG 可以把**知识和模型参数解耦**，文档更新后重新解析、切片和建立索引，后续检索就可以使用新的知识，不需要重新训练模型。

再说纯 Prompt。如果只是把少量资料放进 Prompt，让模型根据资料回答，当然是可以的。但企业知识库的文档数量比较多，而且不同问题真正相关的资料也不同。如果每次都把大量文档放进上下文，会增加 Token 消耗，也不利于模型准确关注真正相关的信息。

所以我们的做法是**RAG + Prompt 工程结合**。

RAG 主要负责解决“**从企业知识库里找到什么资料**”；Prompt 主要负责解决“**模型拿到这些资料之后应该怎么做**”。
比如用户问一个接口参数问题，先通过 BM25 和向量检索找到相关接口文档，再通过 Prompt 要求模型基于这些资料回答；而 Supervisor 的任务拆分、Reflection 的反思以及最终答案组织，也主要依赖 Prompt 来约束模型行为。

所以从这个项目来看，**RAG 更适合解决企业知识持续更新的问题，Prompt 工程负责控制模型行为，而微调并不是当前最优先的方案。**

**可能追问：**

- **那微调完全没用吗？**：不是。如果后续需要统一输出风格、特定任务能力或者让模型稳定遵循某种行为，可以考虑微调。但企业知识本身持续变化，所以知识部分还是更适合通过 RAG 提供。
    
- **如果文档只有几篇，还需要 RAG 吗？**：不一定。如果知识量很小，可以直接放进 Prompt。RAG 的价值是在知识量变大、需要持续更新和精准检索时更加明显。
    
- **RAG 和 Prompt 是替代关系吗？**：不是。RAG 负责提供外部知识，Prompt 负责告诉模型如何使用这些知识，两者通常是配合使用的。
    
- **为什么不直接把所有文档放进上下文？**：文档数量一多，Token 成本和上下文长度都会增加，而且大量无关信息会干扰模型。所以更合理的方式是先检索，再把和当前问题相关的内容交给模型。
    

**补充细节：**

| 方案       | 知识更新成本         | Token 成本     | 适用场景        |
| -------- | -------------- | ------------ | ----------- |
| 微调       | 高（重新训练）        | 低（知识在权重里）    | 改变模型行为、输出风格 |
| 纯 Prompt | 低（直接修改 Prompt） | 高（所有知识都在上下文） | 知识量少、一次性任务  |
| RAG      | 低（上传新文档即可）     | 中（只检索相关片段）   | 知识持续更新、知识量大 |

你可以把三种方案简单理解成：

```text
微调：
企业知识
   ↓
训练
   ↓
模型参数
   ↓
回答
```

问题是：**知识更新需要重新训练。**

---

```text
纯 Prompt：
企业文档 + 用户问题
        ↓
       LLM
        ↓
       答案
```

问题是：**文档一多，Prompt 就会越来越长，而且每次都需要把资料放进上下文。**

---

而 RAG 是：

```text
企业文档
   ↓
解析 / 切片 / 建索引
   ↓
知识库

用户问题
   ↓
检索
   ↓
相关 Chunk
   ↓
Prompt + Chunk
   ↓
LLM
   ↓
答案
```

所以对于你的项目，最核心的区别其实是：

> **微调是把知识/能力更多地“写进模型”，纯 Prompt 是把资料“临时放进上下文”，RAG 则是把知识独立存储，需要的时候再检索给模型。**

而你的企业研发知识库有一个非常关键的特点：**知识是持续变化的。**

因此最适合的是：

> **知识用 RAG 管，行为用 Prompt 管。**

这个总结非常适合你面试的时候最后一句收尾。

对，这版修正以后**准确度明显更高**。尤其是你把“权限校验范围”“Worker 工具白名单”“反思/重规划”“RAG 摘要而不是最终答案”这些容易被面试官抓住的点纠正了。

不过如果目的是**面试口述**，现在这个“完整版本”还是偏长。建议保留技术细节，但把主回答压缩成 1～2 分钟，追问再展开。可以整理成下面这个最终版：

---

### 3. 整体架构和一次问答链路怎么走？

我可以从一次问答请求来说明。

用户请求进入 Spring Boot 后，首先进行**身份认证和会话校验，并通过 userId 做数据隔离**。然后根据问题复杂度选择单 Agent 或 Supervisor-Worker。

简单问题直接进入 Agent Loop；复杂问题先由 Supervisor 拆成 2～4 个子任务，根据任务依赖选择串行或者并行执行。Worker 在独立上下文中执行，而且每个子任务都有**工具白名单**，只能调用指定工具。执行过程中如果连续失败 2 次，会触发 ReflectionService 分析失败原因，再由 ReplanningService 调整当前执行策略，引导 Agent 换工具或参数继续尝试；如果关键步骤最终失败，则会终止并返回澄清问题。

进入 RAG 后，系统默认先进行 Query 扩展，最多生成 3 个不同版本问题。然后对**知识库和笔记分别**进行向量检索和 BM25 检索，每个数据源内部通过 RRF 融合，之后合并结果并调用 Rerank API 精排，再进行动态 Top-N 过滤和邻域上下文扩展。

最后，把筛选后的证据交给模型生成**知识摘要**，这个摘要再回到 Agent 层，由 ResponseComposer 组织最终答案，QualityReviewer 做忠实度检查，最后通过 SSE 流式返回前端。


数据层面，MySQL 保存用户、文档、Chunk、任务和 Trace 等业务数据；Lucene BM25 和 Chroma 主要作为可重建的检索索引，Redis 用来缓存可以重新计算的规划和 Query 扩展结果。
文档入库时，会先解析、切片和 MD5 去重，然后同步写入 MySQL 和 BM25，向量数据通过异步任务批量写入 Chroma。失败后标记 `vector_failed`，再通过重试机制补偿。
整个过程中，Agent 步骤、工具调用、反思重规划、模型调用和异常都会记录 Trace，SSE 则负责把执行事件实时推送给前端。

---

可能追问：

**1. 为什么不全部使用多 Agent？**
简单问题直接走单 Agent，可以减少 Supervisor 的额外规划调用、Token 消耗和执行延迟，同时也降低系统复杂度。只有确实存在多个相对独立子目标时，才使用 Supervisor-Worker。

**2. BM25 或 Chroma 出问题怎么办？**
检索链路有降级机制。某一路异常时先关闭故障路径，继续使用另一条检索方式；如果检索服务都不可用，再降级到 MySQL 关键词检索，保证基本查询能力。

**3. 为什么 Worker 不能随便调用工具？**
主要是为了**控制 Agent 的执行边界**。例如 FETCH 子任务只允许调用 `fetchUrl`，CREATE_NOTE 子任务只允许调用 `createNote`。这样可以减少 Agent 误调用工具、越权操作或者执行无关动作的风险。

**4. SSE 和 Trace 有什么区别？**
SSE 是**实时通知机制**，负责把 thinking、tool_call、response、done 等事件推给前端；Trace 是**持久化记录**，保存完整的 Agent 执行过程。SSE 断开以后，Trace 仍然可以用于任务回溯和问题排查。

**5. RAG 为什么生成的是摘要，不是最终答案？**
因为 RAG 在这里是 Agent 的一个工具。它负责从知识库中检索资料并整理成知识摘要，返回给 Agent 作为证据。最终答案还要结合用户目标、其他工具结果，再经过 ResponseComposer 和 QualityReviewer 处理。

补充细节：

**核心流程：**
```text
用户问题
    ↓
  Spring Boot
    ↓
  身份认证 / 会话校验 / userId 隔离
    ↓
  判断复杂度
    ↓
  单 Agent / Supervisor-Worker
    ↓
  Worker Agent Loop（最多 10 轮）
    ↓
  调用工具（受白名单限制）
    ↓
  连续失败 2 次 →反思 + 重规划
    ↓
  调用 RAG Tool
    ↓
  Query 扩展（最多 3 个变体）
    ↓
  知识库和笔记分别检索
    ├─ 向量检索 + BM25
    └─ RRF 融合
    ↓
  合并 + Rerank 精排
    ↓
  动态 top-N 过滤 + 邻域扩展
    ↓
  生成知识摘要
    ↓
  ResponseComposer 组织答案
    ↓
  QualityReviewer 审查忠实度
    ↓
  SSE 流式返回前端
```

如果让你**一句话概括架构**，可以说：

> **“这是一个以 Spring Boot 为核心的模块化单体，Agent 层负责任务编排和工具执行，RAG 层负责企业知识检索，MySQL、Lucene、Chroma 和 Redis 分别承担事实存储、关键词检索、向量检索和缓存，最后通过 Trace 做过程追踪、SSE 做前端流式展示。”**

### 4. 权限校验与多人协作是怎么设计的？

目前项目采用的是**基于 Space 的团队协作模型**。用户登录后通过 JWT 完成身份认证，进入具体操作时，根据 `userId` 和 `spaceId` 判断用户的数据访问范围。

在数据模型上，我把知识库和笔记都归属到一个 Space。一个 Space 可以包含多个成员，通过 `space_member` 表维护用户和空间的关系，同时给成员设置不同角色，比如 **OWNER、ADMIN 和 MEMBER**。

查询数据时，不再只是简单按照 `userId` 过滤，而是同时考虑用户自己的私有数据和加入的 Space 数据。比如用户加入了多个团队空间，那么查询知识库和笔记时，就可以获取自己的私有内容以及这些 Space 下的共享内容。

对于写入、修改和删除这类操作，会进一步做权限判断。比如 **OWNER 和 ADMIN 可以管理空间内的公共资源，普通 MEMBER 主要负责查看以及管理自己创建的内容**，避免普通成员修改或者删除其他人的数据。

检索这一层也做了权限隔离。BM25 和向量检索都会带上用户对应的 `userId`、`spaceId` 过滤条件，确保 Agent 做 RAG 检索时，只能检索当前用户有权限访问的知识，不能因为语义相似就把其他空间的数据召回出来。

整体来说，我把**身份认证、数据查询、写操作和 RAG 检索**几个环节都做了权限控制，而不是只在前端限制。这样即使用户绕过前端直接调用接口，后端也会重新进行权限校验。

**可能追问：**

- **为什么要引入 Space，而不是直接给每个文档配置权限？**  
    因为研发团队通常是围绕项目或者团队空间协作的，Space 可以让一组成员共享知识库，权限模型比较简单，也方便后续扩展。
    
- **不同角色有什么区别？**  
    OWNER 负责空间管理，ADMIN 可以管理成员和公共资源，MEMBER 主要负责查看以及管理自己创建的内容。
    
- **RAG 怎么保证不会检索到没有权限的数据？**  
    检索时会把用户的 `userId` 和可访问的 `spaceId` 作为过滤条件，BM25 和向量检索都做同样的权限过滤，最后只有有权限的数据才能进入 RAG 上下文。
    

**补充细节：**

可以把整个权限链路简单理解成：

```text
用户登录
   ↓
JWT 身份认证
   ↓
获取 userId
   ↓
确定可访问的 Space
   ↓
MySQL 查询 / BM25 检索 / 向量检索
   ↓
都带权限过滤
   ↓
只有有权限的数据进入 Agent / RAG
```

核心是**权限控制不能只做在查询接口，而要一直贯穿到检索和 Agent 上下文**。

另外，面试时如果按“已实现”来讲，建议不要主动说“目前只支持个人私有”。直接把 **Space + space_member + 角色权限 + 检索过滤**作为当前项目的完整权限设计来回答即可。

### 4. 为什么选择模块化单体，而不是微服务？

我选择模块化单体，主要是结合项目当前的规模和迭代阶段来考虑的。

这个项目里的 Agent、RAG、知识库和评估模块之间调用比较频繁，目前业务规模也还没有达到必须独立部署和扩缩容的程度。如果一开始就拆成微服务，会增加很多额外的复杂度，比如服务之间的网络调用、服务发现、独立部署以及跨服务排查问题。

所以目前我是在一个 Spring Boot 服务内，按照 Agent、RAG、知识库等不同的业务模块进行划分，保持模块之间相对清晰的边界。这样既方便开发和本地调试，也能比较容易地共享领域对象和 Trace 链路。

我认为微服务并不是拆得越早越好，而是应该根据实际的业务瓶颈来决定。如果后面发现某些模块，比如文档解析、向量化或者批量评估，存在明显的资源占用或者独立扩缩容需求，再把这些模块优先拆出去会更合理。

**可能追问：**

- **模块化单体以后会不会变成大泥球？**：这个风险确实存在，所以需要在代码层面保持清晰的模块边界，比如通过包结构、接口和依赖方向进行约束，尽量避免一个模块直接修改另一个模块的内部状态。
    
- **如果要拆服务，你会优先拆什么？**：我会优先考虑文档解析、向量化或者批量评估这类耗时比较长、资源消耗比较高的任务，因为它们比较适合异步执行，也更容易产生独立扩缩容需求。
    
- **什么时候你认为必须拆成微服务？**：当某个模块出现稳定的容量瓶颈，需要独立扩缩容，或者它的故障会明显影响核心业务时，我会考虑拆分。具体还是要结合 QPS、响应时间、资源占用和故障情况等数据判断，而不是简单按照代码模块机械拆分。

**补充细节：**

| 对比维度      | 模块化单体 (Modular Monolith) | 微服务 (Microservices)             |
| --------- | ------------------------ | ------------------------------- |
| **部署方式**  | 单个部署单元（1个JAR/WAR/二进制文件）  | 多个独立部署单元（每个服务独立部署）              |
| **模块边界**  | 逻辑隔离（包/命名空间），同进程内调用      | 物理隔离（独立进程），网络调用（HTTP/gRPC）      |
| **数据管理**  | 单一数据库，模块间共享或逻辑隔离         | 每个服务独立数据库（Database per Service） |
| **通信方式**  | 方法调用（内存级，极快）             | 网络调用（有延迟和失败风险）                  |
| **团队协作**  | 适合中小团队（< 10人），协作紧密       | 适合大型团队，各团队独立负责服务                |
| **运维复杂度** | 低（监控1个应用）                | 高（需服务发现、链路追踪、日志聚合）              |
| **扩展粒度**  | 整体水平扩展（整个应用一起扩）          | 按需独立扩展（只扩瓶颈服务）                  |
| **技术异构性** | 技术栈统一                    | 各服务可用不同语言/框架                    |

你可以简单理解为：

```text
模块化单体：

Spring Boot
├── Agent
├── RAG
├── Knowledge Base
├── Evaluation
└── Trace
```

虽然都在一个应用里运行，但是代码上有明确的模块边界。

如果一开始拆成：

```text
Agent Service
      ↓
RAG Service
      ↓
Knowledge Service
      ↓
Evaluation Service
```

模块之间原本的方法调用就会变成网络调用，同时还需要处理服务发现、接口版本、超时、重试和跨服务排障等问题。

所以你这里的核心逻辑其实是：

> **当前模块之间耦合度比较高、调用频繁，而且没有明确的独立扩缩容需求，因此先采用模块化单体降低系统复杂度。**

未来如果出现明显的资源隔离需求，可以优先把：

```text
文档上传
   ↓
消息队列 / 异步任务
   ↓
解析 Worker
   ↓
Embedding Worker
   ↓
ChromaDB
```

这类耗时任务拆出去，而在线问答、Agent 编排等核心请求链路仍然保留在主服务。

这里要注意一点：目前不要说“模块化单体扩展性一定比微服务好”，也不要说“以后一定会拆微服务”。更准确的说法是：

> **当前阶段模块化单体更适合项目的规模和复杂度，后续是否拆分需要根据实际的容量、性能和故障隔离需求决定。**

### 5. MySQL、Redis、Lucene、ChromaDB 为什么要分工？

主要是根据不同数据的性质和查询场景来分工。

**MySQL** 主要保存用户、文档、Chunk、任务和 Trace 这些业务事实；**Redis** 主要缓存 Supervisor 规划、Query 扩展结果这类可以重新计算的数据；**Lucene** 负责 BM25 关键词检索，比较适合异常名、类名、接口名、版本号这类精确术语；**ChromaDB** 则主要负责向量检索，用来处理语义相似的问题。

这样做的好处是让不同类型的查询使用更合适的存储和检索方式。比如用户问一个具体的异常码，BM25 的精确匹配比较有优势；如果用户换一种口语化的方式描述同一个问题，向量检索就更合适。

当然，多存储也会增加系统复杂度，主要体现在数据同步、故障恢复和状态管理上。所以项目里 MySQL 和 BM25 先同步写入，Chroma 的向量数据异步批量写入，并通过状态记录、重试和补偿处理向量写入失败。

整体上，**MySQL 是业务事实来源，Lucene 和 Chroma 是可以重建的检索索引，Redis 只是缓存**，所以不会把 Redis 或检索索引当成唯一数据源。

**可能追问：**

- **为什么不只用 MySQL？**：MySQL 比较适合保存业务数据和做基础查询，但不适合直接承担高质量的语义召回，而且规划、Query 扩展这类高频可重建结果也更适合放在 Redis 做缓存。
    
- **为什么 Lucene 和 Chroma 都需要？**：因为研发知识里既有异常码、类名、接口名这种需要精确匹配的内容，也有口语化、同义表达的问题。BM25 和向量检索的优势不同，所以采用混合召回。
    
- **如果 Chroma 挂了怎么办？**：Chroma 属于可重建的检索索引，所以不会影响 MySQL 中的原始数据。检索时可以关闭向量路径，继续使用 BM25，必要时再降级到 MySQL 关键词查询；Chroma 恢复后再通过补偿任务重建向量索引。
    

**补充细节：**

```text
文档
 ↓
解析 / 切片 / MD5 去重
 ↓
MySQL + BM25 同步写入
 ↓
Embedding
 ↓
ChromaDB 异步批量写入
```

这里的关键是**数据源和索引要区分开**。例如 Chroma 写入失败，并不是文档丢了，而是“检索索引暂时没建立好”，因为原始文档和 Chunk 还在 MySQL 中，后续可以重新生成 embedding、重新建立 Chroma 索引。

所以当前采用的是**最终一致性**，而不是要求 MySQL、BM25、Chroma 三套存储同时成功才算完成。

### 6. 你在项目中最看重的后端工程能力是什么？

我在这个项目里比较看重的一项后端能力是：**怎么把不稳定的外部能力，变成一个可控、可恢复的后端流程。**

第一个比较明显的是 LLM。模型输出并不是一个完全稳定的协议，所以 Supervisor 生成规划时，我会先通过 Prompt 约束 JSON 格式，然后在后端做代码块、前后说明等内容清理和 JSON 提取。如果还是解析失败，就回退到空计划，走单 Agent，避免因为一次模型格式错误导致整个请求失败。

第二个是 RAG。研发知识既有异常码、类名、接口名这类需要精确匹配的内容，也有口语化的语义问题，所以我采用 BM25 和向量检索混合召回，再通过 RRF 融合，必要时进行 Rerank，同时保留关键词降级路径。这里不是简单地认为组件越多越好，而是通过固定数据集和消融实验比较效果、成本和稳定性。

第三个是多存储之间的一致性。MySQL、BM25 和 Chroma 没办法简单放进一个事务里，所以我让 MySQL 和 BM25 先完成主要数据写入，向量数据异步处理。如果异步写入失败，就记录状态，通过重试和补偿恢复，最终达到数据的最终一致。

所以我认为这个项目不只是把正常链路跑通，更重要的是考虑**模型异常、检索故障、异步失败这些非正常情况发生以后，系统还能不能继续工作，以及能不能恢复。**

**可能追问：**

- **怎么证明这些设计真的有效？**：主要通过固定回归集、消融实验和故障注入进行验证，比如比较召回指标、成功率、延迟、Token 消耗、fallback 比例以及故障后的恢复结果。
    
- **LLM 返回合法 JSON，是不是就说明规划正确？**：不是。JSON 解析成功只能说明格式正确，不能证明任务拆分合理，所以还需要通过任务结果、成功条件以及实际执行结果判断规划是否有效。
    
- **目前还有哪些生产化不足？**：目前一些可靠性机制还可以继续完善，比如更可靠的异步任务投递、统一监控告警以及更完整的幂等测试。当前更准确地说是实现了降级、状态和补偿机制，但不能说已经达到强一致或者零故障。
    

**补充细节：**

这道题其实主要想让你体现一个思维：

```text
普通开发：
正常情况能不能跑通？
↓
工程化：
正常情况能跑
+
模型格式错怎么办？
+
向量库挂了怎么办？
+
异步任务失败怎么办？
+
进程执行到一半挂了怎么办？
+
失败后能不能恢复？
```

所以你三个例子分别对应：

```text
LLM
→ 输出不稳定
→ 清洗 + 解析 + fallback

RAG
→ 检索结果不确定
→ 混合召回 + Rerank + 降级 + 评估

多存储
→ 无法使用一个事务
→ 异步 + 状态 + 重试 + 补偿
```

这里还有一个很重要的面试表达：**不要说“我保证了系统可靠”**，而应该说“我针对这些失败场景设计了降级和恢复机制”。因为这些机制只能提高系统的稳定性，不能保证绝对不出错。

## 二、多agent任务规划

### 7. Supervisor-Worker 具体怎么实现？

我这个 Supervisor-Worker 的设计主要是为了区分简单问题和复杂问题。

当用户请求进来之后，Supervisor 会先判断这个问题是不是需要拆分，主要看它有没有相对独立的子目标、会不会需要多种工具，或者需不需要多步证据。如果是比较简单的问题，Supervisor 就返回一个空数组,系统直接走单 Agent 流程；如果判断是复杂问题，Supervisor 会生成一个子任务数组，每个子任务至少包含 `label`（任务标签）、`description`（任务描述）、`toolHint`（建议使用的工具）。当前的设计还支持 `goal`（目标）、`successCriteria`（成功条件）、`dependsOn`（依赖关系）和 `executionMode`（执行模式）。

在实现上，`SupervisorService` 会调用一个比较精确的模型来生成这个计划，返回结果之后我会先通过 `cleanLlmJson` 和 `extractJsonArray` 做清洗、抽取和解析。如果这些处理都失败了，就回退到单 Agent，避免因为规划失败导致整个请求不可用。

子任务生成之后，系统会根据依赖关系进行调度。如果任务之间没有前置依赖，理论上可以并行执行；如果有前置关系，就必须按顺序执行。每个 Worker 执行的时候有自己独立的 `AgentState`、工具历史和结果，执行完成后会记录步骤和 trace。

最后还有一个合成阶段，合成器会把各个子任务的结果、证据以及失败原因结合起来，生成最终答案。如果某个子任务失败了，合成器也会明确说明哪些部分没有覆盖到。

**可能追问：**

- **如果规划失败会怎样？**：清洗和数组抽取都失败的时候，系统会返回一个空计划，然后走单 Agent，这样可以避免因为规划异常导致简单问题也失败。
    
- **你怎么判断任务能不能并行？**：主要看任务之间有没有依赖关系，以及它们的结果是不是相互独立。不过目前全局并发控制、租户限流这些属于生产化建议，还没有完全实现。
    
- **为什么 LLM 返回的 JSON 还需要清洗？**：因为模型实际返回的内容不一定严格符合 JSON 格式，比如可能会带 Markdown 代码块、前后会有一些额外的说明文字，或者数组外面还有其他内容。所以我在解析之前会先做一层清洗和 JSON 数组提取，提高计划解析的鲁棒性。如果清洗之后还是解析失败，就回退到单 Agent。
    

**补充细节：**

你可以简单理解成：

```text
用户请求
   ↓
Supervisor 判断复杂度
   ↓
简单问题 → 空数组 → 单 Agent
   ↓
复杂问题 → 子任务数组
   ↓
├── 子任务 A (Worker 1)
├── 子任务 B (Worker 2)  ← 如果无依赖，可以并行
└── 子任务 C (Worker 3)  ← 如果依赖 A，就顺序执行
   ↓
合成器汇总结果
   ↓
最终答案
```

对于顺序执行的任务，我只会把压缩之后的必要结果放进 `completedContext`，避免上下文过长。对于并行执行的任务，会尽量隔离各自的上下文，避免互相污染。

这里要特别说明一点：规划、工具白名单、轮次、Token、时间和权限都是由后端约束的，所以当前更准确的说法是**受约束的任务编排型 Agent**，而不是开放式自主 Agent。

### 8. 为什么不能所有请求都走多 Agent？

主要是因为多 Agent 会带来额外的成本和复杂度。

首先，多 Agent 会增加模型调用次数、Token 消耗和整体延迟。因为 Supervisor 规划本身就需要调用一次模型，每个 Worker 执行又是独立的 Agent 循环，还有最后的结果合成，这些都会增加整体的响应时间和成本。

其次，规划本身也可能出错。Supervisor 可能会误拆分，把一个本来很简单的问题拆成多个子任务，反而增加了失败的可能性。比如用户只是问一个简单的定义，如果硬要拆成"查定义""查示例""查应用场景"，反而会让整个流程变得更复杂、更容易出错。

所以我的策略是：**简单问题直接走单 Agent，复杂问题才走 Supervisor-Worker**。判断标准不是看问题的字数或者长度，而是看这个问题有没有相对独立的子目标、需不需要用到多种不同的工具，或者需不需要多步证据。

另外，即使 Supervisor 判断需要拆分，如果最后只生成了一个子任务，或者规划的收益不明显，系统也会做二次降级，还是走单 Agent。这样可以避免为了拆分而拆分。

**可能追问：**

- **怎么验证 Supervisor 是不是真的有用？**：最好是用一个固定的问题集，分别用单 Agent 和 Supervisor-Worker 跑一遍，然后对比子任务完成率、最终答案质量、P95 延迟、Token 消耗以及失败率。通过这些数据来判断 Supervisor 是不是值得保留。
    
- **后续怎么优化这个机制？**：生产化的话可以考虑根据任务类型、租户预算、历史指标来动态路由，比如某些类型的问题历史数据显示拆分效果不好，就直接走单 Agent。另外还可以增加全局并发控制。不过这些都是后续的优化方向，不是当前已经实现的能力。
    
- **什么样的问题适合拆分？**：比如需要分别查询现象原因和历史解决方案，而且这两者互不依赖，这种就比较适合拆分成并行任务。但如果第二步必须依赖第一步提取出来的版本号或者错误码，那就应该按顺序执行，或者干脆不拆分。
    

**补充细节：**

你可以这样理解多 Agent 的成本：

```text
单 Agent：
用户问题 → Agent Loop → 工具调用 → 生成答案
               ↑
            一次模型循环

多 Agent：
用户问题 → Supervisor 规划 → 子任务 A (Agent Loop)
                           → 子任务 B (Agent Loop)
                           → 子任务 C (Agent Loop)
                           → 合成结果
               ↑              ↑                    ↑
            规划调用      多个 Agent 循环       合成调用
```

所以多 Agent 的模型调用次数、Token 和延迟都会明显增加。

另外，多 Agent 的收益不能只看一次响应快不快，还要综合看：

- **任务成功率**：拆分之后是不是更容易完成任务
- **拆分率**：多少比例的问题被拆分了
- **回退率**：拆分之后又回退到单 Agent 的比例
- **延迟**：P95 或 P99 延迟有没有增加
- **Token 消耗**：平均 Token 增加了多少
- **单位成本**：每个任务的平均成本

如果没有做量化压测，只能说"已经实现了路由和降级机制"，不能说"多 Agent 一定能提升 X% 的效果"或者"一定能降低 Y% 的延迟"。

核心逻辑是：

> **多 Agent 不是越多越好，而是要在任务复杂度、成本和收益之间找到平衡点。**

### 9. 如果面试官说这只是工作流，不是真 Agent，怎么回答？

更准确地说，我这个项目是一个**基于 LLM 规划、受后端约束的任务编排型 Agent**，而不是完全开放式的自主 Agent。

它和固定工作流的主要区别在于，工作流通常是后端提前定义好”第一步做什么、第二步做什么”。而我的 Agent Loop 中，模型会根据当前目标、历史工具调用和工具返回的 Observation，决定下一步是否调用工具、调用哪个工具，以及是否继续执行。连续没有进展时，还会触发反思和策略调整。

但另一方面，它确实不是完全自主的。后端会限制工具白名单、最大执行轮次、Token 和工具调用次数，关键步骤失败时也会进行阻断或者降级。所以我更倾向于把它定义成**受约束的 Agent**。

对于研发知识库这种场景，我认为这种设计反而更合适，因为重点不是追求 Agent 无限自主，而是保证**权限可控、执行可追踪、结果可回归、失败可以处理**。所以我不会把它包装成通用的自主智能体。

**可能追问：**

- **那 Agent 和普通工作流到底有什么区别？**：核心区别是执行过程中的部分决策由模型根据当前状态动态决定，而不是所有步骤都由代码预先写死。不过工具范围、状态和终止条件仍然由后端控制。
    
- **为什么不追求完全自主？**：完全自主会增加越权、循环调用、成本不可控以及结果难以回归等风险。对于企业知识库场景，我更看重可控性和稳定性。
    
- **怎么证明你不是简单拼了几个 Prompt？**：可以从执行机制看。系统有独立的 `AgentState`、工具调用历史、Observation、目标判断、预算和轮次控制，还有反思事件以及持久化 Trace，模型是在一个受约束的执行循环中做决策，而不是一次 Prompt 直接生成最终答案。
    

**补充细节：**

你可以把两者简单理解成：

```text
固定工作流：
A → B → C → D
步骤提前写死
```

而你的 Agent 是：

```text
目标
 ↓
LLM 判断下一步
 ↓
调用 Tool
 ↓
Observation
 ↓
LLM 再判断
 ↓
继续调用 / 换工具 / 结束
```

但是这个循环外面还有后端”护栏”：

```text
            Agent Loop
                ↓
      ┌─────────────────┐
      │ Tool 白名单      │
      │ 最大轮次         │
      │ Token 预算       │
      │ 工具调用限制     │
      │ 权限 / 审批       │
      └─────────────────┘
```

所以最准确的定位不是：

> “完全自主 Agent”

也不是：

> “纯固定工作流”

而是：

> **LLM 负责局部决策，后端负责边界和约束的 Agent 系统。**

另外，如果面试官继续追问”那你这个和 Workflow 到底是不是一个东西”，不要陷入概念争论。你可以直接说：

> “从工程实现角度，两者确实存在交叉，我这个系统也有比较强的工作流编排特征。区别主要在于其中的 Agent Loop 部分不是固定步骤，而是允许 LLM 根据执行状态动态选择下一步动作。所以我认为称为'受约束的任务编排型 Agent'会比'完全自主 Agent'更准确。”

### 10. LLM 规划结果不稳定，怎么处理？

LLM 生成规划的时候，返回结果确实不太稳定，所以我设计了一个分层处理的机制。

第一层是从 Prompt 层面约束。在 `SupervisorService` 的系统消息里，我会明确要求模型只返回纯 JSON 数组，不要有额外的说明文字，并且使用一个相对精确的模型配置，尽量减少格式问题。

第二层是后端清洗。因为模型实际返回的内容经常会带 Markdown 代码块（比如 ```json...```），或者前后会有一些解释性文字，所以我会先移除这些 Markdown 标记和前后说明，然后截取第一个看起来像 JSON 的主体部分。

第三层是兜底提取。如果经过清洗之后还是解析失败，我会用括号计数的方式来提取 JSON 数组，通过追踪括号的深度来定位数组的起始和结束位置。

如果这三层处理都失败了，就返回空列表，`AgentRuntime` 会保存一个空计划，然后系统回退到单 Agent 执行。这样可以避免因为规划解析失败导致简单问答也失败。

不过这里要特别说明一点：**JSON 能解析成功，只能说明格式正确，不能证明规划本身是合理的**。也就是说，任务边界、依赖关系或者工具建议可能还是不合理的。所以当前这些清洗机制只是保证格式层面不出错，并不能保证规划的语义正确性。

**可能追问：**

- **清洗之后还是不是合法 JSON 怎么办？**：会用 `extractJsonArray` 这个方法，通过维护字符串状态和括号深度来提取数组。如果还是失败，就返回空计划并降级到单 Agent。
    
- **JSON 合法了就可以直接执行吗？**：不可以。还需要校验字段是否完整、工具名是否在白名单里、依赖关系是否合法、任务数量是否合理，以及用户权限。完整的 schema 校验和依赖环检测属于生产化需要补强的部分。
    
- **怎么评估规划质量？**：最好是建立一个固定的问题集，人工标注每个问题是否应该拆分、子任务边界应该在哪里、依赖关系是什么、应该用哪些工具，然后统计 Supervisor 的误拆率（不该拆的拆了）、漏拆率（该拆的没拆）、完成率和回退率。
    

**补充细节：**

你可以把这三层处理理解成：

```text
LLM 返回
   ↓
“好的，规划如下：```json[{...}]```”
   ↓
第一层清洗：移除 Markdown 代码块
   ↓
“好的，规划如下：[{...}]”
   ↓
第二层清洗：截取第一个 JSON 主体
   ↓
“[{...}]”
   ↓
第三层：括号计数提取数组
   ↓
成功解析 or 返回空数组
   ↓
空数组 → 回退单 Agent
```

规划结果还会写入任务计划快照和 Agent trace，这样可以方便后续复盘。`SubTask` 的数据结构支持 `goal`、`successCriteria`、`dependsOn` 和 `executionMode` 这些字段。

要强调的是：**当前没有证据表明清洗机制能保证规划的语义正确性**，也没有可以公开的固定收益数据。这些清洗只是让格式层面不出错，语义层面的规划质量还需要通过标注数据集和持续评估来提升。

### 11. Supervisor 结果为什么缓存？缓存有什么风险？

缓存 Supervisor 规划主要是为了避免重复的模型调用。

因为相同的查询，Supervisor 生成的规划通常是差不多的，如果每次都重新调用模型生成规划，会增加不必要的延迟和 Token 消耗。所以在当前的实现里，`SupervisorService` 会用 `QueryCacheService.hashKey` 对查询做 SHA-256 哈希，然后把规划结果存到 Redis 里，TTL 设置成 10 分钟。如果命中缓存就直接返回，未命中才会调用模型。

但是缓存也有风险，主要是**缓存内容可能过期或者不适用**。

当前的缓存 key 只包含固定前缀和 query 本身，没有包含 Prompt 版本、模型版本、用户配置或者知识库版本。所以如果这些东西变了，可能还会复用旧的规划。比如我改了 Prompt，让 Supervisor 的拆分策略更激进，但是缓存里还是旧的保守策略，就会出问题。

另外，Redis 读写异常的时候，`QueryCacheService` 会记录 debug 日志并返回空或者继续计算，说明缓存只是优化手段，不是事实来源。系统不会因为 Redis 挂了就整体不可用。

**可能追问：**

- **为什么不把最终答案也这样缓存？**：最终答案还会受到历史对话、用户权限、知识库内容和实时状态的影响，不能简单缓存。当前缓存明确针对的是可以重新计算的规划结果，不能绕过鉴权和检索。
    
- **如果 Prompt 改了，怎么避免用到旧的规划？**：当前只能等 10 分钟 TTL 过期。生产化的做法应该把 Prompt 版本和模型版本也纳入缓存 key，发布新版本的时候主动失效旧的缓存。
    
- **Redis 挂了会怎样？**：缓存读取或写入异常会被捕获并吞掉，未命中的时候会重新调用模型生成规划。代价是模型调用次数和延迟会上升，但不会导致核心流程直接失败。
    

**补充细节：**

缓存的关键在于 key 的设计：

```text
当前 key 设计：
prefix + SHA256(query)

存在的问题：
- Prompt 改了，key 不变 → 用到旧规划
- 模型换了，key 不变 → 用到旧规划
- 用户配置改了，key 不变 → 可能不适用

生产化 key 设计：
prefix + SHA256(query + promptVersion + modelVersion + userConfig)
```

缓存流程：

```text
用户 query
   ↓
计算 hash key
   ↓
查询 Redis
   ↓
命中 → 直接返回缓存的规划
   ↓
未命中 → 调用模型生成规划
         ↓
      写入 Redis (TTL 10分钟)
         ↓
      返回规划
```

当前实现了有限 TTL 和 Redis 故障下的降级，但没有证据表明做了请求合并或者严格的主动失效。生产化建议把模型版本、Prompt 版本、规划配置和必要的租户边界都纳入 key，并增加版本化、主动失效、命中率监控、脏命中监控和缓存延迟监控。

### 12. 多子任务并行一定比串行好吗？

不一定。并行不是在所有情况下都比串行好。

并行只适合那些子任务相互独立、下游容量足够、结果冲突比较少的情况。比如我需要分别检索”现象原因”和”历史解决方案”，这两个检索互不依赖，就可以并行执行。但是如果第二个任务必须依赖第一个任务提取出来的错误版本号或者实体信息，那就必须串行执行，否则第二个任务拿不到需要的输入。

并行确实能减少等待时间，但也会带来一些问题。它会增加模型调用的并发压力、Token 消耗、数据库和检索服务的压力，以及结果合成的复杂度。所以不能简单认为并行就一定更好。

另外要特别说明一点：**当前的实现其实还没有真正做到并行**。虽然 `SubTask` 的数据结构里已经有了 `dependsOn` 和 `executionMode` 这些字段，`AgentTaskStep` 也会持久化依赖信息，但是当前的 `AgentRuntime.start` 还是按照 `subTasks` 列表的顺序，用 `for` 循环一个一个执行的。而且当前还是共享 `AgentState`，会把前一步的产物传给后一步，所以实际链路更接近受约束的串行编排。

这些字段和持久化依赖信息只是为将来实现并行调度预留的，不能说已经实现了真正的多子任务并行。

**可能追问：**

- **什么样的条件适合并行？**：子任务之间没有互相依赖、输入是独立的、工具的副作用可控、资源容量足够，并且结果可以独立合成。满足这些条件的时候并行才有意义。
    
- **当前代码真的实现并行了吗？**：没有。`AgentRuntime.start` 当前是按列表顺序调用 `agentLoop.run` 的。字段和持久化依赖只是为并行调度预留信息，不等于已经实现了并发执行。
    
- **怎么决定是否要上线并行功能？**：最好用固定的问题集，分别测试串行和并行两种方案，对比 P95 延迟、Token 消耗、失败率、资源峰值和合成质量。如果没有压测数据，就只能作为生产化建议，不能说已经验证了收益。
    

**补充细节：**

你可以这样理解串行和并行的区别：

```text
串行执行（当前实现）：
任务 A → 任务 B → 任务 C
  ↓        ↓        ↓
等 A 完   等 B 完   等 C 完
总耗时 = A + B + C

并行执行（未来规划）：
任务 A ┐
任务 B ├→ 全部完成 → 合成
任务 C ┘
总耗时 = max(A, B, C) + 合成时间
```

但并行也有代价：

```text
并发压力：
- 模型调用并发 ↑
- 数据库查询并发 ↑
- 检索服务并发 ↑

复杂度：
- 需要依赖图调度
- 需要有界线程池
- 需要全局/租户并发限制
- 需要结果隔离
- 需要取消和失败合成机制
```

当前实现会按任务类型检查缺失的前置产物，比如抓取网页失败的时候会阻断创建笔记等后续步骤；任务、步骤状态和事件都会持久化。

生产化实现并行之前，应该先增加依赖图调度、有界线程池、租户和全局并发限制、结果隔离、取消机制和失败合成逻辑，并且用实验验证收益。不能只是为了并行而并行。

### 13. 多 Agent 结果怎么融合？

多 Agent 结果融合最容易出四类问题。

第一类是**任务边界重叠导致重复**。比如任务 A 和任务 B 都检索到了同一段内容，如果直接拼接文本，用户就会看到重复的答案。

第二类是**输出粒度不同导致层次混乱**。有的 Worker 返回的是原始 chunk，有的返回的是总结，有的返回的是具体操作步骤，直接混在一起会让答案结构很乱。

第三类是**证据可信度不同**。有的是检索到的原文，有的是模型根据原文推测的，如果不做区分，就会把推测和事实等权处理，降低答案的可信度。

第四类是**部分失败或结果冲突**。比如某个子任务失败了，或者两个子任务的结果相互矛盾，这时候不能简单忽略或者强行投票，需要明确告诉用户哪部分没有覆盖到。

所以我的原则不是简单按照 Worker 完成顺序拼接文本，而是**先保留任务身份、目标、结果、状态、工具记录、观察结果和失败原因，再统一合成**。

在当前的实现里，`SupervisorService` 会把子任务解析成带有 `label`、`description`、`goal`、`successCriteria`、`dependsOn` 和 `executionMode` 的 `SubTask`。每个 Worker 使用独立的 `AgentState`，记录 `toolHistory`（工具历史）、`observations`（观察结果）、`artifacts`（产物）、失败动作和证据级别。`AgentLoop` 执行工具之后会通过 `GoalEvaluator` 判断目标是否达成，并把成功、失败、反思和重规划写入事件和 trace。

合成的时候应该优先使用检索结果和工具返回的内容；对于没有证据的子任务，明确标记为未覆盖；对于冲突的内容，标注来源和不确定性，而不是强行投票选一个。

**可能追问：**

- **怎么避免重复内容？**：可以根据 chunk ID 或者内容的 hash 去重，合成的时候检查是否已经包含了相同的证据。
    
- **如果两个子任务结果冲突怎么办？**：不要强行投票或者只选一个。应该把两个结果都展示出来，标注它们的来源和不确定性，让用户自己判断，或者由合成模型明确说明"存在冲突，需要进一步验证"。
    
- **怎么区分事实和推测？**：检索到的原文可以标记为"检索证据"，模型根据证据推理出来的内容标记为"推测"或"分析"，两者的可信度不同。
    

**补充细节：**

你可以这样理解融合过程：

```text
错误的融合方式：
Worker A 结果 + Worker B 结果 + Worker C 结果
直接拼文本 → 重复、层次乱、无法溯源

正确的融合方式：
Worker A → { 任务标签、目标、结果、证据、状态 }
Worker B → { 任务标签、目标、结果、证据、状态 }
Worker C → { 任务标签、目标、结果、证据、状态 }
         ↓
    统一合成器
         ↓
   - 去重（根据 chunk ID 或内容 hash）
   - 按证据级别排序（检索证据 > 推测）
   - 标记未覆盖部分
   - 标注冲突和不确定性
         ↓
    最终答案
```

顺序任务只传递必要的压缩结果；并行任务只共享必要输入，避免上下文污染。

当前实现已经有了状态、工具结果评估和任务事件记录，但结果是否按照 chunk ID、来源和置信度形成统一的证据对象，不能表述为完整的生产能力。生产化建议让每个结果结构化地携带 chunk ID、来源文档、证据级别和置信度，由合成器基于证据对象生成最终答案，减少长文本拼接造成的重复和幻觉。

验证应该使用带标准证据的复杂问题集，统计子任务覆盖率、重复率、冲突率、Faithfulness（忠实度）和整体延迟，不能只看并行之后是否变快。

**可能追问：**

- **部分任务失败怎么办？**：保留已成功结果，明确列出失败或未覆盖部分，不让模型补齐不存在的证据。
- **为什么不直接拼接结果？**：不同结果的粒度、来源和可信度不同，直接拼接会重复、冲突且难以追溯。
- **当前是否已有完整证据融合？**：当前有任务状态、工具记录、观察和证据级别；统一 chunk 证据对象及置信度融合属于生产化建议。

### 14. Agent 什么时候触发反思和重规划？

反思和重规划主要发生在 Agent 执行过程中遇到问题的时候。

Supervisor 的规划是在任务开始之前做的，主要是根据用户问题和初始上下文决定是否拆分、怎么拆分。而反思和重规划发生在执行过程中，输入是当前的 `AgentState`、目标、工具历史、失败的动作以及观察结果，目标是分析为什么没有推进，并改变后续的执行策略。

在我的实现里，当 Agent 连续两轮都没有新的工具调用、没有获得有效证据、或者目标没有推进的时候，就会触发 `ReflectionService`。反思的结果不是直接给出最终答案，而是一个问题描述和建议策略，比如"工具参数可能错误""关键词过窄""应该换一个搜索工具"或者"需要补充用户信息"。

拿到反思结果之后，`ReplanningService` 会根据这个建议更新待执行的步骤，然后再进入下一轮循环。反思结果也会写入 trace，这样可以知道在哪一轮触发了反思、原因是什么、采用了什么替代策略。

不过要注意，反思和重规划也不能变成无限重试。它们仍然受到最大轮次、时间、Token 和工具调用次数的限制。如果新的策略还是没有进展，就会终止并说明原因。

**可能追问：**

- **反思和 Supervisor 规划有什么区别？**：Supervisor 是任务开始前的规划，主要决定要不要拆分、怎么拆分；反思是执行过程中的策略调整，主要分析为什么卡住了、应该怎么改变后续动作。
    
- **什么情况下会触发反思？**：连续两轮没有新的工具调用、没有获得有效证据、或者目标一直没有推进的时候。具体的触发条件可以根据任务类型调整。
    
- **反思之后一定能成功吗？**：不一定。反思只是尝试换一个策略，如果新策略还是没用，或者已经达到了预算上限，任务就会终止并记录失败原因。
    

**补充细节：**

你可以这样理解两者的区别：

```text
Supervisor 规划（事前）：
用户问题 + 初始上下文
   ↓
判断要不要拆分
   ↓
生成子任务结构
   ↓
开始执行

反思和重规划（事中）：
Agent 执行卡住了
   ↓
分析 AgentState、工具历史、观察结果
   ↓
识别问题：参数错误？关键词不对？工具不合适？
   ↓
生成建议策略
   ↓
更新待执行步骤
   ↓
继续执行
```

反思机制的关键是：

> **不是简单重试，而是分析为什么失败，然后尝试不同的策略。**

但面试时要主动说明：当前的实现更准确地称为**受约束的动态任务执行**，不是模型可以无限自我改写目标的开放式智能体。

### 15. Agent 如何控制预算？

只限制一个维度是不够的，需要组合多种预算控制手段。

如果只限制轮次，某一轮可能输出很长的内容或者工具结果很大；如果只限制 Token，工具可能会反复调用外部系统；如果只限制时间，模型调用次数仍然可能很多；如果只限制工具调用数量，又无法阻止模型在工具调用之间反复生成内容。所以需要组合预算。

在项目里，我的预算控制包括这几个维度：**最大迭代轮次、最大执行时间、最大 Token 和最大工具调用次数**。

每轮开始之前，系统会读取当前状态，累计已经消耗的 Token、工具调用历史和开始时间，检查是否超过了阈值。如果超过了，就返回明确的预算超限原因，记录到任务和 trace 里，不再继续调用模型。

预算配置还可以按任务类型区分。比如检索问答和生成产物这两种任务，预算可能不一样。不过动态的用户级策略属于后续的生产化方向，当前还没有实现。

另外，预算控制还要和并发控制配合。单个任务不超限，不代表全局不会被大量并发任务拖垮。所以生产环境还应该增加租户并发限制、全局模型并发限制和队列长度限制。

**可能追问：**

- **预算超限了会怎样？**：任务会终止，并记录预算超限的原因（比如超过最大轮次、超过最大 Token 或超过最大时间）。前端会收到一个明确的超限提示，用户可以看到任务执行到哪里、为什么停止了。
    
- **不同任务的预算怎么设置？**：可以按任务类型区分。比如简单的检索问答可能设置 5 轮、3000 Token；复杂的生成任务可能设置 10 轮、8000 Token。具体阈值需要根据固定测试集和历史数据调整。
    
- **怎么验证预算设置是否合理？**：主要看预算超限率、平均 Token、任务成功率、超时率和单位任务成本。如果超限率太高，说明预算太紧；如果平均 Token 远低于上限，说明预算太松。阈值不能只往上调，要找到可接受的折中。
    

**补充细节：**

你可以这样理解组合预算：

```text
单一预算的问题：
只限轮次 → 某轮输出很长
只限 Token → 工具反复调用
只限时间 → 模型调用很多次
只限工具数 → 生成内容不受控

组合预算：
┌─────────────────┐
│ 最大轮次：10     │
│ 最大 Token：8000 │
│ 最大时间：60s    │
│ 最大工具数：20   │
└─────────────────┘
任意一个超限 → 终止
```

预算检查流程：

```text
每轮开始前
   ↓
读取当前状态
   ↓
累计 Token、工具历史、执行时间
   ↓
检查是否超过阈值
   ↓
超过 → 终止并记录原因
   ↓
未超过 → 继续执行
```

验证指标不能只看一个任务，要看全局：

- **预算超限率**：多少比例的任务因为预算超限而终止
- **平均 Token**：平均每个任务消耗多少 Token
- **任务成功率**：预算限制下任务能否完成
- **单位成本**：平均每个任务的成本
- **P95/P99 延迟**：高分位延迟是否在可接受范围

通过这些指标找到合适的预算阈值，而不是凭感觉设置。

## 三、混合检索与 RAG 链路

### 16. 什么是 RRF？

RRF（Reciprocal Rank Fusion）是一种排名融合策略，用排名而不是原始分数来合并多路检索结果。

因为向量检索返回的相似度通常在 0-1 之间，BM25 分数可能几十上百，直接加权平均需要手动归一化和调权重，而且分数分布可能随数据变化。RRF 把每个候选在各路中的排名转换成 `1 / (k + rank + 1)`，然后累加。这样高排名候选的累加值更大，而且不需要归一化。

项目里用 chunk ID、doc ID 或 note ID 去重，确保同一内容在不同路径中只计算一次。RRF_K 是可调参数，k 越大，排名差异对累加值的影响越小；项目默认 k=60，可以通过消融实验对比其他值。

RRF 适合合并来源分数分布不同的多路检索，比直接加权平均分数更稳定。但 RRF 也有局限：如果某路召回质量很差，垃圾排名仍会得到不小的 RRF 加成，可能污染最终 TopK；如果某路异常返回空，RRF 本身不做降级，需要外层逻辑兜底。

是否使用 RRF、候选扩大倍数、RRF_K 等参数都需要消融实验找平衡。不要迷信 RRF，它本质是一个排名聚合策略，不解决召回本身的质量问题。

**可能追问：**

- **为什么不用加权平均？**：向量相似度 0-1、BM25 分数可能几十上百，量纲不同。如果要加权平均，就要先归一化，还要手动调权重。RRF 把分数抽象成排名，不需要归一化，而且对分数分布变化更鲁棒。
    
- **RRF_K 怎么选？**：k 控制尾部惩罚幅度。k 越大，排名差异对累加值影响越小；k 越小，高排名和低排名的差距越明显。项目默认 k=60，可以消融对比其他值，看最终召回和答案质量。
    
- **多路都返回空怎么办？**：RRF 无结果可融合，外层逻辑走关键词 fallback。降级策略由 HybridRetriever 控制，不是 RRF 职责。
    

**补充细节：**

RRF 计算示例（假设 k=60）：

```text
chunk A：
  向量检索排名第 2 → 1/(60+2+1) = 0.0159
  BM25 排名第 5    → 1/(60+5+1) = 0.0152
  RRF 总分 = 0.0159 + 0.0152 = 0.0311

chunk B：
  向量检索排名第 1 → 1/(60+1+1) = 0.0161
  BM25 排名第 10   → 1/(60+10+1) = 0.0141
  RRF 总分 = 0.0161 + 0.0141 = 0.0302

chunk A 总分更高 → 排在前面
```

RRF 不关心原始分数，只关心每路给的相对排名。如果某 chunk 在两路都排名靠前，RRF 就会给它更高的总分。

### 17. 什么是 Rerank？

Rerank 是在融合后的候选池上再做一次精排，用更强的模型把语义不相关的候选往下压。

前序召回阶段的目标是高召回，候选池可能包含一些语义相关性不高的结果。Rerank 模型通常是专门训练的排序模型，比向量相似度更能捕捉细粒度的语义相关性。把 query 和候选文本一起发给 rerank 模型，模型返回每个候选的相关性分数，然后重新排序，截取最终 TopK。

项目里可选 cohere、jina 或其他 rerank 模型。开启 rerank 需要一次额外调用和网络往返，会增加延迟和 Token 消耗，但能提高精度。是否使用 rerank 要看业务对延迟和质量的取舍，通过消融实验对比有无 rerank 的答案忠实度和召回覆盖。

Rerank 模型本身也有局限：它只能对候选池重新排序，不能挽救之前召回阶段遗漏的内容。如果前序召回质量差，rerank 也无力回天。所以 rerank 是在召回质量已经不错的基础上做精排，而不是用来弥补召回缺陷的。

Rerank 适合候选池较大、语义噪音较多、对精度要求高、能容忍增加的延迟的场景。不要盲目打开 rerank，它不是万能开关。

**可能追问：**

- **Rerank 用什么模型？**：项目支持 cohere、jina 等。具体选哪个要看延迟、成本和质量平衡，通常通过对比实验决定。不同 rerank 模型的延迟、成本和精度都不一样，要根据业务需求选择。
    
- **Rerank 延迟多少？**：取决于候选数、模型和网络。常见 100-500ms。候选越多、模型越大，延迟越高。如果候选池有几十个文档，rerank 延迟可能比召回本身还高。
    
- **什么时候不用 Rerank？**：延迟敏感、候选池已经很干净、成本受限、或者消融实验发现 rerank 收益不明显时，可以关闭。Rerank 不是必选项，要根据实际效果决定。
    

**补充细节：**

Rerank 流程示意：

```text
融合后得到候选列表
   ↓
把 query 和候选文本一起发给 rerank 模型
   ↓
模型返回每个候选的相关性分数
   ↓
按相关性分数重新排序
   ↓
截取最终 TopK
```

Rerank 不改变候选内容，只改变排序。Rerank 模型的输入是 query 和候选文本对，输出是相关性分数，通常是 0-1 的浮点数。

Rerank 和向量检索的区别：

```text
向量检索：
- 用 embedding 计算相似度
- 基于向量空间的几何距离
- 速度快，但语义粒度粗

Rerank：
- 用排序模型直接打分
- 基于训练好的语义相关性模型
- 速度慢，但语义粒度细
```

Rerank 是在召回之后做的，所以前序召回阶段要保证候选池覆盖足够广，rerank 才能发挥作用。

### 18. Query 扩展怎么做？

Query 扩展是把用户的原始问题改写成多个相关问题，用来扩大检索覆盖范围。

用户问题有时候很短或者很口语化，比如"502 怎么办"，如果直接用这个问题去检索，可能召回不全。Query 扩展会把它改写成"502 错误如何排查""Bad Gateway 原因分析""网关超时处理方法"等多个相关问题，然后分别检索，扩大候选范围。

项目里用 LLM 做改写，通过 Prompt 要求模型生成 2-3 个语义相近但表达不同的问题。改写之后，每个 query 分别走向量和 BM25，最后融合去重。

但 Query 扩展也不是必选项。扩展会增加 LLM 调用、检索次数、Token 消耗和延迟。如果扩展出来的问题质量不高，可能引入噪音，反而降低精度。所以要通过消融实验验证是否有收益。

Query 扩展适合用户问题表达不清晰、关键词覆盖不全、需要提高召回覆盖的场景。如果用户问题已经很明确，或者对延迟很敏感，可以关闭扩展。

**可能追问：**

- **扩展多少个 query 合适？**：项目默认 2-3 个。太少覆盖不够，太多会引入噪音、增加延迟和 Token。具体数量要根据业务需求和消融实验结果调整。
    
- **怎么保证扩展质量？**：Prompt 里要求模型生成语义相近但表达不同的问题，避免生成完全无关的内容。可以加一些约束，比如"保持原问题的核心意图""不要生成反义问题"。
    
- **扩展之后检索结果怎么融合？**：每个 query 分别检索，然后按照 RRF 融合。去重的时候用 chunk ID、doc ID 或 note ID，避免同一内容被重复计算。
    

**补充细节：**

Query 扩展流程：

```text
用户原始问题
   ↓
LLM 改写成 2-3 个相关问题
   ↓
每个 query 分别检索
   ↓
融合去重
   ↓
最终候选池
```

Query 扩展的成本：

```text
额外的 LLM 调用：
- 改写本身需要一次 LLM 调用
- 输入和输出都消耗 Token

额外的检索调用：
- 原来检索 1 次，扩展后检索 3 次
- 向量检索和 BM25 都要重复调用

额外的延迟：
- LLM 改写延迟 + 额外检索延迟
```

所以 Query 扩展要权衡收益和成本，不能盲目打开。

### 19. Agent 工具调用如何避免越权？

避免越权不能只依赖 Prompt，必须在服务端做严格校验。

虽然可以在 Prompt 里告诉模型"不要访问其他用户的资源"，但模型输出是不稳定的，不能把安全完全寄托在 Prompt 上。所以服务端必须校验参数格式、资源归属和当前用户权限。

对于检索工具，`userId`、`spaceId`、`documentId` 这些参数不是完全相信模型传入的值，而是由服务端从会话中提取，或者在工具执行前二次查库校验归属关系。对于写操作、删除操作和外部发布这类高风险操作，我会增加确认或审批流程。

在项目里，工具白名单在 `AgentPolicyService` 中定义。每次调用工具之前，系统会检查工具名是否在白名单里、参数是否完整、资源是否有权限访问、操作类型是否允许。对于高风险的写操作，系统会先返回一个待确认状态，等用户确认之后才真正执行。所有的工具调用和结果都会记录到 trace 里，便于后续审计。

多租户 RAG 系统最危险的不是模型答错了，而是模型通过参数读取了其他用户的文档。所以检索工具会从 Session 里提取 userId，笔记和知识库工具会二次查库验证归属关系，不接受模型直接传入的用户标识。

**可能追问：**

- **工具白名单如何维护？**：当前是按任务类型配置可用工具列表，不在列表里的工具名会被直接拒绝并记录。生产化的话应该支持租户级白名单和运行时动态调整。
    
- **参数校验具体做哪些？**：非空检查、长度检查、格式检查（比如 documentId 是否是合法的 UUID）、资源存在性检查、归属关系检查和权限检查。复杂的参数可以用 schema 校验。
    
- **审计日志记录什么？**：工具名、调用时间、参数摘要（脱敏后的）、执行结果、错误原因和关联的 trace ID。不记录完整的敏感参数，比如完整的文档内容或者用户密码。
    

**补充细节：**

工具越权风险可以分成三类：

```text
读越权：
读取其他用户的文档、笔记、知识库

写越权：
修改或删除其他用户的资源

执行越权：
调用未授权的工具（比如删除、发布）
```

防护需要分层：

```text
第一层：工具白名单
拦截不在列表里的工具名

第二层：参数校验
拦截格式错误的参数

第三层：权限校验
拦截归属不符的资源访问

第四层：二次确认
拦截高风险操作
```

验证方法：

- 构造模型返回其他用户 ID 或资源 ID 的工具调用
- 断言被拒绝并记录审计日志
- 未授权的工具名应该返回明确错误，不执行任何逻辑

核心原则：

> **不信任模型传入的任何用户标识或资源标识，所有权限都由服务端基于 Session 和数据库校验决定。**


### 20. Agent 重试为什么不能无限重试？

无限重试会把一次错误变成模型、线程和外部服务的持续消耗，而且可能重复执行写操作。

如果某个工具调用失败了，无限重试的话，模型会一直消耗 Token、线程会一直被占用、外部服务（比如检索、数据库）会一直被请求。而且对于带副作用的工具，比如创建笔记、发送消息，重复执行可能会创建多条重复记录。

所以在项目里，`AgentLoop` 会通过多个维度限制循环：**最大轮次、最大工具调用次数、最大 Token、运行时间、连续失败次数和相同工具调用次数**。如果检测到相同的失败参数，会被拦截不再重试。比如抓取网页这个步骤连续失败达到 3 次，就会标记失败并终止这个步骤。

另外，不是所有错误都应该重试。网络超时、连接重置这类临时错误可以有限重试，并且使用指数退避（第一次等 1 秒、第二次等 2 秒、第三次等 4 秒）。但是参数错误、权限不足、资源不存在这类确定性错误，通常不应该原样重试，因为重试也不会成功。

对于带副作用的工具，必须保证幂等性。比如创建笔记的时候用一个稳定的业务键，重试的时候先检查是否已经创建过，避免重复创建。

每次重试都会记录 attempt（第几次尝试）、错误类型和耗时，并且受总时间和 Token 预算约束。达到上限之后，任务会进入失败、人工处理或者降级路径，保证任务一定能结束。

**可能追问：**

- **项目里怎么防止重复调用？**：拦截相同的失败参数和上一轮完全相同的动作，并且受 `maxSameToolCalls`、轮次和工具预算的限制。
    
- **为什么不是失败就换工具？**：要先判断错误是否可恢复。如果是权限不足、参数错误或者资源不存在，换工具也解决不了问题。
    
- **指数退避怎么实现？**：对于临时错误，重试间隔递增（比如 1 秒、2 秒、4 秒），避免瞬间把下游服务压垮。对于确定性错误，直接失败不退避。
    

**补充细节：**

重试策略分类：

```text
临时错误（可重试）：
- 网络超时
- 连接重置
- 服务暂时不可用
→ 指数退避重试，最多 3 次

确定性错误（不重试）：
- 参数格式错误
- 权限不足
- 资源不存在
→ 直接失败并记录原因

幂等性要求：
- 创建操作：用业务键去重
- 删除操作：先检查是否存在
- 修改操作：用版本号或时间戳防并发
```

重试计数示例：

```text
第 1 次调用失败（网络超时）
  ↓ 等待 1 秒
第 2 次调用失败（网络超时）
  ↓ 等待 2 秒
第 3 次调用失败（网络超时）
  ↓ 等待 4 秒
第 4 次调用失败
  ↓
达到重试上限，标记失败
```

核心原则：

> **有限重试 + 指数退避 + 幂等保证 + 明确终止条件。**

### 21. 为什么用 MySQL + Redis + Lucene + Chroma 四层存储？

因为不同的查询场景对数据结构和查询性能的要求不同，单一存储无法同时满足所有需求。

MySQL 是主存储，用来存结构化的笔记、文档、用户、会话和任务信息，支持事务、关联查询和持久化。但 MySQL 不擅长全文检索和向量相似度计算。

Lucene（通过内存索引）用来做 BM25 关键词检索。它能快速找到包含特定关键词的文档或 chunk，适合精确匹配和短语搜索。但 Lucene 不支持语义相似度计算。

Chroma 是向量数据库，用来存 embedding 并做相似度检索。它能找到语义相关的内容，即使关键词不完全匹配。但 Chroma 不适合做精确关键词匹配。

Redis 用来做缓存和会话状态存储。热点数据、LRU 缓存、分布式锁和实时计数都放在 Redis 里，减少数据库压力。但 Redis 是内存存储，不适合存大量持久化数据。

所以这四层存储各司其职：MySQL 做主存储和事务、Lucene 做关键词检索、Chroma 做语义检索、Redis 做缓存和实时状态。数据在写入的时候会同步到不同的存储，查询的时候根据场景选择合适的存储路径。

**可能追问：**

- **数据同步怎么保证一致性？**：写入 MySQL 之后异步同步到 Lucene 和 Chroma。如果同步失败，会记录到失败队列并重试。最终一致性可以接受，但 MySQL 是唯一的主存储和真实来源。
    
- **为什么不用 Elasticsearch 替代 Lucene？**：Elasticsearch 更重，需要独立部署和维护。项目用内存 Lucene 索引更轻量，适合中小规模数据。如果数据量很大，可以考虑迁移到 Elasticsearch。
    
- **Redis 挂了怎么办？**：缓存失效，请求会直接打到 MySQL。性能会下降，但功能不受影响。Redis 应该有持久化和主从备份，保证可用性。
    

**补充细节：**

四层存储的职责：

```text
MySQL：
- 笔记、文档、用户、会话、任务
- 事务和关联查询
- 主存储和真实来源

Lucene：
- BM25 关键词检索
- 精确匹配和短语搜索
- 内存索引，轻量快速

Chroma：
- 向量相似度检索
- 语义相关查询
- Embedding 存储

Redis：
- 热点数据缓存
- 会话状态
- 分布式锁和计数
```

数据流示意：

```text
写入流程：
用户创建笔记
   ↓
写入 MySQL（主存储）
   ↓
异步同步到 Lucene（关键词索引）
   ↓
异步同步到 Chroma（向量索引）
   ↓
热点数据写入 Redis（缓存）

查询流程：
用户问题
   ↓
先查 Redis 缓存
   ↓ 未命中
根据查询类型选择：
├─ 关键词检索 → Lucene
├─ 语义检索 → Chroma
└─ 结构化查询 → MySQL
```

核心原则：

> **MySQL 是真实来源，其他存储是派生视图。一致性要求：最终一致性，MySQL 优先。**

### 22. 向量和 BM25 索引怎么保持和数据库一致？

通过异步同步机制保持最终一致性，MySQL 是唯一的真实来源。

当用户创建或修改笔记、文档的时候，先写入 MySQL，事务提交成功之后，再异步同步到 Lucene 和 Chroma。如果同步失败，会记录到失败队列，后台定期重试。

项目里用的是事件驱动的同步方式。写入 MySQL 之后发布一个同步事件，监听器收到事件之后分别调用 Lucene 索引服务和 Chroma 向量服务。Lucene 索引是内存的，重启会重建；Chroma 是持久化的，但也支持重建。

因为是异步同步，短时间内可能出现不一致：MySQL 已经写入了，但 Lucene 和 Chroma 还没有同步完成。这时候用户可能搜不到刚创建的内容。这是最终一致性的代价，但可以通过减少同步延迟来缓解。

如果索引损坏或者长期不一致，可以用全量重建任务。全量重建会从 MySQL 读取所有数据，重新构建 Lucene 和 Chroma 索引。重建期间查询仍然可以走旧索引，重建完成之后切换。

删除操作也是一样：先从 MySQL 删除，再异步从 Lucene 和 Chroma 删除。如果删除失败，重试或者等下次全量重建清理。

**可能追问：**

- **同步延迟多久？**：正常情况下几百毫秒到几秒。如果队列积压或者服务异常，可能更长。可以通过监控同步延迟和失败率来发现问题。
    
- **用户刚创建的内容搜不到怎么办？**：可以在前端加一个"正在同步"的提示，或者在创建之后等待几秒再刷新列表。也可以在检索之前查一下 MySQL，把最新的内容直接加到候选池里。
    
- **全量重建会影响线上吗？**：重建期间查询走旧索引，不影响可用性。重建完成之后切换到新索引。但重建会消耗 CPU、内存和 LLM 调用（生成 embedding），要在低峰期执行。
    

**补充细节：**

同步流程示意：

```text
用户创建笔记
   ↓
写入 MySQL 事务提交
   ↓
发布同步事件
   ↓
监听器收到事件
   ↓
并行同步：
├─ 写入 Lucene 索引
└─ 写入 Chroma 向量库
   ↓
同步成功 → 完成
同步失败 → 记录失败队列 → 后台重试
```

一致性保证：

```text
强一致性（不支持）：
写入 MySQL 的同时同步写入 Lucene 和 Chroma
→ 会增加写入延迟和失败率

最终一致性（当前实现）：
MySQL 先写入，异步同步到 Lucene 和 Chroma
→ 短时间不一致，但最终收敛

降级策略：
索引不可用 → 查 MySQL 关键词 fallback
索引长期不一致 → 全量重建
```

核心原则：

> **MySQL 是唯一真实来源，索引是派生视图。接受最终一致性，不追求强一致性。**

### 23. 为什么 Agent 状态用最终一致性？

因为 Agent 执行是异步的，强一致性会严重影响性能和可用性。

如果要求强一致性，每次状态变更都要等所有副本确认，这会大幅增加延迟。而且如果某个副本不可用，整个任务就会被阻塞。

Agent 的状态更新频率很高：每轮迭代都会更新工具历史、Token 消耗、观察结果和状态摘要。如果每次更新都要同步等待，延迟会很明显。

所以项目里用最终一致性：状态先写入主存储（MySQL），然后异步同步到 Redis 缓存和前端 SSE 推送。前端收到的 SSE 事件只是实时通知，不保证和数据库完全同步。如果用户刷新页面或者 SSE 断线，前端会从数据库重新查询最新状态。

这样设计的好处是：写入延迟低、不会因为副本故障阻塞任务、可以异步推送给多个订阅者。代价是短时间内可能看到旧状态，但最终会收敛到最新状态。

对于关键决策（比如任务是否完成、是否超预算），仍然以数据库为准，不依赖缓存或 SSE 事件。

**可能追问：**

- **SSE 断线怎么办？**：前端重连之后从数据库查询最新状态，不依赖断线期间错过的事件。SSE 只是实时通知，不是状态的唯一来源。
    
- **Redis 缓存和数据库不一致怎么办？**：缓存用来加速查询，但关键决策仍然查数据库。缓存不一致可以通过 TTL 过期或者主动失效来解决。
    
- **为什么不用分布式事务？**：分布式事务会增加复杂度和延迟，而且 Agent 状态更新频率很高，用分布式事务得不偿失。最终一致性对 Agent 场景足够了。
    

**补充细节：**

强一致性 vs 最终一致性：

```text
强一致性：
写入 → 等待所有副本确认 → 返回成功
优点：读到的一定是最新数据
缺点：延迟高、可用性低

最终一致性：
写入主存储 → 立即返回 → 异步同步副本
优点：延迟低、可用性高
缺点：短时间可能读到旧数据
```

Agent 状态更新频率示意：

```text
Agent 执行 1 次任务：
├─ 每轮迭代更新状态：10-20 次
├─ 每次工具调用更新历史：5-15 次
└─ 每次 LLM 调用更新 Token：10-20 次

总更新次数：30-50 次/任务

如果每次都强一致 → 总延迟增加数秒
最终一致性 → 总延迟增加几十毫秒
```

核心原则：

> **高频状态更新用最终一致性，关键决策查主存储。**

### 24. 会话状态为什么放 Redis？

因为会话状态需要高性能读写、支持过期和分布式访问。

会话状态包括用户的登录信息、当前对话上下文、临时缓存的检索结果和正在执行的任务 ID。这些状态的读写频率很高，而且需要在多个服务实例之间共享。

MySQL 虽然可以存会话，但读写延迟比 Redis 高，而且会增加数据库压力。Redis 是内存存储，读写延迟只有几毫秒，而且支持 TTL 自动过期，不需要手动清理过期会话。

项目里用 Redis 存 JWT Token 的黑名单、用户的当前会话 ID、SSE 连接标识和正在执行的任务状态。这些数据都有时效性，过期之后自动删除，不需要持久化。

如果 Redis 挂了，会话会失效，用户需要重新登录。但这不会影响已经持久化到 MySQL 的数据，只是临时状态丢失。所以 Redis 应该有持久化（RDB 或 AOF）和主从备份，保证可用性。

**可能追问：**

- **为什么不全部放 MySQL？**：MySQL 读写延迟比 Redis 高，而且会话状态读写频率很高，会增加数据库压力。Redis 更适合高频临时状态。
    
- **Redis 挂了用户数据会丢吗？**：会话状态会丢，用户需要重新登录。但 MySQL 里的笔记、文档、任务记录不会丢。Redis 应该有持久化和备份，保证可用性。
    
- **会话过期时间怎么设置？**：项目里默认 24 小时。可以根据业务需求调整。过期之后用户需要重新登录，会话状态会被清理。
    

**补充细节：**

Redis 存储的会话状态：

```text
用户会话：
- JWT Token 黑名单
- 当前会话 ID
- 用户 ID 和权限信息
- 临时检索缓存

任务状态：
- 正在执行的任务 ID
- SSE 连接标识
- 实时进度缓存
```

Redis vs MySQL 对比：

```text
Redis：
- 内存存储，延迟低（1-5ms）
- 支持 TTL 自动过期
- 适合高频临时状态
- 数据可能丢失（需要持久化）

MySQL：
- 磁盘存储，延迟高（10-50ms）
- 需要手动清理过期数据
- 适合持久化数据
- 数据不会丢失（事务保证）
```

核心原则：

> **高频临时状态放 Redis，持久化数据放 MySQL。**

### 25. 为什么向量检索和 BM25 要结合？

因为研发问题既有语义表达，也有精确术语，单一检索方式覆盖不全。

研发知识库里的问题，有些是”怎么处理””如何解决”这类语义表达，有些是错误码、类名、接口名、版本号这类必须精确命中的术语。如果只用向量检索，异常码、接口名这类精确匹配会失分；如果只用 BM25，同义表达和口语化问题又召回不好。

所以在项目里，`HybridRetriever` 会按照配置分别走 Chroma 向量检索和 Lucene BM25，然后用 RRF 按排名融合、去重，最后可以选择是否用 rerank。这样先扩大候选覆盖，再做精排。

向量检索擅长语义相似和同义改写，BM25 擅长异常名、类名、版本号、接口名等精确匹配。研发知识库同时存在这两类问题，只用一种检索会在另一类问题上失分。所以项目采用双路召回，再融合和精排。

混合检索的目标是提高候选覆盖率，但最终效果要用固定测试集和指标验证，不能只凭几次主观体验就下结论。

**可能追问：**

- **为什么不直接把两个分数加起来？**：BM25 分数和向量相似度的分布可能完全不同，直接相加需要额外的归一化和权重调参。RRF 只比较排名，不需要归一化。
    
- **怎么证明混合检索有效？**：用固定的数据集和配置做单路基线、逐项消融实验，比较召回率、答案质量、延迟、Token、空结果率和 fallback 率。
    
- **为什么不能只用向量检索？**：研发知识里有很多异常码、类名、接口名和版本号，这些内容对精确匹配比较敏感，向量检索不能完全替代 BM25。
    

**补充细节：**

你可以这样理解两种检索的优势：

```text
BM25（关键词检索）：
擅长：异常码、类名、接口名、版本号
不擅长：同义表达、口语化问题

向量检索：
擅长：语义相似、同义改写
不擅长：精确术语匹配

混合检索：
先用两路各自召回 → 扩大候选池
   ↓
RRF 融合去重 → 综合排名
   ↓
可选 Rerank → 精排
   ↓
最终 TopK
```

降级策略：

```text
向量路径不可用 → 关闭向量检索
BM25 异常 → 当前 query 使用 MySQL 关键词 fallback
全部没有 ranking → 走关键词检索
```

降级能保证基本可用，但不能说语义质量等价。这是已实现的检索组合和降级机制，不代表已经证明了固定收益。项目没有可公开的量化指标时，只能通过固定测试集和消融实验验证 Recall、Context Precision、忠实度、延迟和 Token。

### 26. 完整检索链路是什么？

完整的检索链路从读取配置开始，到返回最终 TopK 结束。

首先，系统会读取消融配置或者默认配置，确定 Query 扩展、向量检索、BM25、RRF、rerank、TopK 和 RRF_K 是否启用。如果 Chroma 不可用，就关闭向量路径；如果 BM25 不可用，就关闭 BM25。

如果打开了 Query 扩展，就把原始问题改写成多个语义相关的 query；否则只使用原始 query。

对每个 query，向量路径会调用 Chroma，候选数通常扩大到最终 TopK 的两倍；BM25 路也会查询扩大后的候选。如果 BM25 调用报错，不是直接让整个链路失败，而是对当前 query 使用 MySQL 关键词 fallback。

所有 ranking 收集之后，如果有多路并且启用了 RRF，就按照每个结果在各路中的排名计算 `1 / (rrfK + rank + 1)`，用 chunk ID、doc ID 或 note ID 去重并累加。如果 RRF 未启用，就走简单合并和相似度排序。

融合结果如果打开了 rerank，就交给 reranker 精排；没有 rerank 的话直接截取最终 TopK。如果向量不可用但 BM25 可用，就继续走 BM25；如果 BM25 也不可用或者没有任何 ranking，就用 MySQL 关键词检索。

关键词 fallback 能保住可用性，但语义召回质量会下降。所以应该在 trace 或日志里记录降级原因、路径和耗时。

这条链路的设计取舍是：**先高召回，再精排，故障逐层降级**。Query 扩展和 rerank 会增加 Token、延迟和失败面，所以要通过消融实验验证，而不是默认全部打开。

**可能追问：**

- **为什么候选要扩大到 TopK 两倍？**：先多召回再精排，避免单路遗漏。融合和 rerank 之后再截取最终 TopK，可以提高最终结果的质量。
    
- **降级路径怎么触发？**：检测到服务异常、超时或者返回空的时候自动降级。降级决策会记录到 trace，便于排查质量问题。
    
- **Query 扩展一定有用吗？**：不一定。扩展可能引入噪音、增加延迟和 Token。需要通过消融实验比较有无扩展的召回和答案质量。
    

**补充细节：**

完整检索链路示意图：

```text
用户 query
   ↓
Query 扩展（可选）→ query1, query2, query3
   ↓
并行检索
├─ 向量检索（Chroma）→ 候选 TopK×2
└─ BM25（Lucene）→ 候选 TopK×2
   ↓
RRF 融合去重
   ↓
Rerank 精排（可选）
   ↓
截取最终 TopK
   ↓
返回结果
```

降级层次：

```text
完整链路：向量 + BM25 + RRF + Rerank
   ↓
降级1：向量失败 → BM25 + Rerank
   ↓
降级2：BM25 失败 → MySQL 关键词
   ↓
降级3：全部失败 → 返回空并记录
```

检索链路每个阶段都可能失败，所以设计为逐层降级。降级不是质量等价，只是保底可用。每次降级都要记录原因、使用的路径和耗时，便于后续分析质量问题。

### 27. 为什么使用 RRF，而不是直接加权分数？

因为 BM25 分数和向量相似度的分布完全不同，直接加权需要先归一化，还要反复调权重，很麻烦。

BM25 的分数可能是几十、上百，向量相似度通常在 0-1 之间。如果要直接相加或加权平均，就得先把它们归一化到同一个范围，然后还要设置权重（比如 BM25 占 0.3、向量占 0.7）。但这个权重不好定，不同的数据集、不同的查询类型，最优权重可能都不一样。而且分数分布可能随着数据变化，今天调好的权重明天可能就不合适了。

RRF（Reciprocal Rank Fusion）不看原始分数，只看排名。每个候选在某一路检索中排名越靠前，它的 RRF 分数就越高。如果同一个 chunk 在向量检索里排第 2，在 BM25 里排第 5，它的 RRF 分数就是这两个排名的贡献之和。

项目里的实现是：排名从 0 开始，用公式 `1 / (rrfK + rank + 1)` 计算每一路的贡献，然后用 chunk ID 做 key 累加。这样同一个 chunk 在多路都排名靠前的话，累加分数就会更高。默认 rrfK 是 30，可以通过配置改。

RRF_K 这个参数控制排名差异的衰减速度。K 越大，排名 1 和排名 10 的差距就越小；K 越小，头部排名的影响就越强。具体选多少，要通过消融实验对比不同 K 值在固定测试集上的召回率、答案质量和候选分布。

RRF 只负责融合排序，不负责权限过滤、文档版本过滤这些。这些还是要在检索前后单独处理。

**可能追问：**

- **RRF_K 如何选择？**：通过消融实验比较不同 K 值（如 10、30、60）在固定测试集上的召回、答案质量和候选分布。当前默认 30 可通过配置覆盖。
    
- **RRF 会丢失原始分数信息吗？**：会。RRF 只保留排名信息。如果需要原始分数可以在融合前记录，但最终排序仍然用 RRF 分。
    
- **为什么不用学习排序（LTR）？**：LTR 需要大量标注样本和特征工程。当前规模下 RRF 足够简单稳定，而且不依赖训练数据。
    

**补充细节：**

RRF 公式：`score = sum(1 / (k + rank_i + 1))`，其中 rank_i 是该 chunk 在第 i 路检索中的排名（从 0 开始）。

去重依赖稳定 ID：chunk ID 最优，文档 ID 次之，内容 hash 兜底。没有稳定 ID 时可能把相同内容重复返回。

多路都命中的 chunk 得分更高，说明它在不同检索策略下都被认为相关。

### 28. Query 扩展有什么收益和风险？

收益是把用户的口语化问题改写成知识库里可能出现的专业表达，提高召回覆盖。风险是增加模型调用、Token 消耗，还可能引入噪音。

用户问"怎么搞定空指针"，知识库里可能写的是"如何处理 NullPointerException"。直接用原问题检索，可能匹配不到。Query 扩展会把原问题改写成 2-3 个相关表述，比如"如何处理空指针异常""NullPointerException 解决方法""空指针错误排查"，然后分别检索，理论上能提高召回的多样性。

但扩展也有代价。首先要调用一次 LLM 做改写，消耗 Token 和时间。其次，扩展出来的问题可能偏离原意，或者引入无关术语，导致召回的候选里噪音变多。最后，每个扩展出来的 query 都要走一遍完整检索，检索次数变多了，总延迟也会增加。

所以 Query 扩展不是越多越好。项目里默认扩展成 2-3 个，扩展结果会缓存到 Redis（TTL 10 分钟），避免重复调用模型。所有扩展 query 的检索结果进入 RRF 融合，理论上能过滤掉一部分噪音，但如果扩展质量不好，后续 rerank 可以部分补救。

评估 Query 扩展不能只看召回数量变多了，要看 Context Recall（证据覆盖率）、Context Precision（噪音比例）、最终答案质量、延迟和 Token 成本。如果召回变多但噪音也变多，不一定是优化。

**可能追问：**

- **扩展后召回变多就是好事吗？**：不一定。召回数量增加但相关性下降时，反而增加模型上下文噪音，影响最终答案质量。
    
- **扩展结果如何缓存？**：用原始 query 的 hash 作为 key，扩展结果存 Redis，TTL 10 分钟。缓存失效或未命中时重新调用模型。
    
- **什么场景适合 Query 扩展？**：用户问题口语化、同义词多、知识库术语标准化的场景。精确查询（如错误码）扩展可能引入噪音。
    

**补充细节：**

Query 扩展属于召回增强策略，不是精排策略。扩展后的多个 query 都会走完整检索链路，结果在 RRF 阶段融合。

验证方法：固定测试集分别跑无扩展、扩展 2 条、扩展 5 条，比较 Context Recall、Context Precision、最终答案质量、延迟和 Token 消耗。

### 29. 为什么要保留关键词降级？

因为向量库和 BM25 都可能暂时不可用，关键词检索虽然能力较弱，但可以保住基本可用性。

Chroma 是独立的向量数据库服务，可能因为网络、重启、资源不足等原因暂时不可用。Lucene BM25 是内存索引，进程重启就要重建。如果两者都不可用，完全不返回结果的话，用户体验很差。

关键词检索走 MySQL 的 `LIKE %keyword%` 或全文索引，虽然无法理解同义表达和语义相似，但能匹配文档标题、chunk 内容中的精确字符串。对于"某某接口""某某异常"这类包含明确术语的问题，仍然能返回部分结果。

项目里的降级路径是：Chroma 不可用时关闭向量检索，继续使用 BM25；BM25 也异常时对当前 query 使用 MySQL 关键词 fallback；全部失败返回空结果并记录降级路径。

降级必须显式记录到 trace：使用了哪一路、为什么降级、结果数量和耗时。否则用户看到答案，却不知道答案来自完整 RAG 还是低质量 fallback，后续也无法分析质量下降。

这是可用性优先的设计，不是质量等价的替代方案。降级之后语义召回能力会明显下降，但至少能保证系统不完全不可用。

**可能追问：**

- **关键词检索为什么不能替代向量？**：关键词只能精确匹配字符串，无法理解"如何解决"和"怎么处理"是同义表达，也无法召回语义相关但用词不同的文档。
    
- **降级后用户会感知吗？**：如果前端展示降级提示，用户可以感知。后端至少要在 trace 和日志中记录，便于排查答案质量问题。
    
- **MySQL 关键词检索性能如何？**：`LIKE %keyword%` 无法使用索引，大表扫描慢。全文索引（FULLTEXT）性能较好但功能受限。这是保底方案，不适合作为主检索路径。
    

**补充细节：**

降级路径优先级：

```text
向量 + BM25（最优）
   ↓ 向量不可用
仅 BM25
   ↓ BM25 也不可用
MySQL 关键词
   ↓ 全部失败
返回空并记录
```

每级降级都要记录到 trace，包含降级原因、实际使用路径、返回结果数和耗时。

验证方法：故障注入，分别关闭 Chroma、BM25 和两者都关闭，断言系统仍能返回结果（即使质量下降）并正确记录降级信息。

### 30. rerank 为什么放在召回之后？

因为 rerank 通常比 BM25 或向量查询更贵，适合在候选集已经缩小后使用。

Rerank 模型要把 query 和每个候选文本一起输入，计算它们的语义匹配度，这比向量相似度或 BM25 分数更精确，但计算成本也更高。如果一开始就对全量文档做 rerank，延迟和成本都受不了。

所以项目里的检索链路是：先用向量和 BM25 做高召回，扩大候选池（比如召回 TopK 的两倍），然后 RRF 融合去重，再对融合后的候选做 rerank，最后截取最终 TopK。

这样的好处是：第一阶段（召回）追求高覆盖、低延迟，允许一定噪音；第二阶段（精排）追求高精度，只处理少量候选。在效果和成本之间找到平衡。

如果 rerank 超时或失败，应该降级返回 RRF 融合结果，不能让一个精排服务故障导致整个问答失败。当前通过消融配置控制是否启用 rerank，可以对比有无 rerank 的质量和延迟差异。

**可能追问：**

- **rerank 和向量检索有什么区别？**：向量检索用 embedding 相似度快速筛选大规模候选。rerank 用更复杂模型对少量候选精确打分，计算成本高但更准确。
    
- **rerank 失败怎么办？**：降级到 RRF 融合结果，跳过精排步骤。记录降级原因到 trace，便于分析质量下降。
    
- **候选多大合适？**：太少可能漏掉相关内容，太多增加 rerank 耗时和成本。当前设为 TopK 两倍（如最终要 5 条，rerank 候选 10 条），可通过消融实验调整。
    

**补充细节：**

两阶段检索架构：

```text
第一阶段（召回）：
- 目标：高覆盖、低延迟
- 策略：向量 + BM25 双路召回
- 允许：一定噪音

第二阶段（精排）：
- 目标：高精度
- 策略：rerank 模型打分
- 代价：高延迟、高成本
```

验证方法：固定测试集比较"无 rerank""有 rerank"的 Context Precision、答案质量、P95 延迟和 Token。rerank 应该降低候选噪音、提升最终答案质量，代价是增加延迟。

### 31. 如何证明检索优化有效？

我觉得证明检索优化有效必须用数据说话，而不是凭主观感觉。核心方法是固定测试集、文档版本和模型配置，分别跑基线和消融实验组，比较召回率、答案质量、延迟和 Token。消融实验一次只改一个组件，才能归因。

不能只凭几次主观体验就说"优化有效"。要有固定的问题集、固定的知识库版本、固定的模型和配置，然后分别跑不同的检索策略，对比客观指标。

项目里通过 `AblationExperimentService` 管理实验配置。每组配置包含是否启用 Query 扩展、向量检索、BM25、RRF、rerank，以及 TopK、RRF_K 等参数。固定问题集在不同配置下运行，保存每次的召回结果、最终答案、耗时、Token 和四项 RAGAS 指标（Context Precision、Context Recall、Faithfulness、Answer Relevancy）。

分析时不能只看综合分，要看各指标变化。向量检索可能提高同义召回，BM25 提高术语精确率；Query 扩展可能提高 Context Recall 但引入噪音；rerank 可能提高 Context Precision 但增加延迟。每个改动都要看它带来了什么、牺牲了什么。

消融实验的关键是一次只改一个变量。如果同时改了 TopK、关了 rerank、还换了模型，即使效果变好了，也不知道是哪个改动起的作用。应该先定基线（完整流水线：Query扩展 + BM25 + 向量检索 + RRF + rerank），然后逐个关闭组件，每次只改一项，对比指标变化。

**可能追问：**

- **基线怎么选？**：当前项目的基线是完整流水线配置（Query扩展 + BM25 + 向量检索 + RRF + rerank 全部启用），这是 application.yml 中 ablation.rag 的默认配置。消融实验时逐个关闭组件，验证每个组件的必要性和贡献。
    
- **测试集怎么构建？**：从真实用户问题中抽样，覆盖常见场景、边界情况和历史失败案例。人工标注预期答案或关键证据，作为评估基准。
    
- **如果没有测试集怎么办？**：只能说实现了检索优化机制，不能声称带来了具体的提升百分比。应该尽快补充测试集，用数据说话。
    

**补充细节：**

消融实验示例：

```text
基线（完整流水线）：Query扩展 + BM25 + 向量 + RRF + rerank
  → Context Recall: 0.82
  → Context Precision: 0.82
  → Answer Relevancy: 0.88
  → 延迟: 2.6s

- 关闭 rerank：
  → Context Precision: 0.70 (-15%)
  → 延迟: 1.9s (-27%)
  结论：rerank 对精度贡献明显，但延迟较高

- 关闭 Query 扩展：
  → Context Recall: 0.75 (-9%)
  → Context Precision: 0.84 (+2%, 噪音减少)
  → 延迟: 2.4s (-8%)
  结论：Query 扩展提升召回，但引入噪音

- 关闭向量检索（仅 BM25）：
  → Context Recall: 0.60 (-27%)
  → Context Precision: 0.75 (-9%)
  → 延迟: 1.2s (-54%)
  结论：向量检索对同义召回至关重要

- 关闭 RRF（多路结果简单合并）：
  → Context Precision: 0.75 (-9%)
  → Context Recall: 0.80 (-2%)
  结论：RRF 融合提升精度，延迟影响小
```

每次改动都记录指标变化，最后根据业务需求选择最优组合。

同时记录空结果率、fallback 率、rerank 失败率和重复 chunk 率。没有固定数据集时，只能说完成机制，不能声称效果提升。每个结论要结合效果、成本和稳定性，不能只看单一指标。

**可能追问：**

- **为什么基线是完整流水线而不是最简配置？**：因为项目的设计目标就是实现混合检索的完整流水线，消融实验的目的是验证每个组件的贡献和必要性。如果某个组件关闭后指标下降明显，说明该组件是必要的；如果下降不明显但成本降低很多，可以考虑在性能敏感场景关闭该组件。
- **消融实验怎么做？**：基线上逐项增加组件（+Query扩展、+RRF、+rerank），每次只改一项，观察指标变化并归因。
- **多次运行结果不一致怎么办？**：LLM 评估有随机性，应多次运行取均值和方差；方差过大说明评估不稳定或测试集太小，需要扩充样本或固定随机种子。

**补充细节：**

Recall@K 看是否召回关键证据，Precision 看召回内容噪音比例，Answer Relevancy 看答案是否回应问题，Faithfulness 看答案是否基于证据。四项指标组合才能定位问题：召回不足是检索问题，召回噪音大是精排问题，答案不忠实是生成问题。

验证方法：准备 20-50 条标注问题，每条记录预期证据范围或关键事实；同一套问题在不同配置下运行，生成对比报告；人工抽样验证 LLM judge 是否合理。

## 四、RAG 评估与消融实验

### 32. 你说的 RAGAS 是直接接入官方库吗？

我们项目不是直接调 Python RAGAS 官方库，而是参考它的指标定义，在 Java 评估服务里自己实现的。我觉得这样做主要有几个好处：

首先是技术栈统一。项目后端全是 Java，如果要调 Python 库就得搞进程通信、环境管理这些额外的东西，维护成本挺高的。自己用 Java 实现的话，可以直接复用现有的 RagTrace、配置管理和异常处理机制，所有数据都在一个服务里，query、扩展 query、召回上下文、最终答案、Token、延迟、错误信息这些都能统一保存，调试起来也方便。

其次是可控性更强。我们可以加入超时控制、规则评分兜底、降级标记这些逻辑，评估本身也可能超时或不稳定嘛，所以四项指标可以并行执行，单项设置安全等待时间，异常的话返回可识别的失败值（比如 -1），报告里明确区分真实评分和缺失评分，不会把失败样本混进平均分里。

关于四项指标：Faithfulness 关注答案中的事实是否能被检索上下文支持，主要用来发现幻觉；Answer Relevancy 关注回答是否真正回应了问题；Context Precision 关注返回上下文里相关内容的比例，反映噪音程度；Context Recall 关注应该出现的证据是否被召回了，反映漏召回情况。前两项偏生成质量，后两项偏检索质量，组合起来才能定位问题在哪一层。

以 Faithfulness 为例，我们不是简单让模型输出一个总分，而是先让评估模型把答案拆成原子声明，再逐条判断是否被上下文支持，按支持比例聚合；解析失败时做规则或宽松匹配兜底，但会标记评估降级。

需要强调的是，这套评估不是线上绝对正确率。LLM judge 本身有偏差，Ground Truth 也可能不完整，所以我们主要用它做同一数据集、同一配置下的相对比较，再结合人工抽样、规则指标和多次运行方差来综合判断。

**可能追问：**

- **为什么不直接用官方 Python 库？**：项目是 Java 技术栈，调用 Python 需要额外的进程通信和环境管理，增加复杂度。自研可以直接复用 Java 服务的 trace、配置和异常处理，所有数据在一个服务里统一管理，调试和维护都更方便。

- **自研评估和官方库结果一致吗？**：指标定义是相同的，但实现细节（Prompt 怎么写、解析逻辑、超时处理）可能有差异。我们应该通过相同样本对比验证一致性，如果偏差太大就需要调整实现细节。

- **评估结果可以直接当线上指标吗？**：不能。LLM judge 有偏差和随机性，主要用于相对比较（方案 A vs B）和回归检测，不能解释成绝对准确率。线上质量还需要结合用户反馈、人工抽样等多种方式验证。

**补充细节：**

`EvaluationService` 实现四项指标评估，每项都调用 LLM 并解析结构化输出：

```java
// 并行执行四项评估，减少总耗时
CompletableFuture<Double> faithfulness = evaluateFaithfulness(query, answer, context);
CompletableFuture<Double> relevancy = evaluateRelevancy(query, answer);
CompletableFuture<Double> precision = evaluatePrecision(query, context);
CompletableFuture<Double> recall = evaluateRecall(query, context, groundTruth);

// 单项超时返回特殊值 -1，而非默认分数
faithfulness.orTimeout(30, TimeUnit.SECONDS).exceptionally(ex -> -1.0);
```

报告格式示例：

```text
总样本: 50
有效样本: 45
  Faithfulness 均值: 0.82
  Answer Relevancy 均值: 0.78
失败样本: 5 (3 个超时, 2 个解析失败)
```

验证方法：准备标注样本（问题、答案、上下文、预期证据），手工评估和 LLM judge 结果对比，计算一致率；不一致样本分析是评估 Prompt 问题还是标注问题。

### 33. 四个指标分别看什么？

我觉得这四项指标各有侧重，组合起来才能完整诊断 RAG 系统的问题。

首先是 Faithfulness，它看答案中的事实是否能被检索上下文支持，主要用来发现幻觉。如果 Faithfulness 低，说明模型在瞎编，没有基于检索到的证据生成答案。

然后是 Answer Relevancy，它看答案是否真正回应了问题，避免答非所问。有时候答案虽然基于证据，但跑题了，这时候 Faithfulness 可能挺高，但 Relevancy 就低了。

Context Precision 看的是召回上下文中相关内容的比例，反映噪音程度。如果 Precision 低，说明召回了太多无关内容，这时候需要精排或者调整 TopK。

Context Recall 看的是应该出现的证据是否被召回了，反映漏召回情况。如果 Recall 低，说明关键证据根本没召回上来，这是检索策略的问题。

前两个指标更偏生成质量，后两个更偏检索质量。四项结合才能判断问题来自漏召回、召回噪音还是模型生成偏离证据。比如：

- Context Recall 低 → 关键证据没召回，检索策略需要优化（可能需要 Query 扩展、调整 TopK、或者改进向量检索）
- Context Precision 低 → 召回了太多无关内容，需要精排或调 TopK
- Faithfulness 低但上下文正确 → 模型没有基于证据生成，可能是 Prompt 或模型问题
- Answer Relevancy 低 → 答案虽然基于证据但没回应问题核心，可能是 Prompt 引导不够明确

项目中通过 `EvaluationService` 分别计算四项指标，每项都调用 LLM 并解析结构化输出。报告按指标聚合均值、有效样本数和失败样本数，便于定位具体问题层。

重要的是，这四项不应该简单平均成一个综合分，因为它们反映不同维度的问题，需要分别分析。比如我可能愿意接受稍低的 Context Precision（多召回一些内容）来换取更高的 Context Recall（不漏掉关键证据），这是一个权衡。

**可能追问：**

- **Context Precision 和 Recall 有什么区别？**：Precision 看召回内容里有多少是相关的（噪音控制），Recall 看应该召回的内容有多少被召回了（覆盖完整性）。这是经典的查准率和查全率的关系，通常需要在两者之间做权衡。

- **Faithfulness 和 Answer Relevancy 有什么区别？**：Faithfulness 看答案是否基于证据（不瞎编），Relevancy 看答案是否回应问题（不跑题）。一个答案可能完全基于证据但答非所问，也可能回应了问题但加了一些证据里没有的内容。

- **四项指标都高就是好系统吗？**：不一定。还要看延迟、Token、成本和稳定性。高质量但慢、贵或经常超时的系统也不适合生产环境。我们需要在质量、成本、延迟之间找到平衡点。

**补充细节：**

每个指标的评估都可能失败或超时，因此报告应区分有效评分和缺失评分：

```text
实验配置：向量检索 + BM25 + RRF + Rerank
总样本: 50

Context Precision:
  有效样本: 48, 均值: 0.85
  失败样本: 2 (超时)

Context Recall:
  有效样本: 50, 均值: 0.78
  失败样本: 0

Faithfulness:
  有效样本: 45, 均值: 0.82
  失败样本: 5 (3 个超时, 2 个解析失败)

Answer Relevancy:
  有效样本: 47, 均值: 0.88
  失败样本: 3 (超时)
```

50 个样本中 5 个 Faithfulness 评估超时，应该报告"有效样本 45，均值 0.82，失败 5"，而不是把超时当 0 或 0.5 混入均值，那样会严重扭曲结果。

验证方法：人工标注部分样本的四项指标预期值，与 LLM judge 结果对比；不一致时分析是评估 Prompt 问题、模型理解偏差还是人工标注问题。

### 34. Faithfulness 怎么评估才更可解释？

我认为比起直接让模型输出一个总分，更稳的方式是先把答案拆成原子声明，再判断每条声明是否被上下文支持，最后按支持比例聚合。这样做的好处是过程可解释，失败时能看到具体哪条声明出了问题。

项目中 Faithfulness 评估分两步走：

第一步，让 LLM 把答案拆分成独立的事实声明列表。比如答案是"接口返回 404，需要检查路由配置"，应该拆成两条独立声明："接口返回 404" 和 "需要检查路由配置"。每条声明必须是独立的、可验证的事实，不能是"系统很好"这种主观评价。

第二步，逐条判断每个声明是否能在检索上下文中找到支持证据。对每条声明，评估模型返回 `supported: true/false` 和具体的支持依据（哪一段上下文支持了这个声明）。最终 Faithfulness 分数 = 被支持声明数 / 总声明数。

这种方式比直接打分透明多了。如果评估结果不理想，我能看到具体是哪条声明不被支持，是因为上下文里确实没有相关证据，还是评估模型理解出了偏差。

当然，声明拆分或判断可能失败。这时候我们会降级到规则匹配：检查答案关键词是否出现在上下文中，或者宽松判断为 0.5 并标记降级。重要的是保留原始声明列表和判断依据，便于人工抽样验证评估质量。

需要强调的是，这类 LLM judge 仍然不是绝对真值。它有偏差和随机性，所以我们主要把它用于同一测试集、同一配置下的相对比较，并保留原始声明和证据，方便后续人工抽样验证。

**可能追问：**

- **声明拆分失败怎么办？**：降级到规则评分（比如计算关键词覆盖率）或返回失败标记，不强行给出 0 或 1。报告中明确区分正常评分和降级评分，保证结果可追溯。

- **为什么不直接让模型打 0-1 分？**：直接打分缺少中间过程，无法解释为什么给这个分。拆分声明后可以看到具体哪些事实被支持、哪些不被支持，出问题时能快速定位是检索问题还是生成问题。

- **人工怎么验证评估质量？**：抽样查看声明拆分是否合理、判断依据是否准确，统计与人工标注的一致率。不一致样本需要分析是评估 Prompt 的问题还是标注问题，然后针对性调整。

**补充细节：**

原子声明示例：

```text
原始答案：
"接口超时可能是网络问题或服务端负载高，建议检查日志"

拆分后的原子声明：
1. "接口超时"
2. "可能原因：网络问题"
3. "可能原因：服务端负载高"
4. "建议检查日志"

逐条判断：
1. 支持 ✓ (上下文: "请求在 30s 后超时")
2. 支持 ✓ (上下文: "可能的原因包括网络延迟...")
3. 支持 ✓ (上下文: "或者服务端负载过高")
4. 不支持 ✗ (上下文中未提及日志检查)

Faithfulness = 3/4 = 0.75
```

评估记录应包含：

```java
{
  "answer": "原始答案...",
  "claims": ["声明1", "声明2", ...],
  "support": [
    {"claim": "声明1", "supported": true, "evidence": "支持证据..."},
    {"claim": "声明2", "supported": false, "evidence": null}
  ],
  "score": 0.75,
  "status": "SUCCESS"  // 或 PARSE_ERROR, TIMEOUT, MODEL_ERROR
}
```

验证方法：人工标注部分答案的原子声明和支持情况，与 LLM 拆分结果对比；Faithfulness 分数与人工判断的相关性应在 0.7 以上，如果低于这个阈值说明评估质量有问题，需要调整评估 Prompt。

### 35. 为什么评估任务需要超时和默认值？

我觉得这个问题的核心是要理解：默认值的作用是让批量任务能结束，不是伪造一个看起来正常的分数。

评估任务调用外部 LLM，可能会超时、解析失败、或者模型本身异常。如果不设超时，一个样本卡住就会导致整个批量评估永远跑不完。但更重要的是，处理失败的方式不能简单粗暴。

我们的做法是，每条评估结果同时保存多个字段：score（分数）、status（状态）、error（错误信息）、duration（耗时）和 evaluator version（评估器版本）。状态包括：
- `SUCCESS`：正常完成
- `TIMEOUT`：超时
- `PARSE_ERROR`：解析失败
- `MODEL_ERROR`：外部模型异常

关键是聚合的时候不能把超时样本全部当成 0.5，然后和真实评分混成一个平均数。这样做会严重扭曲结果，而且掩盖评估系统本身的问题。

更合理的报告至少要给出：有效样本数、失败样本数、各指标均值（只基于有效样本）和失败率。如果某组实验有效样本不足（比如 50 个样本里 20 个失败），就应该标记结论不可靠，而不是硬着头皮算一个平均分出来。

比较方案 A/B 时，还要确保两边有效样本口径一致。如果方案 A 有 45 个有效样本，方案 B 只有 30 个，直接比较均值是不公平的，要么单独报告缺失差异，要么只对比共同有效的样本。

另外，规则评分也要与 LLM 指标分开保存。回答为空、上下文为空、延迟超阈值、Token 超预算这些属于确定性的系统问题，不能完全交给 LLM judge。最终可以生成一个综合分，但必须保留子分数和原始证据，避免综合分掩盖”答案语义不错但检索全空”这种问题。

项目中我们对四项指标并行评估，单项设置超时（比如 30 秒），超时返回特殊值 `-1` 并标记状态为 `TIMEOUT`。报告生成时分别统计有效评分均值、失败数和失败率，不把失败样本混入均值计算。

**可能追问：**

- **超时应该设置多长？**：根据评估模型的调用延迟确定，一般 10-30 秒。过短会增加失败率，过长会拖慢批量评估。我们通常先跑几个样本观察平均耗时，然后设置为 P95 耗时的 2 倍作为超时阈值。

- **失败样本怎么处理？**：记录失败原因和样本 ID，人工排查是评估服务问题（比如网络不稳定）、模型不稳定（比如返回格式不一致）还是样本异常（比如答案过长导致超时）。修复问题后重新评估失败样本，而不是直接丢弃或给默认分。

- **为什么不能把失败当 0 分？**：0 分表示质量很差，失败表示无法评估，这是两个完全不同的概念。混在一起会低估真实质量并掩盖评估系统本身的问题。如果失败率很高，说明评估系统有问题，需要优先修复，而不是继续用错误的数据做决策。

**补充细节：**

评估结果存储格式：

```java
{
  “sampleId”: “qa-001”,
  “score”: 0.82,
  “status”: “SUCCESS”,  // 或 TIMEOUT, PARSE_ERROR, MODEL_ERROR
  “error”: null,
  “duration”: 2350,  // 毫秒
  “evaluatorVersion”: “v1.2”,
  “timestamp”: “2026-08-27T10:30:00Z”
}
```

报告格式示例：

```text
实验：Query扩展 + 向量检索 + BM25 + RRF
总样本: 50

Faithfulness:
  有效样本: 45 (90%)
  均值: 0.82 ± 0.08
  失败样本: 5 (10%)
    - 超时: 3
    - 解析失败: 2

Context Recall:
  有效样本: 48 (96%)
  均值: 0.78 ± 0.12
  失败样本: 2 (4%)
    - 模型异常: 2

结论可信度: 高 (有效样本率 > 85%)
```

规则评分示例：
- 答案长度 < 10 字符 → 标记为无效
- 上下文为空 → Context Precision/Recall 标记为 N/A
- 延迟 > 10 秒 → 系统性能扣分（独立于 LLM judge）

规则分数和 LLM judge 分数分开保存，最终报告同时展示两者，避免混淆。

验证方法：故意构造超时场景（Mock 慢模型）、解析失败场景（返回非法 JSON），断言系统正确标记失败状态且不影响其他样本的评估；报告应明确区分有效样本和失败样本统计，失败率过高时给出警告。

### 36. Skill 系统是干什么的？

Skill 系统主要是为了让 Agent 的专业能力能够**模块化、按需扩展**，而不是把所有能力都写死在 Agent 核心代码里。

比如 Agent 本身可以提供知识库检索、创建笔记、网页抓取这些基础能力，但如果以后要增加代码审查、SQL 生成、数据分析等专业能力，如果每增加一种能力都修改 Agent 核心逻辑，维护成本会比较高。

所以我把这些专业能力封装成独立的 Skill。Skill 包主要包含 `manifest.json`、Prompt 模板、示例以及可选的工具定义。用户导入 Skill 后，系统会根据 `capabilities` 等信息判断它适合什么任务，Agent 执行对应任务时再动态加载。

这样 Agent 核心保持比较稳定，专业能力可以独立扩展，也方便用户或者社区贡献新的 Skill，而不需要修改核心代码。

**可能追问：**

- **Skill 和 Tool 有什么区别？**：Tool 解决的是“Agent 能做什么具体操作”，比如搜索、创建笔记；Skill 更像是“怎么专业地完成一类任务”，主要提供 Prompt、能力描述和工具组合。
    
- **Skill 怎么保证安全？**：当前 Skill 本身不包含可执行代码，主要是 Prompt 和工具声明。真正的工具执行仍然由系统控制，并且会经过工具白名单和权限限制，所以不能因为导入一个 Skill 就直接执行任意代码。
    
- **Skill 和普通 Prompt 模板有什么区别？**：普通 Prompt 主要是一段提示词，而 Skill 除了 Prompt，还包含标准化的能力描述、版本和可选工具定义，可以被系统注册、匹配和按任务动态加载。
    

**补充细节：**

可以把 Skill 理解成一个**专业能力包**：

```text
Skill
├── manifest.json    → 我是谁、能做什么
├── prompt/          → 怎么专业地完成任务
├── examples/        → 给模型参考的示例
└── tools/           → 我需要哪些工具
```

例如：

```text
code-review Skill
        ↓
“我是代码审查专家”
        ↓
关注安全、性能、可维护性
        ↓
可以使用相关工具
```

所以它不是重新造一个 Agent，而是在**已有 Agent 上增加一层专业能力**。

核心可以记成一句话：

> **Skill 是对专业能力的声明式封装，让 Agent 可以按需加载能力，而不是把所有能力硬编码进 Agent。**


### 37. Skill 怎么和 Agent 结合？

Skill 和 Agent 的结合主要通过**任务匹配 + 动态加载**实现。

用户发起任务后，Supervisor 首先分析任务类型。如果任务和某个 Skill 的 `capabilities` 匹配，就可以加载这个 Skill。加载之后，Skill 的 Prompt 会加入当前 Agent 的执行上下文，对 Agent 的行为进行专业化约束；如果 Skill 声明了相关工具，也会加入当前任务允许使用的工具范围。

比如用户说“帮我审查这段代码”，Supervisor 判断这是 `code_review` 类型任务。如果系统里有 `code-review` Skill，就加载它的 Prompt，让 Agent 按照代码审查的规则执行。

用户也可以显式指定 Skill，这种情况下就不需要自动匹配，直接加载指定 Skill。

一个比较重要的设计是，**Skill 是任务级作用域的**。当前任务结束以后，Skill 的 Prompt 和工具配置就不会继续影响其他任务，这样可以减少不同 Skill 之间互相污染的问题。

**可能追问：**

- **多个 Skill 都匹配怎么办？**：可以按照优先级进行选择，比如用户显式指定的 Skill 优先；如果多个 Skill 都符合，也可以由 Supervisor 决定，避免无条件全部加载。
    
- **Skill 可以调用另一个 Skill 吗？**：当前不支持 Skill 嵌套。Skill 主要负责当前任务的能力增强，如果需要组合多个专业能力，可以交给 Supervisor 拆成多个子任务。
    
- **Skill 会增加 Token 消耗吗？**：会。Skill 的 Prompt 注入后会增加上下文长度，所以 Skill Prompt 应该尽量精简，只保留当前任务真正需要的专业规则。
    

**补充细节：**

整个过程可以理解成：

```text
用户：
“帮我审查这段代码”
       ↓
Supervisor
       ↓
判断任务类型：code_review
       ↓
匹配 Skill
       ↓
加载 code-review Skill
       ↓
Prompt + 工具声明
       ↓
当前 Agent
       ↓
执行代码审查
       ↓
任务结束
       ↓
Skill 作用域结束
```

这里要注意一个概念：

**Skill 不是 Agent 本身。**

更准确地说：

```text
Agent
= 负责理解目标、决策和执行

Skill
= 给 Agent 提供某一类专业任务的指导和能力配置

Tool
= Agent 实际调用的具体操作能力
```

所以可以记成：

> **Agent 决定怎么做，Skill 告诉它某类任务应该怎么专业地做，Tool 负责真正执行操作。**


### 38. Skill Center 怎么实现的？

Skill Center 在当前项目里并不是一个复杂的应用市场，主要可以理解成一个**Skill 清单 + 安装管理服务**。

后端提供 Skill 清单接口，返回 Skill 的名称、版本、描述、作者、标签、下载地址以及安装状态。前端的 Skill 管理页面展示这些信息，用户选择 Skill 后调用安装接口。

安装时，后端把 Skill 包下载到临时目录，解压后校验 `manifest.json` 和必要字段，校验通过后保存到 Skill 目录，并把 Skill 的元信息注册到数据库。之后 Agent 就可以查询已安装的 Skill 并进行匹配和加载。

版本管理主要通过 `manifest.json` 中的 `version` 字段完成，安装时保存版本信息，后续可以比较本地版本和市场版本来判断是否需要更新。

**可能追问：**

- **Skill 版本怎么管理？**：在 `manifest.json` 中维护版本号，数据库保存当前安装版本，更新时比较版本号，再下载新版本替换。
    
- **Skill 有依赖怎么办？**：当前 Skill 是自包含的，不支持 Skill 之间的依赖。如果未来需要组合依赖，可以在 manifest 中增加 `dependencies` 字段，再设计依赖解析机制。
    
- **Skill Center 真的是一个完整的市场吗？**：目前不是，更准确地说是一个轻量的 Skill 管理和分发机制。真正的市场还需要增加后台审核、发布、版本管理和安全扫描等能力。
    

**补充细节：**

你可以把 Skill Center 理解成：

```text
Skill Center
    ↓
Skill 清单
    ↓
用户选择
    ↓
下载安装包
    ↓
manifest 校验
    ↓
保存 Skill
    ↓
注册数据库
    ↓
Agent 可以使用
```

所以当前实现的核心并不是“做了一个像 npm 一样复杂的包管理平台”，而是：

> **定义统一的 Skill 包格式，然后提供发现、安装、注册和使用这几个基本能力。**

还有一个面试时非常值得强调的点：

> **Skill 的安全边界在系统，而不是 Skill 包自己。**

也就是说，即使 Skill 声明了某个工具：

```text
Skill
 ↓
声明：“我需要执行 SQL 的工具”
 ↓
系统检查
 ↓
权限 / 白名单 / 参数校验
 ↓
Tool 才真正执行
```

而不是：

```text
导入 Skill
 ↓
Skill 自己执行任意代码
```

这也是为什么你可以把当前 Skill 定位成**声明式能力扩展**，而不是传统意义上可以随便执行代码的插件系统。


### 39. 回归测试的目标是什么？

核心目标是在改动代码、配置或模型之后，快速发现质量退化，避免把退化版本推到生产环境。

RAG 系统经常需要调整检索配置、Prompt、模型版本、TopK、rerank 开关等。每次改动之后，不能只凭几次主观体验就上线，应该跑一遍固定测试集，对比改动前后的指标变化。

项目里的回归测试会保存一批固定的测试问题、预期答案和评估配置。每次改动之后，用新配置跑完整个测试集，生成报告，对比四项指标（Context Precision、Context Recall、Faithfulness、Answer Relevancy）、延迟、Token 和失败率。

如果发现某项指标明显下降（比如 Faithfulness 从 0.85 降到 0.70），就要排查是哪个改动导致的，评估是否可以接受这个代价。有时候为了降低延迟可能会牺牲一点质量，这种权衡需要明确。如果发现延迟或 Token 大幅增加，也要评估是否值得。

需要强调的是，回归测试不是为了证明系统完美，而是为了在改动之后快速发现问题。它是质量保障的最后防线，帮助我们在上线前及时发现和修复问题。

**可能追问：**

- **测试集怎么构建？**：从真实用户问题中抽样，覆盖常见场景、边界情况和历史失败案例。人工标注预期答案或关键证据，作为评估基准。测试集应该定期更新，加入新的失败案例，保持对系统弱点的覆盖。

- **多久跑一次回归测试？**：每次改动检索配置、Prompt、模型版本或工具逻辑之后都应该跑。理想情况下集成到 CI/CD 流程，代码合并前自动触发并生成报告。如果测试集很大，可以先跑快速冒烟测试，通过后再跑完整测试。

- **指标下降多少算退化？**：没有绝对标准，要根据业务容忍度定。通常 Faithfulness 和 Answer Relevancy 下降超过 5% 就要排查，因为这两项直接影响用户体验；Context Precision 和 Recall 下降超过 10% 也要关注。如果是为了优化延迟而牺牲质量，需要明确说明权衡点。

**补充细节：**

回归测试流程：

```text
改动代码/配置/模型
   ↓
触发回归测试（手动或自动）
   ↓
跑固定测试集（20-50 个样本）
   ↓
生成报告
  - 四项指标对比
  - 延迟 P50/P95 对比
  - Token 平均值对比
  - 失败率对比
   ↓
对比基线（改动前的结果）
   ↓
发现退化 → 排查原因 → 修复或回滚
未发现退化 → 继续上线流程
```

测试集结构示例：

```json
[
  {
    "id": "test-001",
    "query": "502 错误怎么排查",
    "expectedEvidence": ["网关超时", "上游服务"],
    "expectedAnswer": "检查上游服务状态和网关配置",
    "category": "故障排查"
  },
  {
    "id": "test-002",
    "query": "如何优化 SQL 查询",
    "expectedEvidence": ["索引", "EXPLAIN"],
    "expectedAnswer": "使用 EXPLAIN 分析执行计划，添加合适索引",
    "category": "性能优化"
  }
]
```

报告示例：

```text
回归测试报告 - 2026-08-27

配置变更：TopK 从 5 增加到 10

结果对比：
                 基线      当前      变化
Context Recall   0.78     0.85     +9%  ✓
Context Precision 0.82    0.75     -8%  ⚠
Faithfulness     0.85     0.84     -1%  ✓
Answer Relevancy 0.88     0.87     -1%  ✓
延迟 P95         2.3s     3.1s    +35%  ⚠
Token 平均       1200     1800    +50%  ⚠

结论：召回提升明显，但精度下降、延迟和成本增加。
建议：根据业务场景权衡，或尝试增加 rerank 改善精度。
```

核心原则：

> **回归测试是质量保障的最后防线。改动之后必须跑测试，对比基线，发现退化及时修复或权衡。**

### 40. 消融实验和回归测试有什么区别？

我觉得这两者的目标不同：消融实验是为了搞清楚每个组件的贡献，回归测试是为了防止质量退化。

消融实验主要用在优化阶段。你想知道 Query 扩展、向量检索、BM25、RRF、rerank 这些组件各自贡献了多少，哪些是必要的、哪些在特定场景下可以关闭。做法是基于完整流水线配置（Query扩展 + BM25 + 向量检索 + RRF + rerank 全开）作为基线，然后逐个关闭组件，观察指标变化。每一步只改一个变量，这样才能归因到具体组件。

比如我想验证 rerank 的价值，就跑两组实验：基线（全开）和关闭 rerank，对比 Context Precision、延迟和成本变化。如果想知道向量检索是否必要，就跑基线和关闭向量检索（只保留 BM25），对比召回率和语义理解能力。如果想验证 Query 扩展的收益，就跑基线和关闭 Query 扩展，对比召回覆盖率和噪音比例。

回归测试主要用在开发和维护阶段。每次改动代码、调整配置、升级模型之后，跑一遍固定测试集，对比改动前后的指标。目的是快速发现"我这次改动是不是把系统搞坏了"，而不是搞清楚各组件贡献。

两者的测试集可以共用，但侧重点不同。消融实验需要控制变量，一次只改一个组件，多次运行对比；回归测试是改动前后的一次对比，关注整体质量有没有退化。

消融实验的结果可以指导架构决策，比如"rerank 提升精度 15% 但延迟增加 35%，性能敏感场景可以关闭"或者"关闭向量检索后召回率下降 27%，说明向量检索是必要的，不能随便关"或者"Query 扩展提升召回 9% 但噪音增加 3%，需要结合 rerank 一起使用才能发挥价值"。回归测试的结果用于质量把控，比如"这次 Prompt 调整导致 Faithfulness 下降 8%，必须回滚"。

**可能追问：**

- **消融实验怎么设计？**：先固定测试集、文档版本、模型、Prompt 和评估配置。基线选完整流水线配置（全开）。然后设计实验组：关闭 Query 扩展、关闭 rerank、关闭向量检索、关闭 RRF。每组跑完整测试集，保存四项指标、延迟、Token、降级率。分析时不能只看综合分，要看各指标变化并归因到具体组件。

- **回归测试和消融实验能合并吗？**：可以共用测试集和评估逻辑，但触发时机和对比方式不同。消融实验是一次性多组对比（基线 vs 各关闭组件的配置），回归测试是改动前后两组对比（旧版本 vs 新版本）。

- **没有测试集能做消融实验吗？**：不能。消融实验需要稳定的测试集来对比各组件效果。没有测试集时，只能说"实现了消融实验框架"，不能声称"rerank 提升了 X%"或"关闭向量检索导致召回下降 Y%"，因为没有数据支撑。

**补充细节：**

消融实验示例：

```text
目标：评估各检索组件的必要性和贡献

测试集：50 个标注问题

实验配置：
1. 基线（完整流水线）：Query扩展 + BM25 + 向量 + RRF + rerank, TopK=5
2. 实验A：关闭 Query 扩展
3. 实验B：关闭 rerank
4. 实验C：关闭向量检索（仅 BM25）
5. 实验D：关闭 RRF（多路简单合并）

结果对比：
                基线    实验A   实验B   实验C   实验D
Context Recall  0.82    0.75   0.83    0.60    0.80
Context Precision 0.82  0.84   0.70    0.75    0.75
Faithfulness    0.88    0.86   0.85    0.82    0.86
Answer Relevancy 0.88   0.87   0.86    0.80    0.87
延迟(s)         2.6     2.4    1.9     1.2     2.5
Token          1500    1300   1500    1000    1500

结论：
- 关闭 Query 扩展：召回下降 9%，精度提升 2%（噪音减少），成本降低 13%
  建议：精确查询场景可以关闭，探索性查询保留
- 关闭 rerank：精度下降 15%，延迟降低 27%
  建议：性能敏感场景可以关闭，质量要求高时必须保留
- 关闭向量检索：召回下降 27%，答案相关性下降明显
  建议：向量检索是必要的，不能关闭
- 关闭 RRF：精度下降 9%，延迟影响小
  建议：RRF 融合是必要的，保留
```

回归测试示例：

```text
目标：验证 Prompt 调整未导致退化

改动：优化系统 Prompt，引导模型更简洁回答

结果对比：
                改动前   改动后   变化
Faithfulness    0.85     0.82    -3%  ✓
Answer Relevancy 0.88    0.91    +3%  ✓
延迟            2.3s     2.1s    -9%  ✓
Token           1500     1200   -20%  ✓

结论：Faithfulness 轻微下降可接受，
回答相关性和成本都有改善，通过测试。
```

核心原则：

> **消融实验搞清楚各组件贡献，指导架构决策。回归测试防止质量退化，保障上线质量。两者目标不同，但可以共用测试集和评估逻辑。基线应该是完整流水线配置（全开），消融实验通过逐个关闭组件来验证每个组件的必要性。**

### 41. 评估结果波动很大，怎么排查？

我觉得评估结果波动的排查应该从三个层面入手：数据层、模型层和评估层。

首先要确认测试集、知识库版本和 Ground Truth 有没有变化。这个看起来很基础，但实际上很容易被忽略。比如知识库里新增或删除了文档，召回的 chunk 就会变化；测试集被别人误改了，评估基准就不一样了。所以第一步我会对比前后召回的 chunk ID 列表，如果 ID 都变了，那肯定是检索层出问题了。

然后是模型配置层面。模型版本、温度参数、Prompt 调整都会影响答案的表述。即使召回的证据一样，温度从 0.7 改成 0.3，答案的风格可能就完全不同。所以我会检查模型配置和 Prompt 有没有改动，最好每次实验都保存配置快照，包括模型版本、温度、Prompt hash、检索配置（TopK、RRF_K、是否启用扩展/rerank）、评估器版本和知识库文档数量。

最后是评估层本身的问题。评估模型也有随机性，超时导致部分样本降级评分，或者评估 Prompt 被改了，都会导致结果不稳定。我的做法是检查评估日志中有多少样本超时或降级，如果失败率很高，说明评估系统本身有问题。如果是评估器随机性导致的波动，就应该多次运行取均值和标准差，标准差大于 0.1 说明评估不稳定，需要增加样本数或者固定随机种子。

我认为 RAG 评估更适合看多次运行的均值、方差和相对差异，不能拿一次跑出来的 0.83 当成线上真实准确率 83%。每次实验必须保存配置快照和 trace，这样才能复现和回溯问题。

**可能追问：**

- **如何确认是检索变化还是生成变化？**：固定答案生成部分，只对比检索结果 chunk ID；如果 chunk 变了是检索问题，chunk 没变但答案变了是生成问题。
- **评估器随机性如何量化？**：相同样本运行 5-10 次，计算每个样本评分的标准差；标准差过大的样本单独排查或标记不稳定。
- **配置快照保存什么？**：模型版本、温度、Prompt hash、检索配置（TopK、RRF_K、是否启用扩展/rerank）、评估器版本、知识库版本或文档数量。

**补充细节：**

配置快照应包含所有影响结果的因素，便于复现和回溯。每次实验生成配置 hash，相同配置的多次运行应该结果相近（考虑随机性）。

验证方法：固定所有配置运行多次，计算指标方差；方差小于阈值（如 0.05）认为稳定；更换单一配置项（如模型温度），观察指标变化是否符合预期。

## 五、文档解析、切片与一致性

### 79. 文档入库为什么拆成同步和异步？

我觉得这个设计主要是为了平衡用户体验和系统稳定性。

文档上传之后，用户肯定希望尽快看到上传成功的反馈，不想一直等着。但是向量化和写入 Chroma 这个过程比较耗时，一个文档可能要几秒甚至几十秒。如果全部同步执行，用户体验会很差，而且还会占用 Web 请求的线程，影响整体并发能力。

所以我的设计是这样的：文档处理先计算 MD5，检查同一用户是否已经上传过相同文件，避免重复上传。然后进行解析和切片，MySQL 和 BM25 相关数据先同步写入，这样文档元信息和关键词检索能尽快可用。数据库的唯一约束可以兜底并发上传的问题，避免两个请求都通过了”先查询后插入”的检查。

MySQL 主流程成功之后，我会投递一个 Chroma 异步任务，embedding 和向量写入不占用上传请求的长事务。异步任务需要记录文档 ID、chunk、批次、状态、重试次数和错误原因，这样出问题的时候能追溯。

批量 embedding 如果失败了，会先有限重试，实在不行可以回退到单条处理，避免一条坏数据拖垮整个批次。写入 Chroma 的时候应该使用稳定的 chunk ID，任务重试时跳过已经成功的 chunk 或者覆盖同一个 ID，避免重复向量。

如果 MySQL 成功但 Chroma 失败，系统短时间内会允许文档处于”向量未完成”状态，这时候检索可以走 BM25 或关键词路径降级。失败任务之后由补偿机制重试，超过最大次数就进入明确的失败或人工处理状态。

我认为这里实现的是最终一致性，不是分布式强事务。验证的时候不能只看上传接口返回 200 就认为成功了，还要检查 MySQL、BM25、Chroma 的实际数据、状态机流转和补偿后的最终结果。

**可能追问：**

- **为什么不全部同步？**：embedding 和向量写入耗时长（几秒到几十秒），同步执行会阻塞上传接口响应；异步后用户立即得到上传成功反馈。
- **异步失败怎么通知用户？**：文档状态标记为 `vector_failed`，用户查询文档列表时可以看到；也可以通过 WebSocket 或轮询推送状态变更。
- **补偿任务如何触发？**：定时任务扫描 `vector_pending` 或 `vector_failed` 状态且创建时间超过阈值的文档，重新投递异步任务；超过最大重试次数进入人工处理。

**补充细节：**

同步写入流程：上传 → MD5 去重 → 解析切片 → MySQL 事务（文档 + chunk） → BM25 索引 → 返回成功。
异步写入流程：投递任务 → 批量 embedding → 写入 Chroma → 更新状态为 `completed`；失败记录错误并标记 `vector_failed`。

验证方法：上传文档后立即查询，断言 MySQL 和 BM25 已有数据；等待异步完成后查询 Chroma，断言向量存在；故障注入 Chroma 不可用，断言文档状态为 `vector_failed` 且检索降级到 BM25。

### 44. 为什么还需要规则评分？

我认为 LLM judge 只能判断语义质量，但系统质量不能只看语义，还有很多硬指标必须保证。

比如回答为空、上下文为空、延迟超阈值、相似度过低、Token 超预算，这些都可以用确定性规则判断。这些是系统必须保证的基本约束，不应该完全依赖 LLM 评估。LLM 可能会给一个空回答打个 0.3 分，但实际上空回答就是 100% 的失败，不应该有任何模糊性。

项目里的规则评分包括：答案长度检查（小于 10 字符标记异常）、上下文非空检查、延迟阈值（超过 10 秒扣分）、Token 预算检查、检索降级标记。这些规则分数单独保存，不与 LLM judge 的语义评分直接混合。最终报告可以生成一个综合分，但必须同时展示规则分和语义分，避免一个综合分掩盖"答案语义不错但检索全空"这种问题。

规则权重应该通过回归样本校准，而不是随意设置。比如我发现历史失败样本中 80% 都是延迟超 10 秒，那延迟应该是高权重规则；如果答案长度和质量相关性不高，就降低长度规则权重。

我觉得规则和语义评分结合才能全面反映系统质量。只用 LLM judge 的话，无法捕捉系统性能和可用性问题；只用规则的话，又无法判断答案是否答非所问或产生幻觉。两者结合才是完整的评估体系。

**可能追问：**

- **规则和 LLM judge 如何组合？**：分开保存，报告同时展示。需要综合分时按校准权重加权，但不能让综合分掩盖子项问题。
    
- **规则权重如何校准？**：用历史失败样本统计各规则与实际问题相关性，调整权重使综合分与人工判断相关性最高。
    
- **为什么不只用规则？**：规则无法判断语义质量，比如答案长度够、延迟正常，但答非所问或产生幻觉，需要 LLM judge 补充。
    

**补充细节：**

常见规则示例：

```text
答案长度 < 10 或 > 2000 → 标记异常
上下文为空 → Context Precision/Recall 标记 N/A
延迟 > P95 阈值 → 扣分
Token > 预算 → 扣分
检索降级到关键词 → 标记质量降级
```

验证方法：

- 构造明确违反规则的样本（空答案、超长延迟）
- 断言规则正确识别并扣分
- 规则分和语义分应独立展示
- 综合分变化应可解释

核心原则：

> **规则评分捕捉系统硬指标，LLM judge 评估语义质量。两者结合才能全面反映系统质量。**

### 45. 多格式文档怎么解析？

我的做法是非 PDF 文件统一走 Apache Tika，PDF 单独使用 PDFBox 按页抽取，并保留页码信息。

项目里 `DocumentProcessor` 会先根据文件扩展名和 MIME 类型判断格式。PDF 文件我选择用 PDFBox 逐页提取文本，每页可以保存为独立 chunk 或者合并到段落，同时记录页码。这样做的好处是召回结果可以显示”第 3 页”，方便用户定位原文，这个对企业知识库场景挺重要的。

非 PDF 文件，比如 Word、TXT、Markdown、HTML，统一用 Tika 解析。Tika 内部会调用对应的解析器，支持几十种格式，基本能覆盖常见的办公文档。解析后的文本我会做 Unicode 归一化（NFKC），统一全角半角、兼容字符，避免”３”和”3”无法匹配这种问题。

解析失败、空文档、扫描图片 PDF 和超长文件都要有明确状态，不能只看接口是否返回 200。扫描 PDF 如果解析出的文本为空或很少，我会标记为 `OCR_REQUIRED`。OCR 如果没有实现，应该如实说是后续能力，不要含糊其辞。超长文件（比如超过 10MB 或 1000 页）应该限流或拒绝，避免解析占用过多资源把服务拖垮。

我认为文档解析这块的关键是异常处理要清晰。解析失败要保存错误信息到文档状态，空文档标记为空不入库，扫描 PDF 标记需要 OCR，超长文件直接拒绝或限流。不能让异常文档静默丢弃，否则用户根本不知道自己的文档有没有真正入库。

**可能追问：**

- **Tika 支持哪些格式？**：Office 文档（docx/xlsx/pptx）、PDF、HTML、XML、纯文本、Markdown、RTF、邮件等几十种格式。不支持的格式返回解析失败。
    
- **为什么 PDF 不用 Tika？**：Tika 也可以解析 PDF，但不方便精确控制页码和分页逻辑。PDFBox 可以逐页处理并保留页码元数据。
    
- **扫描 PDF 怎么办？**：当前只能标记为需要 OCR，不自动执行 OCR。生产化建议接入 OCR 服务（如 Tesseract、百度 OCR），将图片转文字后再切片入库。
    

**补充细节：**

解析流程：

```text
上传文件
   ↓
检测格式（扩展名 + MIME）
   ↓
选择解析器
├─ PDF → PDFBox 逐页提取 + 保留页码
└─ 其他 → Tika 统一解析
   ↓
提取文本
   ↓
Unicode 归一化（NFKC）
   ↓
切片
```

解析异常处理：

```text
解析失败 → 保存错误信息到文档状态
空文档 → 标记为空，不入库
扫描 PDF → 标记 OCR_REQUIRED
超长文件 → 拒绝或限流
```

验证方法：

- 准备各格式测试文件（PDF、Word、TXT、Markdown）
- 上传后检查文本提取正确、页码保留、归一化生效
- 扫描 PDF 应标记 `OCR_REQUIRED`
- 超长文件应拒绝或限流

核心原则：

> **PDF 单独处理保留页码，其他格式用 Tika 统一解析。解析异常有明确状态，不静默丢弃。**

### 46. 切片怎么做？为什么有 overlap？

我觉得切片这块的核心问题是：固定长度切片实现简单，但容易在段落、代码、表格或步骤中间截断，导致语义不完整。

截断之后一个 chunk 可能缺少主语或前置条件，向量表达就不完整；另一个 chunk 可能只剩结论，检索命中后模型仍然无法理解上下文。这种情况在技术文档里特别常见，比如"配置 xxx 参数"被截断了，前面的 chunk 没有说是什么参数，后面的 chunk 没有说为什么要配置。

当前的实现流程是这样的：先用 Tika 处理非 PDF，PDF 用 PDFBox 按页提取，并保留页码。文本经过清洗后交给切片逻辑，普通文本我会优先按段落、换行、句子等自然边界切分；代码、列表和表格尽量保持结构完整，实在太长的话按行切；相邻 chunk 之间保留 overlap，减少跨边界信息丢失。每个 chunk 同时保存文档标题、章节、内容类型和页码等元数据。

Overlap 是指相邻 chunk 之间有一定重叠内容，比如前一个 chunk 结尾 100 字符和后一个 chunk 开头 100 字符重复。这样做的好处是即使关键信息在边界附近，也能被至少一个 chunk 完整包含。代价是索引存储和检索计算量增加，但我认为能提高召回完整性，这个代价是值得的。

切片参数不是越大越好。chunk 太小会导致证据碎片化和召回次数增加，太大则会把无关内容一起送给模型，增加 Token 和上下文噪音。应该用固定测试集比较 Recall@K、Context Precision、答案忠实度、Token 和延迟，找到最优的平衡点。

**可能追问：**

- **Overlap 大小如何选择？**：一般 10%-20% chunk 大小，如 chunk 500 字符则 overlap 50-100 字符。太小无法覆盖边界信息，太大增加冗余。
    
- **代码和表格为什么要特殊处理？**：代码截断会破坏语法结构，表格截断会丢失列对应关系。应按函数、代码块或表格行切分。
    
- **切片后如何去重？**：相同 chunk 内容（去除空白后）用 MD5 去重。overlap 导致的部分重复是预期行为，不去重。
    

**补充细节：**

切片策略示例：

```text
普通文本：
优先按段落（双换行）
→ 超长段落按句子
→ 超长句子按标点
→ 兜底按字符数

代码：
按函数、类或代码块
保留完整语法结构

表格：
保留表头，按行切分
或保持整表（小表格）

Markdown：
按标题层级切分
保留标题路径
```

Overlap 示例：

```text
Chunk 1：...前面内容 [边界区域 100 字符]
Chunk 2：[边界区域 100 字符] 后面内容...

重叠部分确保边界附近信息完整
```

验证方法：

- 准备包含长段落、代码、表格的测试文档
- 检查切片是否在自然边界
- 检查 overlap 是否生效
- 检查元数据是否正确
- 人工评估边界附近的检索效果

核心原则：

> **优先自然边界切分，保留结构。Overlap 提高召回完整性，代价是冗余增加。**

### 47. MD5 去重能解决什么，不能解决什么？

我觉得 MD5 去重主要是用来识别同一用户重复上传的相同文件，减少重复解析和索引，但它也有明确的边界。

项目里上传文件时，我会先计算完整文件的 MD5，然后查询 `user_id + md5` 是否已存在。如果存在就直接返回已有文档，不重复解析。数据库对 `user_id + md5` 建立了唯一索引，即使两个并发请求同时通过了查询，插入时也会有一个失败并返回唯一约束冲突，前端可以提示”文件已存在”。

但是 MD5 不能识别内容相同但格式或排版不同的文件，比如同一篇文章的 Word 和 PDF 版本，它们的 MD5 是完全不同的。MD5 也不能表示 embedding 模型和切片版本的变化。如果 embedding 模型从 v1 升级到 v2，已有文档的向量应该重新生成，但 MD5 没变，系统不知道需要重建。

我认为生产化的幂等键应该纳入 parser version、chunk version 和 embedding model，这样模型或切片策略变化时能自动触发重建。但当前实现只用了 MD5，所以 MD5 只负责文件内容去重，不负责版本化重建。

另外，MD5 只在同一用户范围内去重。不同用户可以上传相同文件，因为权限是隔离的。但系统层面可以考虑全局 MD5 索引，相同文件只存一份物理数据，通过权限控制访问，类似文件系统的硬链接。不过当前按用户隔离会更简单，不用处理引用计数和复杂的删除逻辑。

**可能追问：**

- **为什么不用全局 MD5 去重？**：不同用户可能上传相同公开资料但需要独立管理和删除。全局去重需要引用计数和权限控制，当前按用户隔离更简单。
    
- **切片或模型变化怎么处理？**：生产化应增加 `parser_version` 和 `embedding_model` 字段，变更时触发重建任务。当前未实现版本化重建。
    
- **MD5 碰撞怎么办？**：MD5 碰撞概率极低（2^64），对业务文件去重足够。如果要求更高安全性可用 SHA-256。
    

**补充细节：**

去重流程：

```text
上传文件
   ↓
计算 MD5
   ↓
查询 user_id + md5
   ↓
命中 → 返回已有文档（快速）
未命中 → 继续解析
   ↓
插入时唯一约束兜底并发
```

唯一约束示例：

```sql
UNIQUE KEY uk_user_md5 (user_id, md5)
```

保证同一用户相同文件只有一条记录。

验证方法：

- 上传文件两次，第二次应命中去重并快速返回
- 并发上传相同文件，断言最终只有一条记录且无重复解析
- 修改文件内容后 MD5 变化，应作为新文档入库

核心原则：

> **MD5 负责文件内容去重，不负责版本化重建。按用户隔离，数据库唯一约束兜底并发。**

### 48. Chroma 写入失败如何恢复？

我觉得这里首先要承认这是最终一致性设计，不是强一致。

项目里文档入库流程是 MySQL 和 BM25 同步成功后就返回了，然后异步投递 Chroma 写入任务。任务执行时先批量生成 embedding（比如 10 个 chunk 一批），再批量写入 Chroma。任何一步失败都会记录到文档状态（从 `vector_pending` 变成 `vector_failed`）和错误原因。

任务支持有限重试，一般是 3 次，每次重试之间指数退避（1秒、2秒、4秒）。如果批量失败了，我会回退到单条处理，逐个处理并跳过已经成功的 chunk。这样可以避免一条坏数据拖垮整个批次。

补偿机制必须保证幂等：使用稳定的文档/chunk ID、唯一约束或者任务锁，避免重复向量。写入 Chroma 时我用 chunk ID 作为向量 ID，重试时相同 ID 会覆盖而不是新增，这样就能保证幂等。超过最大重试次数的任务会进入人工处理或死信状态，不能无限重试。

另外还有定时补偿任务，会扫描 `vector_failed` 或者超时未完成的文档，重新投递任务。成功后更新状态为 `completed`。

我认为面试中不要说”保证强一致”或者”使用分布式事务”，因为 Chroma 和 embedding 根本不在 MySQL 事务内。准确的表述应该是”通过状态标记、有限重试和补偿任务实现最终一致性”。

**可能追问：**

- **批量写入为什么要回退单条？**：一条坏数据（如文本过长、特殊字符）可能导致整批失败。回退单条可以跳过问题 chunk，让其他正常 chunk 入库。
    
- **如何保证幂等？**：用稳定 chunk ID 作为向量 ID，Chroma 写入时相同 ID 覆盖。补偿任务重试前检查 Chroma 中是否已有该 ID，有则跳过。
    
- **补偿任务如何避免重复处理？**：任务领取时用乐观锁或状态原子更新（如 `CAS vector_failed → processing`），处理完成后更新为 `completed`。
    

**补充细节：**

状态流转：

```text
pending → processing → completed（成功）
                    → vector_failed（失败）
```

补偿任务只处理 `vector_failed` 和超时 `pending` 状态文档。

重试策略：

```text
第 1 次失败 → 等待 1 秒 → 重试
第 2 次失败 → 等待 2 秒 → 重试
第 3 次失败 → 等待 4 秒 → 重试
超过 3 次 → 标记 vector_failed
```

幂等保证：

```text
Chroma 写入：
use chunk_id as vector_id
相同 ID 写入 → 覆盖，不新增
```

核心原则：

> **最终一致性，不是强一致性。状态标记 + 有限重试 + 幂等补偿。**

### 49. SSE 推送断线怎么办？

验证方法：故障注入 Chroma 不可用，断言文档状态为 `vector_failed`；恢复 Chroma 后触发补偿，断言状态变为 `completed` 且向量存在；重复补偿不应产生重复向量。

我觉得 SSE 断线其实不是大问题，因为 SSE 只是实时推送机制，不是状态的唯一来源。

项目里前端通过 SSE 接收任务进度事件，但是任务状态和 trace 都持久化在 MySQL 里。如果 SSE 断线了，前端应该从持久化的快照查询，而不是依赖 SSE 重传。当前是否支持自动重订阅属于接口和前端流程的问题，但核心原则是不能把 SSE 当成持久化存储。

所以我的设计是：SSE 只负责推送进度通知，比如"正在检索""调用了某个工具""生成答案"这些实时消息。真正的任务状态、工具调用记录和异常信息都要落到 Trace 里。用户刷新页面或者 SSE 断线重连后，前端应该先查询任务当前状态，再决定是否重新订阅 SSE。

### 50. 批量 embedding 为什么失败后还要单条回退？

我觉得这个设计的核心思想是：批量请求吞吐高，但一个坏 chunk、上游限制或者瞬时错误可能导致整批失败。有限重试后回退单条，可以定位和跳过问题 chunk，提升整体成功率。

项目中 `DocumentTaskExecutor` 执行异步向量化时，我会先把文档的所有 chunk 按批次（比如 10 个一批）调用 embedding 服务，再批量写入 Chroma。如果批量调用失败了，比如网络超时、服务限流、或者某个 chunk 文本过长，整批都无法入库。

这时候我不是直接放弃，而是把这批 chunk 拆开，逐个调用 embedding 和写入 Chroma。单条成功就标记成功，单条失败记录具体错误原因。这样正常 chunk 不会被坏数据拖累，至少能保证大部分数据能入库。

代价是请求次数和处理时间会增加，所以要设置批大小、总超时和最大重试次数，并记录批次 ID、失败 chunk 和最终状态。失败补偿的时候不能只重试整份文档，否则会重复处理已经成功的 chunk。应该先查询哪些 chunk 向量已经存在，只处理缺失的 chunk。

我认为这个策略的关键是在吞吐和成功率之间找平衡。批量 10 个假设 1 秒，单条 10 次需要 10 秒，看起来慢了。但如果批量失败率高，单条回退反而能提高成功率，总体更可控。

**可能追问：**

- **批量大小如何选择？**：根据 embedding 服务限制和网络稳定性，一般 5-20 个 chunk 一批；太小增加请求次数，太大一个失败影响面大。
- **如何识别坏 chunk？**：单条重试仍失败的 chunk，记录错误原因（如文本过长、特殊字符、编码问题）；人工排查后可以删除或修复。
- **单条回退增加多少耗时？**：批量 10 个假设 1 秒，单条 10 次需要 10 秒；但如果批量失败率高，单条回退反而能提高成功率，总体更可控。

**补充细节：**

批量策略：优先批量 → 批量失败重试 → 重试仍失败则单条回退 → 单条失败记录并跳过。补偿任务重新处理文档时，先查询 Chroma 已有 chunk ID，只处理缺失部分。

验证方法：构造包含一个超长 chunk 的批次，断言批量失败后单条回退，正常 chunk 成功、超长 chunk 失败并记录原因；补偿任务应跳过已成功 chunk，只重试失败部分。

### 51. 如何验证入库链路最终一致？

我觉得验证最终一致性的核心是对账：查 MySQL 文档记录、对应的 chunk 数量、Lucene 和 Chroma 的实际索引数量，如果都一致,说明最终一致达成。

项目中文档入库后，数据分布在多个存储：MySQL `knowledge_document` 保存文档元信息，MySQL `knowledge_document_chunk` 保存文本 chunk，Lucene 保存 BM25 索引，ChromaDB 保存向量。由于异步处理和外部调用，写入可能有延迟或失败。

我的验证方式分三种：

**1. 主键对齐验证**：从 `knowledge_document` 查出所有文档 ID，对每个文档查 `knowledge_document_chunk` 得到 chunk 数量，再查 Lucene 索引得到该文档的 chunk 数量，最后查 ChromaDB 得到该文档的向量数量。三者应该相等。如果不等，说明某个环节没写成功。

**2. 轮询验证**：异步任务完成后等待几秒（比如 5 秒），再查 Chroma。如果数量不匹配，标记为失败待补偿。这个适合实时校验，但要注意不能等太久，否则用户体验差。

**3. 对账任务**：定时任务（比如每天凌晨 2 点）扫描所有文档，对比 MySQL chunk 数量和向量数量，找出不一致的文档 ID，记录到日志或补偿队列。这个是兜底机制，保证长期运行下不会积累太多坏数据。

我认为现实中网络抖动、Chroma 写入慢、embedding 限流都可能导致短暂不一致。只要有补偿机制和定期对账，就能保证最终一致。关键是不要假设写入一定成功，也不要每次都同步等待所有存储都成功才返回，那样延迟太高。

不能只断言上传接口返回 200。要验证 MySQL 文档和 chunk 是否存在、状态是否从 processing 推进到 completed 或 vector_failed、Chroma 是否有对应 chunk、失败后重试是否成功、检索在向量未完成时是否按预期降级。

还要做故障注入：MySQL 提交后进程崩溃、embedding 超时、Chroma 不可用、批量部分失败和重复补偿。最终验证数据没有重复、状态可追踪、任务可恢复。检索降级验证：向量未完成时检索应走 BM25，断言仍能返回结果但质量可能下降。

**可能追问：**

- **如何模拟进程崩溃？**：在 MySQL 提交后、异步任务投递前强制退出或抛异常，重启后检查文档状态和补偿任务是否恢复。
- **如何验证幂等？**：同一文档触发多次补偿任务，断言 Chroma 中向量数量不变、没有重复 ID。
- **如何验证检索降级？**：向量未完成时发起检索，断言走 BM25 路径（trace 中记录降级）且能返回基本结果；向量完成后再检索，断言走完整混合检索路径。

**补充细节：**

验证脚本示例：上传文档 → 等待 5 秒 → 查询文档状态 → 查询 MySQL chunk 数量 → 查询 Chroma 向量数量 → 断言一致。故障注入后检查状态、错误日志、重试记录和最终恢复结果。

自动化测试应覆盖正常流程、各类故障和补偿恢复，不能只测正常路径。

## 六、上下文与记忆管理

### 52. Token 上下文管理怎么做？

我觉得这个问题的核心是在有限的上下文窗口内，既要保留足够的历史信息让对话连贯，又不能让 Token 超限导致调用失败或成本失控。

项目中 `ContextManager` 的做法是先估算历史 Token，预算超过 32K 时优先压缩早期长 AI 消息，再把更早消息压成摘要，保留最近消息原文。工具结果压缩后还会限制字符数，摘要失败有兜底。

具体流程是先遍历会话历史，用简单规则估算每条消息 Token（字符数 / 2.5）；累计超过阈值时触发压缩。压缩策略分三层：第一层截断早期长工具结果和 AI 响应，保留开头和结尾各 N 字符；第二层把更早的多轮对话合并成摘要，调用 LLM 提取关键信息；第三层如果仍超预算，丢弃最早轮次并记录日志。压缩后的消息列表重新计算 Token，确保在预算内再发送给模型。

核心取舍是保留近期意图和任务状态，牺牲早期长原文的细节。摘要不是无损操作，因此需要保留原始消息或支持重新检索，不能把摘要当作唯一证据。如果用户提到很早之前的内容，可以从 MySQL 历史记录中重新加载或提示用户重新提供上下文。

我认为上下文管理不能只看 Token 数量，还要看信息的实际价值。最近几轮对话往往包含当前任务的核心意图，必须完整保留；早期的长工具结果可能只是中间过程，压缩影响不大。所以压缩顺序很重要，不能简单地"从最早的开始删"。

**可能追问：**

- **为什么不直接截断最早消息？**：简单截断会丢失上下文连贯性，用户可能提到早期内容；摘要可以保留关键信息骨架。
- **摘要失败怎么办？**：降级到简单截断或保留消息标题，标记压缩降级；不能因为摘要失败导致整个对话无法继续。
- **如何验证压缩效果？**：构造超长历史会话，断言压缩后 Token 在预算内、最近消息保留原文、早期消息被摘要或截断；追问早期内容应能部分回忆或提示重新提供。

**补充细节：**

压缩顺序：长工具结果截断 → 早期 AI 消息截断 → 更早消息摘要 → 最早消息丢弃。每步压缩后重新计算 Token，达到预算即停止。

验证方法：构造包含 10 轮对话、每轮包含长工具结果的历史，触发压缩后检查消息列表、Token 估算和最近轮次是否完整保留。

### 53. 为什么先压缩长工具结果，再做历史摘要？

我觉得这个顺序的核心考虑是：长工具结果通常占 Token 多，但信息密度不稳定，后续往往只需要结论和关键证据。先压缩它们能减少成本，也较少破坏用户原始意图；如果仍超预算，再摘要更早的对话。

项目中压缩优先级是：长工具结果 > 早期 AI 长消息 > 更早多轮对话。工具结果可能包含完整检索上下文、长文档片段或详细日志，这些内容模型在生成答案后不一定需要完整保留。压缩时保留工具名、参数和结果摘要（如"检索到 5 个相关文档"），截断详细内容。AI 长消息也可以压缩开头和结尾，保留关键结论。只有这两步仍超预算时，才把更早的完整对话轮次合并成摘要。

但要注意代码实现中是按早期 AI 长消息压缩，并不等于识别了所有真实工具消息。面试时应表述为"当前实现采用长 AI/工具结果压缩策略"，不要夸大成完美语义分类。如果压缩后用户追问早期内容，可以从历史记录重新加载或提示"该内容较早，请重新提供上下文"。

我认为压缩的关键是既要节省 Token，又要尽量保持对话可理解性。用户的原始问题永远不能压缩，因为那是意图的直接表达；工具结果可以大胆压缩，因为它们是中间过程；AI 的最终回答要慎重压缩，因为可能被后续对话引用。

验证方法：构造包含 10 轮对话、每轮包含长工具结果的历史，触发压缩后检查消息列表、Token 估算和最近轮次是否完整保留。

### 54. 当前项目有长期记忆吗？

我觉得这个问题要诚实回答：严格说没有完整的持久化长期记忆。当前已有的是会话级历史压缩和 `ConversationContextManager` 维护的活动笔记、任务进度等上下文状态。

短期消息用于多轮对话，结构化会话上下文用于指代解析和 Agent 执行；如果要跨会话保留用户偏好、事实或经验，需要增加持久化记忆、召回策略、过期和删除机制。不能把内存态会话上下文说成长期记忆。

项目中 `ConversationContextManager` 维护当前会话的活动笔记列表、进行中的任务、最近提及的文档和关键实体，这些状态只在会话生命周期内有效。新会话开始时这些状态清空，不会跨会话保留。如果要实现真正的长期记忆，需要把用户偏好、历史决策、常用工具、重要事实等持久化到数据库，并在新会话中根据相关性召回。

生产化长期记忆还需要考虑记忆更新、冲突解决、过期策略和用户删除权限。比如用户上次说”我喜欢简洁回答”，下次会话应该记得；但用户也可能改变偏好，系统要能更新或删除过期记忆。

我认为长期记忆不是简单地把所有历史都存下来，而是要有选择地提取有价值的信息，并在需要时能够精准召回。这需要语义索引、时效性判断和冲突处理，是一个独立的子系统，不是会话管理的自然延伸。

**可能追问：**

- **会话上下文和长期记忆有什么区别？**：会话上下文只在当前对话中有效，新会话清空；长期记忆跨会话持久化，新会话可以召回使用。
- **如何实现长期记忆？**：持久化用户偏好、关键事实到数据库，带时间戳和重要性评分；新会话根据用户 ID 和问题相关性召回 TopK 记忆。
- **长期记忆会有冲突吗？**：会，用户可能前后说法不一致；应记录时间戳，新记忆覆盖旧记忆或明确提示用户确认。

**补充细节：**

当前能力：会话历史压缩、活动笔记列表、任务进度、最近文档引用。缺失能力：跨会话记忆召回、用户偏好持久化、记忆更新和过期。

生产化方案：新增 `UserMemory` 表，保存用户 ID、记忆类型（偏好/事实/决策）、内容、时间戳、重要性；新会话开始时召回相关记忆注入上下文。

验证方法：当前会话记录用户偏好，新会话应不记得（无长期记忆）；实现长期记忆后，新会话应能召回并应用偏好。

### 55. 上下文压缩有什么风险？

我觉得上下文压缩的主要风险是信息丢失：摘要可能漏掉边界条件，长结果截断可能丢证据，压缩边界还可能误删下一轮需要的信息。解决方式是优先保留最近原文、当前任务相关事实、来源 ID 和结构化状态；需要具体证据时重新检索原文，而不是只依赖摘要。

项目中压缩风险主要有三类。第一是摘要丢失细节：比如用户提到"版本 2.3.5 有个 bug"，摘要后可能只剩"早期版本有问题"，丢失具体版本号。第二是截断破坏完整性：工具返回的检索上下文截断后，可能保留了无关开头、丢失了关键结论。第三是过度压缩导致指代不清：用户说"按照刚才的方案"，如果"刚才的方案"已被摘要，模型可能无法理解具体指什么。

缓解措施是优先保留最近 3 轮完整对话，保留结构化状态（活动笔记、任务列表、文档引用），保留证据来源 ID（chunk ID、文档 ID）而不只是文本片段。需要早期内容时，从 MySQL 历史记录重新加载或通过 chunk ID 重新检索原文。摘要失败有降级兜底，不能因摘要异常导致对话中断。

压缩效果应通过 Token、延迟、追问成功率和答案忠实度一起验证，不能只看 Token 是否降低。

我认为压缩不是为了压而压，而是为了在有限窗口内保留最有价值的信息。如果压缩后用户频繁需要重复早期信息，或者答案质量明显下降，说明压缩策略有问题，需要调整保留规则或者增加上下文预算。

**可能追问：**

- **如何验证压缩没有丢失关键信息？**：构造依赖早期内容的追问，断言模型能回忆或提示重新提供；统计追问成功率，压缩后不应显著下降。
- **摘要质量如何保证？**：用明确的摘要 Prompt（保留时间、实体、关键决策），人工抽样检查摘要是否保留核心信息；摘要失败降级到截断。
- **什么时候不应该压缩？**：当前任务强依赖早期上下文、用户明确引用早期内容、会话轮次很少（< 5 轮）时不应压缩。

**补充细节：**

保留优先级：最近 3 轮原文 > 结构化状态 > 证据来源 ID > 早期摘要 > 更早丢弃。

验证方法：长对话后压缩，追问早期内容（如"刚才提到的版本号是多少"），断言能回答或提示重新提供；对比压缩前后的追问成功率和答案质量。

## 七、后端工程高频题

### 56. Redis 缓存穿透、击穿、雪崩分别是什么？项目怎么处理？

我觉得这三个问题的本质都是缓存失效导致大量请求直接打到后端，但原因和解决方式不同。

穿透是查询大量不存在的 Key，每次都穿透到后端；击穿是热点 Key 过期瞬间并发查询；雪崩是大量 Key 同时过期或 Redis 故障。

项目中 Redis 主要缓存 Supervisor 规划和 Query 扩展结果。穿透风险：如果用户故意查询不存在的问题，每次都穿透到模型调用，增加成本。处理方式是缓存空结果并设置短 TTL（如 1 分钟），避免相同无效查询反复调用模型。击穿风险：热点问题缓存过期瞬间，大量并发请求同时调用模型。处理方式是用 SETNX 获取分布式锁，只有一个请求去调用模型，其他请求等待或返回降级结果。雪崩风险：大量缓存同时过期或 Redis 故障，所有请求穿透到模型。处理方式是给 TTL 加随机抖动（如 10 分钟 ± 1 分钟），避免集中过期；Redis 故障时降级直接调用模型。

项目已有空值标记、短锁和随机 TTL。缓存数据必须可重建，写操作还要明确更新或删除顺序。Redis 故障时，规划和 Query 扩展可以直接重新计算，业务事实仍从 MySQL 读取。

我认为缓存不是万能的，不能把所有问题都指望缓存解决。如果热点问题本身计算就很贵，除了缓存还要考虑限流和降级；如果 Redis 经常故障，说明容量或配置有问题，要从根本上解决。

**可能追问：**

- **SETNX 锁如何实现？**：`SETNX key lock_value EX 10`，获取锁的请求计算结果并写缓存，其他请求等待或快速失败；释放锁时检查 lock_value 防误删。
- **空值 TTL 为什么要短？**：避免长时间缓存无效数据；如果业务数据可能新增，短 TTL 可以较快感知变化。
- **Redis 完全不可用怎么办？**：规划和扩展直接调用模型，业务数据从 MySQL 读取；监控 Redis 可用性并告警，尽快恢复。

**补充细节：**

缓存更新策略：先更新 MySQL，再删除 Redis 缓存（Cache-Aside）；下次查询时重建缓存。不采用先删缓存再更新库，避免并发读到旧数据又回写缓存。

验证方法：并发查询不存在的 Key，断言空值缓存生效、模型调用次数受限；热点 Key 过期瞬间并发请求，断言只有一个请求调用模型；关闭 Redis，断言降级到直接计算且功能可用。

### 57. MySQL 事务和异步任务怎么配合？

我觉得这个问题的核心是：本地事务只覆盖 MySQL 内部的文档、chunk 和状态写入。事务提交后再启动或投递向量化任务，避免异步线程读取到未提交数据。异步执行使用有界线程池，并记录任务状态、重试次数和错误原因。

项目中文档上传流程是：开启 MySQL 事务 → 插入文档记录 → 插入 chunk 记录 → 更新 BM25 索引 → 提交事务 → 投递异步向量化任务。如果在事务内就投递异步任务，异步线程可能立即查询文档，但事务还未提交，导致查不到数据或读到不一致状态。因此必须先提交事务，再投递任务。

异步任务通过 `CompletableFuture` 或线程池执行，使用有界队列（如容量 1000）防止任务堆积。任务记录文档 ID、批次、状态（pending/processing/completed/failed）、重试次数和错误原因到数据库。失败任务按指数退避重试，超过最大次数（如 3 次）标记为 failed。

如果要支持进程重启后的可靠投递，生产化应使用任务表或消息队列，而不是只依赖内存 `CompletableFuture`。任务领取要有租约、状态锁或乐观锁，防止多个 Worker 重复处理。

我认为事务和异步任务的配合关键是明确边界：事务内保证 ACID，事务外通过状态机和补偿保证最终一致。不要试图用一个大事务把所有操作都包进去，那样会导致锁等待时间长、回滚代价高。

**可能追问：**

- **事务内投递任务有什么问题？**：异步线程可能读到未提交数据（脏读），或者事务回滚但任务已执行，导致不一致。
- **如何保证任务不丢失？**：进程内异步任务，进程崩溃会丢失；生产化应持久化任务到任务表或消息队列（如 RabbitMQ、Kafka），Worker 从队列消费。
- **任务重复执行怎么办？**：用稳定业务键（文档 ID + chunk ID）保证幂等，Chroma 写入时相同 ID 覆盖而非新增。

**补充细节：**

事务和异步配合模式：
```
try {
    beginTransaction()
    insert document
    insert chunks
    commitTransaction()
    // 事务已提交，异步任务可以安全查询
    submitAsyncTask(documentId)
} catch {
    rollbackTransaction()
    // 不投递任务
}
```

验证方法：事务内抛异常，断言异步任务未执行；事务提交后投递任务，断言任务能查询到文档；进程重启前任务未完成，重启后检查任务状态和补偿机制。

### 58. 如何设计文档上传幂等？

我觉得幂等设计要从三个层面考虑：业务层去重、数据库约束兜底、向量存储幂等写入。

项目中幂等设计分三层。第一层是业务去重：计算文件 MD5，查询 `user_id + md5` 是否存在，存在直接返回已有文档 ID。第二层是数据库约束：`users_documents` 表建立 `UNIQUE KEY uk_user_md5 (user_id, md5)`，并发插入时数据库保证只有一个成功，其他返回唯一约束冲突。第三层是向量幂等：Chroma 写入时用 `chunk_id` 作为向量 ID，相同 ID 多次写入会覆盖而非新增，保证重试不产生重复向量。

如果需要接口级幂等，还应引入客户端 `Idempotency-Key` 并保存请求结果。客户端生成唯一 Key（如 UUID），首次请求保存 Key 和结果到 Redis（TTL 24 小时），重复请求直接返回缓存结果。这样即使文件不同但请求重复（如网络重试），也能保证幂等。

MD5 不是万能幂等键，文件内容相同但解析版本或 embedding 模型变化时应重新构建索引。生产化应把 parser_version 和 embedding_model 纳入幂等判断。

我认为幂等不是一次性设计好就完事了，而是要考虑业务演进。比如初期只用 MD5，后来发现模型升级后需要重建，就要增加版本字段；再后来发现需要支持接口重试，就要增加 Idempotency-Key。每一层幂等都有明确的保护范围，不能指望一个机制解决所有问题。

**可能追问：**

- **并发上传如何处理？**：数据库唯一约束兜底，冲突的请求返回错误或查询已有文档返回；前端可以重试或提示"文件已存在"。
- **Idempotency-Key 如何使用？**：客户端生成 UUID 放 Header，服务端首次处理后缓存结果，重复请求直接返回；Key 过期后视为新请求。
- **模型升级后如何重建？**：扫描所有文档，embedding_model 不匹配的标记待重建，异步重新生成向量并覆盖 Chroma。

**补充细节：**

幂等流程：
```
1. 计算 MD5
2. 查询 user_id + md5 → 存在返回
3. 插入文档（唯一约束保护）→ 冲突查询返回
4. 异步向量化（chunk_id 幂等）
5. 重试时跳过已有向量
```

验证方法：并发上传相同文件，断言只有一条记录；重复上传，断言快速返回不重复解析；异步失败重试，断言向量不重复。

### 59. SSE 和 WebSocket 怎么选？SSE 断线怎么办？

我觉得这个选择的核心是看通信模式：如果主要是服务端单向推送，SSE 更简单；如果需要双向频繁通信，WebSocket 更合适。

我认为 SSE 的核心优势是简单可靠，但要处理好断线场景。不能假设连接一直在，也不能把 SSE 当成唯一的状态通道。持久化是根本，推送只是体验优化。

项目中选择 SSE 因为问答流程是单向推送：服务端生成事件（如 `thinking`、`tool_call`、`response`、`done`），客户端只需接收展示。SSE 基于 HTTP，复用现有鉴权和负载均衡，浏览器原生支持 `EventSource`，断线后自动重连。如果用 WebSocket，需要处理握手、心跳、状态同步和连接池管理，复杂度更高。

SSE 连接断开不能丢任务状态。任务和 trace 要先持久化，客户端断线后通过 task 查询接口获取最终状态或重新订阅；事件应带任务 ID 和可去重信息。服务端要处理心跳、超时、客户端断开和后台任务取消，避免连接没了任务还无限消耗模型资源。

项目中每个 SSE 事件包含 `taskId`、`type`（thinking/tool_call/response/done/error）、`data` 和时间戳。客户端断线时服务端检测到连接关闭，停止发送事件但任务继续执行；任务完成后状态和结果保存到数据库。客户端重连后可以查询任务状态，决定是继续等待还是重新订阅。

**可能追问：**

- **SSE 如何检测客户端断线？**：发送事件时捕获 IOException，或定期发送心跳事件（如 `:ping`），写入失败说明断线。
- **任务执行中客户端断线怎么办？**：任务继续执行到完成或超时，状态保存到数据库；客户端重连后查询状态并展示结果。
- **什么场景适合 WebSocket？**：需要客户端频繁发送消息（如实时协作、游戏、双向聊天）、需要服务端主动推送且延迟要求极低的场景。

**补充细节：**

SSE 事件格式：
```
event: tool_call
data: {"taskId":"123","tool":"ragSearch","status":"running"}

event: response
data: {"taskId":"123","content":"根据检索结果..."}

event: done
data: {"taskId":"123","status":"completed"}
```

验证方法：客户端断线后任务继续执行，重连后能查询到完成状态；服务端检测断线并停止推送；多次断线重连不影响任务执行。

### 60. Java 线程池如何设置？

我觉得线程池设置的核心是：先区分 CPU 密集和 IO 密集，再结合 embedding、数据库连接池和外部模型的容量设置并发。必须使用有界队列和明确拒绝策略，监控活跃线程、队列长度、拒绝次数和任务耗时。

项目中有三类线程池。文档向量化线程池用于异步 embedding 和 Chroma 写入，属于 IO 密集，核心线程 10、最大 20、队列 1000、拒绝策略 `CallerRunsPolicy`（降级到调用线程执行）。评估线程池用于批量评估任务，核心 5、最大 10、队列 500。Agent 后台任务线程池用于 Supervisor-Worker 执行，核心 5、最大 15、队列 200。

文档向量化、评估和 Agent 后台任务最好使用隔离线程池，避免评估任务挤占在线问答资源。直接 `new Thread` 无法统一限流、回收、监控和优雅停机。线程池命名便于排查（如 `document-vector-pool-1`），并注册到监控系统上报线程数、队列长度和拒绝次数。

核心线程数参考：IO 密集 = CPU 核心数 × 2~4，CPU 密集 = CPU 核心数 + 1。最大线程数不应过大，避免上下文切换开销。队列长度根据任务积压容忍度设置，过大导致任务等待时间长、OOM 风险，过小导致频繁拒绝。

我认为线程池配置不是一次性的，要根据实际运行情况调整。初期可以保守设置，通过监控观察队列积压、拒绝率、任务耗时，再逐步优化。关键是要有监控和告警，不能设置完就不管了。

**可能追问：**

- **拒绝策略如何选择？**：`CallerRunsPolicy` 降级到调用线程、适合可以慢但不能丢的任务；`AbortPolicy` 抛异常、适合需要感知拒绝的场景；`DiscardPolicy` 静默丢弃、一般不推荐。
- **如何监控线程池？**：暴露 `activeCount`、`queueSize`、`completedTaskCount`、`rejectedCount` 指标到 Prometheus，设置队列积压和拒绝率告警。
- **为什么不用无界队列？**：无界队列可能导致 OOM，任务无限积压也无法及时发现问题；有界队列能暴露容量瓶颈。

**补充细节：**

线程池配置示例：
```java
ThreadPoolExecutor pool = new ThreadPoolExecutor(
    10, // corePoolSize
    20, // maximumPoolSize
    60, TimeUnit.SECONDS, // keepAliveTime
    new LinkedBlockingQueue<>(1000), // 有界队列
    new ThreadFactoryBuilder().setNameFormat("doc-vector-%d").build(),
    new ThreadPoolExecutor.CallerRunsPolicy() // 拒绝策略
);
```

验证方法：提交大量任务，观察活跃线程数、队列长度变化；任务超过容量时断言触发拒绝策略；优雅停机时等待任务完成或超时强制终止。

### 61. 如何定位一次慢请求？

我觉得定位慢请求的关键是分段计时：先用 traceId 拆分排队、数据库、Redis、BM25、向量、rerank、LLM 和 SSE 各阶段耗时，再看 Token、重试和线程池队列。慢 SQL 看执行计划，缓存看命中率，模型调用看上游延迟和输入长度。

项目中每次请求生成唯一 traceId，记录到日志和 trace 表。慢请求排查步骤：第一步查看 trace，按阶段拆分耗时（如检索 500ms、LLM 3000ms、总计 4000ms）；第二步定位最慢阶段，检索慢看 BM25 和向量查询日志，LLM 慢看输入 Token 和模型响应时间；第三步检查是否有重试、降级或队列等待。

常见慢请求原因：BM25 索引未优化、向量库查询超时、rerank 批量过大、上下文 Token 过多（历史未压缩）、模型调用排队、SSE 推送阻塞。优化方向：BM25 索引优化、向量查询并行、rerank 限流、上下文压缩、模型调用异步、线程池扩容。

优化后用相同数据集、模型和并发条件比较 P50、P95、P99、错误率和成本。没有分段指标时先补观测，不能凭感觉改参数。

我认为性能优化最怕的是盲目猜测。看到慢就加缓存、加并发、加机器，结果可能根本不是瓶颈所在。必须先有数据，知道瓶颈在哪里，再针对性优化，最后验证效果。

**可能追问：**

- **如何拆分各阶段耗时？**：在关键节点记录时间戳（开始/结束），计算差值；用 AOP 或拦截器自动记录方法耗时。
- **LLM 调用慢怎么优化？**：压缩上下文减少输入 Token、使用流式响应提前返回、缓存高频问题结果、升级模型或增加并发配额。
- **数据库慢查询怎么排查？**：开启慢查询日志（如 > 1s），分析执行计划（EXPLAIN），检查索引覆盖、回表、全表扫描。

**补充细节：**

Trace 记录示例：
```
traceId: abc123
├─ 鉴权: 50ms
├─ 检索: 800ms
│  ├─ 向量查询: 400ms
│  ├─ BM25: 300ms
│  └─ RRF融合: 100ms
├─ LLM调用: 3000ms (输入2000 tokens)
└─ SSE推送: 200ms
总计: 4050ms
```

验证方法：构造慢请求场景（大上下文、复杂检索），查看 trace 定位瓶颈；优化后对比 P95 延迟下降。

### 62. MySQL 分页和索引怎么设计？

我觉得索引设计的核心是围绕真实过滤和排序场景，不能凭感觉建索引。例如用户文档列表可考虑 `user_id + created_at + id` 联合索引；任务列表要覆盖用户、状态和稳定排序字段。大 offset 分页会扫描并丢弃大量数据，适合改成基于最后 ID/时间的游标分页。

项目中文档列表查询 `WHERE user_id = ? ORDER BY created_at DESC, id DESC LIMIT 20`，建立 `idx_user_created (user_id, created_at DESC, id DESC)` 联合索引，覆盖过滤和排序。任务列表查询 `WHERE user_id = ? AND status IN (?) ORDER BY created_at DESC LIMIT 20`，建立 `idx_user_status_created (user_id, status, created_at DESC)`。

大 offset 分页问题：`LIMIT 10000, 20` 需要扫描前 10020 行再丢弃 10000 行，越往后越慢。改成游标分页：`WHERE user_id = ? AND (created_at, id) < (?, ?) ORDER BY created_at DESC, id DESC LIMIT 20`，利用索引直接定位起始位置，性能稳定。

排序必须稳定，时间相同用 ID 做第二排序键，否则翻页可能漏数据或重复。最终以 `EXPLAIN` 和实际数据分布验证，不背固定索引结论。

我认为索引不是越多越好，每个索引都有维护成本。应该根据高频查询建立索引，低频查询可以容忍稍慢。而且索引设计要考虑数据分布，如果某个字段区分度很低（比如性别只有两个值），单独建索引意义不大。

**可能追问：**

- **为什么要用联合索引而不是单列索引？**：单列索引只能优化一个条件，联合索引可以同时优化过滤、排序和避免回表，提升查询效率。
- **游标分页有什么限制？**：不能跳页（如直接跳到第 100 页），只能顺序翻页；适合流式加载（下拉刷新）场景。
- **如何验证索引生效？**：用 `EXPLAIN` 查看执行计划，检查 `type` 是否为 ref/range、`key` 是否使用预期索引、`rows` 扫描行数是否合理。

**补充细节：**

索引设计原则：
1. 最左前缀匹配：联合索引 (a, b, c) 可以优化 a、a+b、a+b+c 查询，但不能优化 b 或 c 单独查询
2. 区分度高的列放前面：如 user_id 区分度高于 status
3. 覆盖索引：查询列都在索引中，避免回表

游标分页示例：
```sql
-- 首页
SELECT * FROM docs WHERE user_id = 1 
ORDER BY created_at DESC, id DESC LIMIT 20

-- 下一页（上一页最后记录：created_at=2024-01-01, id=100）
SELECT * FROM docs WHERE user_id = 1 
  AND (created_at < '2024-01-01' OR (created_at = '2024-01-01' AND id < 100))
ORDER BY created_at DESC, id DESC LIMIT 20
```

验证方法：大 offset 分页测试性能（LIMIT 10000, 20），对比游标分页性能；检查 EXPLAIN 执行计划确认索引使用。

## 八、测试开发与质量保障

### 63. 这个项目如何做测试分层？

测试分层的核心是把质量分成协议正确性、系统可靠性和 AI 效果三个层面。协议层包括 Controller 参数、鉴权、错误码、JSON 结构、SSE 事件顺序和幂等；系统层包括 MySQL/Redis/Chroma 故障、异步任务、并发重复上传、重试、超时、线程池和数据一致性；效果层包括检索是否召回证据、回答是否忠实、是否相关、上下文精确度和召回率。

测试分层上，纯逻辑用单元测试，例如 RRF、切片、MD5、JSON 清洗、预算判断和状态转换；Repository、事务、Redis 用集成测试；API 和 SSE 用契约测试；上传、异步向量化、检索和问答用少量端到端测试。模型调用不能全部放在单测里，否则结果随机、耗时高、成本不可控；单测 Mock 模型和工具，固定数据集上的真实模型评估放到隔离回归任务。

RAG 回归集不能随机写几条问题，而要覆盖术语、同义表达、错误码、上下文指代、多跳问题、空结果、文档缺失和高幻觉风险问题。每条样本记录预期答案或证据范围、文档版本、检索配置和模型版本。发现错误时先查看 trace：关键证据没有召回，是检索问题；证据召回但答案没用，是生成问题；证据过时或错误，是知识库问题。

最终还要做故障注入和恢复测试。例如 Chroma 断开、embedding 超时、批量部分失败、MySQL 提交后进程退出、补偿任务重复执行、SSE 中途断线。质量标准不是”接口返回 200”，而是错误被正确分类、状态闭环、数据不重复、任务可以恢复或明确失败。

我认为测试分层不是为了凑覆盖率，而是为了快速定位问题。单元测试快，能立即发现逻辑错误；集成测试覆盖模块交互，能发现集成问题；端到端测试少但关键，验证完整链路。每一层都有明确的职责，不要混在一起。

**可能追问：**

- **如何 Mock 模型调用？**：单测中用 Mockito mock LLM 服务，返回固定响应；集成测试用测试桩返回预设 JSON；回归测试用真实模型但固定数据集。
- **如何测试 SSE？**：用支持流式的 HTTP 客户端逐事件接收，断言事件类型、顺序和字段；测试断线重连、超时和并发订阅。
- **测试覆盖率要求多少？**：单元测试行覆盖 > 70%，核心逻辑 > 90%；集成测试覆盖主要业务流程；端到端测试覆盖关键用户路径。

**补充细节：**

测试金字塔：单元测试（快速、大量、Mock 外部依赖）→ 集成测试（中等、覆盖模块交互）→ 端到端测试（慢、少量、覆盖关键路径）。

验证方法：CI 中自动运行单元和集成测试，回归测试定期或发布前运行；故障注入测试手动或自动化执行，验证系统韧性。

### 64. RAG 最难测什么？

我觉得 RAG 最难测的是答案不是固定字符串，但系统仍需要可判断的质量标准。解决方式是保存完整 trace，把问题拆成检索质量、生成忠实度、答案相关性、延迟、Token 和降级率，再用固定数据集批量比较。

测试最终答案前，先测试检索结果。如果关键证据没召回，是检索问题；证据已召回但答案没有使用，是生成或 Prompt 问题；证据本身错误，则是知识库问题。

项目中 RAG 测试分三层。第一层测检索：固定问题集，断言关键 chunk 被召回（chunk ID 匹配），统计 Recall@K。第二层测生成：给定固定上下文，断言答案包含关键事实、不包含幻觉内容。第三层测端到端：完整问答流程，用 RAGAS 四项指标评估质量，保存 trace 便于分析。

难点还包括：答案表述多样但语义相同、部分正确部分错误的答案如何评分、如何区分模型能力问题和系统问题。因此需要 LLM judge + 规则评分 + 人工抽样组合，不能只依赖单一方式。

我认为测试 RAG 不能只看最终答案对不对，而要看整个链路每一步是否符合预期。检索没召回正确文档，后面再怎么生成都是错的；检索对了但生成错了，说明 Prompt 或模型有问题；检索和生成都对但用户不满意，可能是知识库本身的问题。分层测试才能快速定位问题根源。

**可能追问：**

- **如何测试检索召回？**：人工标注每个问题的关键 chunk ID，检索后断言这些 ID 在 TopK 结果中；统计召回率和排名位置。
- **如何测试幻觉？**：给定上下文和问题，模型生成答案，用 Faithfulness 评估答案中每个事实是否被上下文支持；构造故意不相关的上下文，断言模型不生成答案或明确说"无相关信息"。
- **如何区分检索问题和生成问题？**：检索返回正确证据但答案错误 → 生成问题；检索未返回关键证据 → 检索问题；证据和答案都对但不相关 → Prompt 或理解问题。

**补充细节：**

测试用例设计：
- 精确术语匹配："NullPointerException 如何处理" → 应召回包含该异常的文档
- 同义改写："怎么解决空指针" → 应召回相同文档
- 多跳问题："A 接口调用 B 服务失败怎么办" → 需要召回 A 接口文档和 B 服务文档
- 空结果："不存在的概念" → 应返回空或明确说明无相关信息

验证方法：准备 50+ 标注样本，每个样本包含问题、预期召回 chunk、预期答案要点；批量测试并生成报告，分析失败样本定位问题。

### 65. 如何设计 RAG 回归集？

我觉得回归集设计的关键是按场景分层：术语精确匹配、同义改写、代码和异常栈、多跳问题、上下文指代、空结果、文档缺失和容易产生幻觉的边界问题。每条样本保存预期答案、证据范围或关键事实。

项目中回归集设计包含以下类型：精确术语（"NullPointerException"、"HTTP 404"、"版本 2.3.5"）、同义改写（"如何解决" vs "怎么处理"）、技术栈特定（Java 代码、异常栈、配置文件）、多跳推理（"A 调用 B 失败"需要 A 和 B 两份文档）、上下文指代（"按照刚才的方案"）、边界情况（不存在的概念、过时文档、冲突信息）。

每条样本记录：问题文本、预期召回的 chunk ID 列表、预期答案要点或完整答案、文档版本、检索配置（TopK、是否 rerank）、模型版本。执行回归时固定这些配置，对比实际召回和答案与预期的差异。

固定测试集不等于永远不更新。新增线上失败样本时要脱敏后加入回归集，并记录数据集版本，避免测试集变化导致指标无法比较。回归集应该持续积累，从初始 20 条逐步扩展到 100+ 条，覆盖各类典型场景和历史失败案例。

我认为好的回归集不是随便写几个问题，而是要覆盖真实用户会问的各种情况。精确术语、同义改写、多跳推理、边界情况，每一类都要有代表性样本。而且回归集要随着系统演进持续更新，每次发现新的失败案例，都要加进去，这样才能真正防止质量退化。

**可能追问：**

- **如何标注预期答案？**：人工根据文档内容编写预期答案要点，或标注必须包含/不能包含的关键词；完整答案标注成本高，关键要点更实用。
- **回归集多大合适？**：初期 20-50 条覆盖主要场景，成熟后 100-200 条；过小覆盖不足，过大运行耗时长、维护成本高。
- **如何处理过时样本？**：定期人工审查，文档更新或业务变化后更新预期答案；标记样本版本和有效期。

**补充细节：**

回归集示例：
```json
{
  "id": "test-001",
  "question": "NullPointerException 如何排查",
  "expectedChunks": ["chunk-123", "chunk-456"],
  "expectedKeywords": ["堆栈信息", "null检查", "日志"],
  "avoidKeywords": ["数据库", "网络"],
  "documentVersion": "v1.2",
  "modelVersion": "gpt-4",
  "config": {"topK": 5, "useRerank": true}
}
```

验证方法：定期运行回归集，生成对比报告（召回率、答案质量、指标变化）；新功能上线前必须通过回归测试；失败样本加入回归集防止重复。

### 66. 如何测试模型返回非法 JSON？

我觉得测试 Agent 的关键是把模型当成一个有不确定输出的外部依赖。协议测试覆盖纯 JSON、Markdown 代码块、中文前缀、截断数组、字段缺失、类型错误和未知字段，验证清洗、解析、回退和错误记录。语义测试则验证规划是否覆盖目标、依赖是否合理、工具是否符合任务和权限。

轨迹测试把模型响应和工具结果 Mock 成固定序列，覆盖：规划成功后正常完成；规划失败后降级单 Agent；工具临时失败后有限重试；连续无进展触发反思；反思后换工具；预算超限终止；暂停后恢复；一个子任务失败但其他任务继续并最终标记缺失。每轮断言 AgentState、pending steps、tool history、任务状态、trace 和终止原因。

还要测幂等和安全边界：同一写工具重复执行不能重复创建；模型传入其他用户的文档 ID 必须被拒绝；高风险写操作需要确认。最终文本可以做宽松断言，但状态、工具次数、权限和终止事件应该做严格断言，因为这些是后端必须稳定保证的协议。

项目中协议测试用固定非法 JSON 字符串（带 Markdown 代码块、中文前缀等），调用 `cleanLlmJson` 和解析逻辑，断言能正确清洗或降级。轨迹测试 Mock LLM 返回固定规划和决策序列，Mock 工具返回固定结果，执行完整 Agent 循环并断言每轮状态。

我认为测试 Agent 不能只看最终答案，因为答案是自然语言，很难严格断言。但是状态转换、工具调用次数、权限检查、终止原因这些都是确定性的，必须严格测试。这样即使模型输出变化，我们也能保证系统行为是可控的。

**可能追问：**

- **如何 Mock 模型返回？**：单测中注入 Mock LLM 服务，预设返回内容（合法/非法 JSON、空、超长）；集成测试用测试桩按序列返回。
- **轨迹测试如何设计？**：定义场景（如"工具失败重试后成功"），Mock 模型返回工具调用、Mock 工具第一次失败第二次成功，断言重试发生且最终成功。
- **如何测试越权？**：Mock 模型返回包含其他用户文档 ID 的工具调用，断言被权限检查拒绝并记录审计日志。

**补充细节：**

协议测试用例：
```java
// 带 Markdown 代码块
String json = "```json\n[{\"label\":\"task1\"}]\n```";
List<SubTask> tasks = supervisor.parseAndClean(json);
assert tasks.size() == 1;

// 中文前缀
String json = "好的，规划如下：[{\"label\":\"task1\"}]";
// 应能提取出 JSON 数组

// 截断
String json = "[{\"label\":\"task1\"}, {\"label\":\"tas";
// 应降级返回空列表或部分解析
```

验证方法：准备各类非法 JSON，断言清洗和解析逻辑；轨迹测试覆盖正常和异常路径，断言状态机转换正确。

### 67. 如何做 Agent 轨迹测试？

我觉得轨迹测试的核心是把模型决策和工具结果 Mock 成固定轨迹，覆盖规划成功、工具成功、临时失败重试、连续无进展触发反思、预算超限、暂停恢复和子任务部分失败。

断言每轮状态、工具顺序、最大调用次数、终止原因、trace 完整性以及 SSE 是否发送且只发送一个终止事件。轨迹测试比只断言最终自然语言更容易定位问题。

项目中轨迹测试场景包括：正常完成（规划 → 检索 → 生成 → done）、规划失败降级（非法 JSON → 清洗失败 → 单 Agent）、工具重试（检索超时 → 重试成功）、预算超限（达到最大轮次 → 终止并记录原因）、反思触发（连续两轮无进展 → 反思 → 换工具）、部分失败（子任务 A 成功、B 失败 → 合成时标记 B 未完成）。

每个场景 Mock 完整交互序列，断言每轮的 AgentState（目标、工具历史、观察结果）、任务状态变化、trace 记录和最终输出。不只断言成功/失败，还要断言中间状态转换和资源清理。

我认为轨迹测试的价值在于让 Agent 行为可预测。模型输出可能变化，但是给定相同的模型输出和工具结果，系统的状态转换、重试逻辑、终止条件应该是确定的。这样即使模型升级，只要协议不变，系统行为就是稳定的。

**可能追问：**

- **如何 Mock 完整轨迹？**：定义场景脚本，按顺序返回模型决策和工具结果；用队列或状态机控制 Mock 行为，每次调用返回预定内容。
- **如何验证反思触发？**：Mock 连续两轮工具返回空结果或无新信息，断言第三轮触发 ReflectionService 且 trace 记录反思事件。
- **并发轨迹如何测试？**：Mock 多个子任务并行执行，断言工具调用顺序符合依赖关系、共享状态隔离、最终合成正确。

**补充细节：**

轨迹示例（工具重试成功）：
```
轮次1: 规划 → 返回 [task1]
轮次2: 调用检索工具 → Mock 超时
轮次3: 重试检索工具 → Mock 成功返回结果
轮次4: 调用生成 → 返回答案
断言: 工具调用2次、重试记录存在、最终状态completed
```

验证方法：准备 10+ 轨迹场景，每个场景 Mock 完整交互，运行后断言状态机转换、trace 完整性、资源释放；失败场景不应泄漏资源或无限循环。

### 68. 如何测试最终一致性？

我觉得测试最终一致性的核心是构造 MySQL 失败、embedding 超时、Chroma 失败、批量部分失败、提交后进程崩溃、重复补偿等场景。每个场景检查数据库状态、重试次数、错误原因、向量数量、是否重复和最终可恢复性。

还要验证”永远失败”路径：超过最大重试后进入人工处理或死信，不能无限重试。

我认为测试最终一致性不是验证”数据一定一致”，而是验证”出问题后能自动恢复”。短暂不一致是允许的，但必须有补偿机制、有状态标记、有最大重试限制。测试要覆盖各种异常场景，确保系统不会卡在中间状态，也不会无限重试。

项目中最终一致性测试覆盖以下场景：正常流程（MySQL 成功 → 异步成功 → 状态 completed）、Chroma 不可用（MySQL 成功 → 向量失败 → 状态 vector_failed → 补偿恢复）、批量部分失败（10 个 chunk，1 个超长 → 批量失败 → 单条回退 → 9 个成功 1 个失败）、进程崩溃（事务提交后、异步前崩溃 → 重启后补偿任务处理）、重复补偿（同一文档触发多次补偿 → 幂等保证不重复向量）。

每个场景验证：MySQL 数据完整性、BM25 索引存在、Chroma 向量数量正确、文档状态准确、重试记录完整、补偿任务执行且幂等。最终验证检索能返回文档（降级到 BM25 或完整混合检索）。

**可能追问：**

- **如何模拟进程崩溃？**：在关键点（事务提交后、异步投递前）抛异常或强制退出，重启后检查状态和补偿机制。
- **如何验证幂等？**：同一文档多次补偿，查询 Chroma 向量数量不变、MySQL 无重复记录、状态最终为 completed。
- **补偿任务如何触发？**：定时任务扫描 vector_failed 或超时 pending 状态文档，重新投递；测试时手动触发或减少扫描间隔。

**补充细节：**

测试步骤示例（Chroma 失败恢复）：
```
1. 关闭 Chroma 服务
2. 上传文档
3. 断言 MySQL 和 BM25 成功，状态 vector_failed
4. 检索文档，断言降级到 BM25
5. 恢复 Chroma 服务
6. 触发补偿任务
7. 断言状态变为 completed，Chroma 有向量
8. 检索文档，断言走混合检索
```

验证方法：自动化故障注入测试，覆盖各类失败场景；每个场景检查数据一致性、状态流转和最终可恢复性。

### 69. 如何做并发测试？

我觉得并发测试的核心是验证：文档重复上传最终只有一条业务记录和一份有效索引；同一任务重复暂停/恢复不能破坏状态；同一缓存 Key 并发 miss 不能造成数据库或模型请求风暴。

逐步增加并发，观察 QPS、P95/P99、错误率、线程池队列、数据库连接池和外部限流；结束后核对数据，不只看 HTTP 200。

项目中并发测试覆盖：重复上传（100 并发上传相同文件 → 断言只有一条记录）、并发检索（1000 QPS 检索 → 观察延迟、错误率、降级率）、缓存击穿（热点 Key 过期瞬间 100 并发 → 断言只有一次模型调用）、线程池压测（提交超过队列容量任务 → 断言拒绝策略生效）、数据库连接池（并发查询超过连接数 → 断言等待或拒绝，无死锁）。

并发测试工具用 JMeter、Gatling 或自写压测脚本。测试后检查：数据一致性（无重复记录、状态正确）、资源释放（连接归还、线程回收）、错误日志（无死锁、无资源泄漏）、监控指标（CPU、内存、GC、慢查询）。

我认为并发测试不只是看系统能不能扛住压力，更重要的是看并发下数据会不会乱。比如并发上传相同文件，如果没有幂等保护，可能产生重复记录；缓存击穿时如果没有锁，可能瞬间打爆数据库。测试完一定要对账，确认数据没问题。

**可能追问：**

- **如何模拟并发上传？**：用 JMeter 或线程池并发调用上传接口，传入相同文件；断言只有一个请求成功创建，其他返回"已存在"或冲突。
- **缓存击穿如何验证？**：并发请求同一个刚过期的 Key，在处理逻辑中打点记录模型调用次数；断言只有一次模型调用、其他请求等待或快速失败。
- **压测后如何验证数据一致性？**：查询数据库记录数、Chroma 向量数、BM25 索引；对比压测前后数据，检查是否有重复、遗漏或脏数据。

**补充细节：**

并发测试指标：
- 吞吐量：QPS（每秒请求数）
- 延迟：P50、P95、P99、max
- 错误率：HTTP 非 200、业务错误、超时
- 资源：CPU、内存、线程数、连接数、队列长度
- 降级率：缓存 miss、检索降级、模型限流

验证方法：逐步增加并发（10 → 50 → 100 → 500），记录各阶段指标；达到瓶颈后分析原因（CPU、数据库、外部服务）；压测后检查数据完整性和一致性。

### 70. 如何测试 SSE？

我觉得测试 SSE 的关键是逐事件检查响应头、事件类型、字段和顺序。正常流程应有任务创建、进度或工具事件、响应事件和 done；异常流程应有 error，不能同时 done 和 error。

测试慢客户端、断网、服务端超时、重复订阅、事件去重和客户端主动断开。必须使用流式客户端逐事件断言，不能等完整响应后才测试。

项目中 SSE 测试覆盖：正常流程（连接 → thinking → tool_call → response → done）、异常流程（连接 → error 且 HTTP 200）、断线重连（连接中途断开 → 重连后查询任务状态）、超时（长时间无响应 → 服务端发送心跳或超时关闭）、并发订阅（同一任务多个客户端订阅 → 都能收到事件）。

测试用流式 HTTP 客户端（如 OkHttp 支持 SSE、或原生 EventSource），逐事件读取并断言。检查响应头 `Content-Type: text/event-stream`、`Cache-Control: no-cache`；事件格式 `event: type\ndata: json\n\n`；事件顺序和完整性。

我认为测试 SSE 不能只测正常流程，断线重连、超时、并发订阅这些异常场景更重要。因为 SSE 本质上是长连接，网络不稳定时很容易断开。测试要验证断开后任务不会丢失，重连后能恢复状态，这样用户体验才能有保障。

**可能追问：**

- **如何测试断线重连？**：连接后收到几个事件，主动关闭连接，重新连接并查询任务状态，断言能获取完整结果。
- **如何模拟慢客户端？**：客户端接收事件后 sleep 延迟读取，观察服务端缓冲区和超时处理。
- **如何验证事件顺序？**：记录接收到的事件类型序列，断言符合预期顺序（如 thinking 必须在 response 之前、done 必须最后）。

**补充细节：**

SSE 事件格式：
```
HTTP/1.1 200 OK
Content-Type: text/event-stream
Cache-Control: no-cache

event: thinking
data: {"taskId":"123","content":"正在检索..."}

event: tool_call
data: {"taskId":"123","tool":"ragSearch","status":"running"}

event: response
data: {"taskId":"123","content":"根据检索结果..."}

event: done
data: {"taskId":"123","status":"completed"}
```

验证方法：用流式客户端逐事件接收，断言响应头、事件类型、字段完整性、顺序正确；测试异常场景（断线、超时、并发）并验证行为符合预期。

### 71. 如何测性能？

我觉得性能测试的关键是把文档导入、关键词检索、混合检索、单 Agent 和多 Agent 问答分成不同场景，固定文档规模、Query 集、TopK、模型和并发，分别测冷缓存和热缓存。报告吞吐、P50/P95/P99、错误率、Token、队列和资源占用。

如果没有真实压测记录，只能准确说”采用了异步、缓存、批量和降级机制”，不能给出未经验证的提升百分比。

项目中性能测试场景包括：文档导入（100 个文档并发上传 → 观察 MySQL、BM25、异步任务耗时）、关键词检索（1000 QPS → P95 延迟、错误率）、混合检索（500 QPS，启用向量+BM25+rerank → P95、降级率）、单 Agent 问答（100 QPS → 端到端延迟、Token）、多 Agent 问答（50 QPS → 规划+执行+合成总耗时）。

测试前准备：固定知识库（1000 文档、10000 chunk）、固定问题集（100 个问题）、固定配置（TopK=5、模型版本）。测试时逐步增加并发，记录各阶段指标。冷缓存测试重启服务清空 Redis，热缓存测试预热后再压测。

报告包含：吞吐量（QPS）、延迟分布（P50/P95/P99/max）、错误率、Token 消耗、降级率、资源占用（CPU/内存/GC）、瓶颈分析（数据库/模型/网络）。

我认为性能测试不是为了追求数字好看，而是为了了解系统的真实能力和瓶颈。知道 P95 延迟是多少，才能给用户设定合理的预期；知道瓶颈在哪里，才能有针对性地优化。而且性能数字必须有依据，没有压测过就不要编造提升百分比。

**可能追问：**

- **冷缓存和热缓存有什么区别？**：冷缓存所有请求都 miss，需要计算或查询；热缓存高命中率，延迟更低。两者都要测，反映真实情况。
- **如何确定瓶颈？**：分段计时，观察哪个阶段耗时最长；监控资源占用，CPU 高是计算瓶颈，IO 等待高是存储瓶颈，队列堆积是下游限流。
- **性能优化后如何验证？**：相同场景、相同数据、相同并发下对比优化前后指标；P95 延迟降低、吞吐提升、错误率不增加才算有效优化。

**补充细节：**

性能基线示例：
- 关键词检索：P95 < 200ms，QPS 1000，错误率 < 0.1%
- 混合检索：P95 < 800ms，QPS 500，降级率 < 1%
- 单 Agent：P95 < 5s，QPS 100，Token 均值 2000

验证方法：用 JMeter/Gatling 压测，记录指标并生成报告；对比基线发现回归；优化后重新压测验证效果。

## 九、常见高压追问

### 72. 没有 ChromaDB，系统还能工作吗？

我觉得这个问题的答案是：能工作，但语义召回能力会下降。`HybridRetriever` 发现 Chroma 不可用后关闭向量路径；如果 BM25 可用，仍然可以处理异常名、类名和接口名等精确查询；BM25 查询异常时，对当前 query 使用 MySQL 关键词 fallback；如果没有任何 ranking，再走纯关键词检索。

因此这是可用性优先的降级，不是质量等价。系统应该记录降级原因、实际检索路径和结果数量，评估时单独统计 fallback 率和降级状态下的质量。面试中不要说”没有 Chroma 完全不受影响”，准确说法是”基本功能可用，但语义召回和答案质量可能下降”。

项目中降级检测：启动时健康检查 Chroma 连接，失败时标记不可用；检索时捕获 Chroma 异常，记录到 trace 并关闭向量路径；监控降级率指标并告警。降级后用户可见：前端显示”当前使用关键词检索，语义检索暂不可用”；trace 记录降级原因和实际路径。

恢复后自动切回：定期健康检查（如每 30 秒），Chroma 恢复后重新启用向量路径；不需要重启服务。

我认为降级不是为了掩盖问题，而是为了在故障时保持基本可用。用户能用关键词查到文档，总比完全不可用要好。但必须明确告知用户当前处于降级状态，并记录降级率用于监控告警，这样运维才能及时发现和处理问题。

**可能追问：**

- **降级到关键词后能回答什么问题？**：包含明确术语的问题（如”NullPointerException”、”版本 2.3.5”）仍能匹配；同义表达和语义相关问题召回效果差。
- **如何通知用户降级？**：前端根据 API 返回的降级标识显示提示；trace 记录降级信息便于排查；监控告警通知运维。
- **降级率多少算正常？**：正常应该 < 0.1%；超过 1% 说明 Chroma 不稳定，需要排查；接近 100% 说明 Chroma 完全不可用。

**补充细节：**

降级流程：
```
1. 尝试向量检索
2. 捕获异常 → 标记向量不可用
3. 继续 BM25 检索
4. BM25 也失败 → MySQL 关键词
5. 全部失败 → 返回空并记录
```

验证方法：关闭 Chroma 服务，发起检索请求，断言走 BM25 路径且返回结果；查看 trace 记录降级原因；恢复 Chroma，断言自动切回混合检索。

### 73. RAGAS 评估模型不稳定，结果还有意义吗？

我觉得这个问题要这样理解：因为很多生成质量无法完全靠字符串规则判断，LLM Judge 可以提供忠实度、相关性等语义层信号。但它不是绝对真值，可能受模型版本、Prompt、温度、上下文顺序和自身偏差影响。

所以我把它定位成回归和调优工具，而不是线上正确率统计。固定测试集、模型、Prompt、知识库版本和配置，重复运行并查看均值、方差和失败率；保留原子声明、上下文和原始输出，配合人工抽样和规则评分。这样更适合比较”方案 A 是否比方案 B 好”，不适合解释成”0.83 就是线上准确率 83%”。

项目中评估结果稳定性处理：同一样本运行 3-5 次，计算均值和标准差；标准差 > 0.1 标记为不稳定样本，单独分析；对比实验时确保两边样本、配置、运行次数一致。人工抽样 10-20% 样本验证 LLM judge 结果，计算一致率；不一致样本分析是评估 Prompt 问题还是模型理解偏差。

结合规则评分：空答案、超长延迟、Token 超预算用规则判断；语义质量用 LLM judge；两者结合给出综合评估，但保留子分数。

我认为 LLM 评估的价值不在于给出一个精确的分数，而在于提供一个相对稳定的比较基准。同样的评估器、同样的配置下，如果方案 A 比方案 B 分数高，这个相对关系是有参考意义的。但不能把评估分数当成线上准确率，那是两回事。

**可能追问：**

- **如何提高评估稳定性？**：固定温度（如 0）、固定 Prompt、使用更稳定的评估模型、多次运行取均值。
- **人工抽样怎么做？**：随机抽取 10-20% 样本，人工标注四项指标预期值，与 LLM judge 对比；一致率 > 0.7 认为评估可用。
- **评估成本如何控制？**：用较小较快的模型做评估（如 GPT-3.5）、批量并发调用、缓存评估结果避免重复评估。

**补充细节：**

评估稳定性报告示例：
```
样本ID: test-001
运行5次 Faithfulness: [0.85, 0.83, 0.87, 0.84, 0.86]
均值: 0.85, 标准差: 0.015 (稳定)

样本ID: test-002  
运行5次 Answer Relevancy: [0.6, 0.9, 0.7, 0.85, 0.65]
均值: 0.74, 标准差: 0.13 (不稳定，需人工复核)
```

验证方法：同一样本多次评估，统计方差；对比人工标注和 LLM judge 结果；不一致样本深入分析原因。

### 74. 这个项目还有哪些不足？
区分”代码已有能力”和”生产化设计”。

第一，长期记忆不能写成完整持久化记忆系统，目前更接近会话历史压缩和会话级上下文状态。第二，多 Agent 不能写成开放域自主智能体，准确说是 LLM 规划的受约束任务编排。第三，RAGAS 不能写成直接接入官方库，如果代码是参考指标后自研，就要如实说明。第四，数据一致性不能写成强一致或分布式事务，而应写最终一致性。第五，性能提升百分比必须有测试集或压测报告，否则只写采用了缓存、异步、批量和降级机制。

项目当前实现聚焦在核心问答链路、检索优化、Agent 编排和评估框架上。生产化不足包括：任务可靠投递（当前内存异步，应该用任务表或消息队列）、全局并发控制（当前单机线程池，应该增加租户级和全局限流）、完整监控告警（当前有 trace 和日志，缺少统一指标和告警规则）、安全加固（API 限流、敏感数据脱敏、审计日志完善）、文档版本管理（当前 MD5 去重，缺少 parser/embedding 版本化重建）。

我认为项目不足不是问题，关键是知道不足在哪里，为什么当前没做，将来如何补充。初期验证核心链路和效果更重要，生产化能力可以逐步补充；过早优化可能浪费在错误方向上。

**可能追问：**

- **如果有更多时间，优先做什么？**：第一优先补可靠任务投递和幂等，避免进程重启丢失任务；第二优先补规划质量评估和证据融合，提升多 Agent 效果；第三优先补统一监控告警。
- **为什么不一开始就做完整？**：初期验证核心链路和效果更重要，生产化能力可以逐步补充；过早优化可能浪费在错误方向上。
- **如何平衡功能和质量？**：核心功能优先保证可用和正确，周边能力根据实际需求逐步补充；定期技术债务清理，避免累积到无法维护。

**补充细节：**

不足清单：
- 长期记忆：无跨会话持久化
- 并行调度：当前顺序执行，未实现真正并行
- 任务可靠性：依赖进程内异步，无持久化队列
- 版本管理：无 parser/embedding 版本化
- 监控告警：有 trace 和日志，缺统一指标
- 安全审计：有基本权限校验，缺完整审计

补充这些能力时要平衡成本和收益，不是所有生产化能力都必须立即实现。

### 75. 如果继续优化，优先做什么？

我觉得优先级应该从"最影响用户体验"和"最容易出问题"两个维度来排序，而不是追求技术上的完美。

首先是数据一致性增强。当前文档入库和索引更新使用的是内存异步任务，如果应用重启或者崩溃，正在执行的任务会直接丢失。这在生产环境是最大的风险，因为用户上传了文档，系统显示"处理中"，结果后台任务丢了，用户永远等不到结果。我的做法是增加任务表或者接入消息队列（比如 RabbitMQ 或 Kafka），把异步任务持久化下来，失败了可以重试，重启了可以恢复。这个改动成本不高，但能大幅提升可靠性。

其次是监控告警完善。当前有 trace 和日志，但缺少统一的指标采集和告警规则。线上出问题了，只能靠用户反馈或者手动翻日志，排查效率很低。我认为应该对接 Prometheus + Grafana 或者类似方案，采集调用链追踪（Trace）、关键指标（Metrics，比如请求量、延迟、错误率、向量库 QPS）、日志聚合（Logs），并且配置告警规则（比如错误率超过 5%、P99 延迟超过 3 秒就告警）。这样可以快速发现和定位问题，而不是等用户投诉。

第三是长期记忆持久化。当前的会话历史压缩是会话级的，用户换个会话或者过几天回来，之前的上下文就丢了。如果要做真正的个性化助手，需要增加用户级、知识库级的长期记忆，持久化到数据库或者向量库。比如用户之前问过什么、偏好什么样的回答风格、哪些知识点经常访问，这些信息可以用来优化后续的回答质量。实现上可以参考 Mem0 或者 LangChain Memory 的设计，定期提取重要信息，存储到独立的记忆库。

第四是安全加固。当前 API 有基础的 JWT 鉴权，但缺少限流、敏感数据脱敏、完善的审计日志、工具调用权限的细粒度控制。比如用户可能恶意调用 Agent 工具、上传超大文件、频繁请求导致资源耗尽。我认为应该增加 API 限流（Spring Cloud Gateway 或 Sentinel）、敏感字段脱敏（日志和返回结果中不输出密码、token）、审计日志（记录谁在什么时间做了什么操作）、工具权限白名单（每个用户或租户只能调用授权的工具）。

最后是性能优化。当前是单机线程池，没有租户级并发控制和全局限流。如果多个用户同时发起大量请求，可能导致线程池耗尽、数据库连接池打满、向量库 QPS 超限。我的做法是增加租户级并发控制（每个租户最多并发 N 个请求）、全局限流（整个系统最多承载 M 个请求）、缓存预热（启动时加载热数据到 Redis）、慢查询优化（分析 MySQL 慢日志，添加索引或改写查询）。

我认为优先级取决于当前阶段目标。如果是产品验证期，优先打磨核心体验（检索准确率、回答质量、响应速度），用户体验好才能留住用户。如果是生产化准备期，优先补齐可靠性和可观测性，保证系统不出大问题、出问题能快速定位。如果是规模化扩展期，优先提升性能和成本效率，降低单次请求成本、提高吞吐量。不同阶段优化重点不同，不能一上来就追求大而全。

**可能追问：**

- **规划质量如何量化？**：准确率（该拆的拆了、不该拆的没拆）、边界清晰度（子任务无重叠）、依赖正确性（前置关系合理）、完成率（拆分后任务能完成）。
- **证据融合具体怎么做？**：每个 chunk 标记来源文档 ID、页码、检索分数、证据类型（事实/意见/代码）；合成时按分数排序，相同事实多来源标记高置信度，冲突事实列出所有来源让模型判断或标记不确定。
- **为什么不优先增加功能？**：当前核心链路已有，增加功能前要先保证质量可衡量、故障可恢复、成本可控制；否则功能越多问题越多。

**补充细节：**

优化优先级：质量评估 > 证据融合 > 任务可靠性 > 统一观测 > 新功能。

每项优化要有明确的验证指标：规划评估看误拆率和完成率、证据融合看忠实度和冲突标记、任务可靠性看故障恢复率、统一观测看告警准确率。

### 76. 用一句话总结项目价值

核心价值是：**把原本不稳定的 LLM 和 RAG 能力，通过 Agent 编排、检索优化和工程化保障，落地成一个可控、可追踪、可持续使用的企业研发知识助手。**

具体来说，主要有三个方面。

第一是**提升知识获取效率**。传统研发过程中，很多技术信息分散在文档、接口说明、历史方案中，开发人员需要花大量时间查找。通过 RAG 检索和 Agent 能力，系统可以根据用户问题自动定位相关资料，并进行整理和总结，而不是简单返回一堆文档。

第二是**提升 AI 系统的可靠性**。普通的大模型应用更多关注能不能回答，但实际使用中更关注答得是否可信。因此项目中通过 Supervisor-Worker 任务编排、工具白名单、执行轮次限制、反思和失败降级机制，让 Agent 的执行过程更加可控；同时通过 BM25 + 向量检索 + RRF + Rerank，提高知识召回的准确性。

第三是**解决 AI 应用工程落地的问题**。企业系统不仅需要效果，还需要可维护和可排查。因此项目中增加了 Trace 记录、RAG 评估、检索降级、多存储最终一致性和失败补偿机制，让系统出现问题时能够定位原因并恢复，而不是只能依赖人工排查。

所以这个项目并不是简单地把大模型接入知识库，而是围绕企业场景，把模型能力包装成一个更稳定、更可靠的后端服务，让研发人员真正愿意使用。

### 77. 60 秒项目介绍模板

快速建立三个认知：这是什么、为什么做、我做了什么。

这是一个面向研发团队的知识库智能助手，我负责后端和部分全栈开发。系统基于 Spring Boot 3，使用 MySQL 管理文档和任务，Lucene BM25 加 ChromaDB 做混合检索，Redis 做缓存，Vue 3 和 SSE 提供交互。
研发团队每天要查大量文档、API、设计方案，传统搜索只能关键词匹配，大模型又容易幻觉，单独用都不够准确。我们希望通过 RAG 结合精确检索和语义理解，让系统能准确回答技术问题，并且能追溯答案来源，避免误导。
项目难点有三个：第一，复杂故障问题不能全部交给单 Agent，所以用 Supervisor-Worker 拆分任务，配合预算控制和失败回退，避免无限调用和不可控。第二，研发问题同时需要精确术语匹配和语义匹配，所以使用 BM25 加向量检索、RRF 融合和 rerank 排序，并且设计了逐层降级策略，保证检索服务故障时仍能基本可用。第三，文档向量化依赖外部服务，所以 MySQL 和 BM25 同步写入，Chroma 异步写入，并用状态机和补偿任务处理最终一致性，避免索引和数据库长期不一致。
最后通过参考 RAGAS 的四类指标（Context Precision、Context Recall、Faithfulness、Answer Relevancy）和消融实验验证检索策略，而不是只凭主观感受调参。

### 78. 项目失败案例模板

要能说清楚”出了什么问题、为什么会出问题、系统怎么处理的、我学到了什么”。

选择一个真实的故障场景，比如 Supervisor 返回了带中文前缀的非法 JSON。具体情况是这样的：有一次测试时，用户提了一个复杂问题，Supervisor 规划后返回的 JSON 前面多了一段中文解释，类似”我认为应该这样拆分：{...}”，导致 JSON 解析直接失败，整个 Agent 链路卡住了。

为什么会出现这个问题？因为我们用的是 LLM 生成 JSON，虽然 prompt 里明确要求”只返回 JSON，不要其他文字”，但 LLM 有时候还是会”好心”地加上解释。这暴露了两个问题：第一，过度依赖 LLM 的输出格式，没有做足够的防御；第二，错误处理不够完善，解析失败后直接抛异常，用户看到的只是”系统错误”，完全不知道发生了什么。

系统当时怎么处理的？我们在 `SupervisorService` 里加了 JSON 清洗逻辑，用正则表达式提取第一个完整的 JSON 对象，过滤掉前后的多余文字。同时增加了 trace 记录，把 LLM 原始返回、清洗后的 JSON、解析结果都记录下来，方便排查。如果清洗后还是解析失败，就回退到单 Agent 模式，至少保证用户能拿到一个基本答案，而不是直接报错。

这个案例让我学到了什么？第一，LLM 输出永远不可信，必须做防御性解析和兜底方案。第二，错误处理要分层：解析层失败不应该直接影响业务层，应该有降级和回退机制。第三，完整的 trace 非常重要，没有 trace 的话，这种问题根本查不出来，只能靠猜。第四，测试用例要覆盖 LLM 返回异常格式的场景，不能假设 LLM 总是按预期返回。

我认为讲失败案例不是为了暴露缺点，而是为了展示你的问题解决能力和从错误中学习的能力。好的失败案例应该包含：问题现象、根因分析、解决方案、预防措施、经验总结。不要虚构”线上事故”或者没有证据的收益数据，如实说明这是测试或开发环境的故障注入案例更有说服力。

**可能追问：**

- **为什么不用 JSON Schema 强制约束？**：我们确实用了 JSON Schema 验证，但 LLM 有时候会在 JSON 前后加文字，这发生在解析之前，Schema 验证不到。清洗逻辑就是为了处理这种”格式正确但位置不对”的情况。
- **如果清洗也失败了怎么办？**：记录完整的原始返回和错误信息到 trace，然后回退到单 Agent 模式。用户会收到一个基本答案，虽然可能不够准确，但至少有结果。后续可以根据 trace 分析，优化 prompt 或者增强清洗规则。
- **这个问题频率高吗？**：开始时比较高，大概 5-10% 的复杂问题会触发。优化 prompt（增加示例、强调格式）和清洗逻辑后，降到了 1% 以下。剩下的情况基本都能被清洗逻辑处理，真正失败回退的不到 0.1%。

## 十、代码映射

- Supervisor 规划与 JSON 清洗：`backend-java/src/main/java/com/rag/notebook/agent/SupervisorService.java`
- Agent 循环、预算和状态：`backend-java/src/main/java/com/rag/notebook/agent/AgentLoop.java`
- 上下文 Token、长结果和摘要压缩：`backend-java/src/main/java/com/rag/notebook/agent/ContextManager.java`
- 会话上下文：`backend-java/src/main/java/com/rag/notebook/agent/ConversationContextManager.java`
- 混合检索、RRF、降级：`backend-java/src/main/java/com/rag/notebook/rag/HybridRetriever.java`
- 文档解析、MD5、同步入库和异步任务触发：`backend-java/src/main/java/com/rag/notebook/rag/DocumentProcessor.java`
- 异步 Chroma 写入、批处理和重试：`backend-java/src/main/java/com/rag/notebook/rag/DocumentTaskExecutor.java`
- 切片和 overlap：`backend-java/src/main/java/com/rag/notebook/rag/RagChunker.java`
- RAG 指标评估：`backend-java/src/main/java/com/rag/notebook/evaluation/service/EvaluationService.java`
- 消融实验：`backend-java/src/main/java/com/rag/notebook/evaluation/service/AblationExperimentService.java`
- Agent 任务、步骤和 trace：`backend-java/src/main/java/com/rag/notebook/agent/` 下对应任务与 trace 实体/服务
- SSE 事件与任务接口：`backend-java/src/main/java/com/rag/notebook/agent/controller/AgentController.java`

